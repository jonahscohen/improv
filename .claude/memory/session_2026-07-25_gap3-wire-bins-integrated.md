---
name: Gap 3 integrated - drift flow-wired into audit, 5 bins discoverable; loop nearing completion
description: wire-bins integrated - sidecoach-drift genuinely wired into flowK (audit) Theming lens (replaced a placeholder), the other 5 bins (palette/roll/preauthor/deck/refs) made discoverable in list/help + SKILL with honest reasons they're not flow-invoked. 86 suites green. This closes Gap 3. Remaining: honest trivial tail + push.
type: project
relates_to: [session_2026-07-25_wire-bins-discoverable-drift-flow.md, session_2026-07-25_waveA-integration.md]
author_human: Jonah
author_model: claude-opus-4.8
source: session
verified: gate - build clean, 86 suites; drift flow-wire probe 15/15 (invoked, fail-closed, contained); list/help show all 6; foreground Codex 4 folded
confidence: high
---

Collaborator: Jonah. 2026-07-25. /loop Gap 3.

## Honest integration depth (no overclaim)
- **drift = FLOW-INVOKED** (the one genuine wire): flowK (multi-lens audit) Theming lens shelled out to bin/sidecoach-drift.js. flowK's Theming already CLAIMED "token consistency (no hardcoded colors)" but shipped a static placeholder; drift measures exactly that vs DESIGN.md, and flowK's canExecute already requires projectPath (drift's only input) - zero fabrication. Wired into the directory-target/guidance path, NOT the URL read-path (that's in the orchestrator core, out of scope) - stated, not overclaimed. Fail-closed + contained: bin failure -> null -> static placeholder retained, no flow crash. Probe 15/15.
- **5 DISCOVERABLE** (palette/roll/preauthor/deck/refs): now in `sidecoach list`/`help` (grouped generative/governance) + SKILL.md, so they're part of the product surface not orphaned bins. Honest reason each is NOT flow-invoked: authoring aids / human-in-the-loop pickers / no flow-handler execution context carries their input (preauthor needs a brief JSON no handler has; flowG emits guidance and does not build). Forcing a flow call would fabricate inputs - declined.

## Lead actions
Cleared the suppress-fix-gate flag wire-bins set. Rebuilt dist (flow-handler-multi-lens-audit). 86 suites green. Committed.

## Loop status (nearing completion)
- Gap 1 DONE (oversized-h1 fixed, 3 honestly unfixable). Gap 2 KEY-BLOCKED (no provider keys). Gap 3 DONE (this).
- FU1 domain-validation DONE. FU2 = Gap 1. FU3 = Gap 3. FU4 trivial tail + FU5 push REMAIN.
- Trivial tail honest read: 337 historical unindexed beats (low-value - on-disk + searchable), dep-map node removal (cosmetic), darwin-only containment port (Linux-only, works on darwin), visual-arm residual (minor edge). Mostly low-value housekeeping to be named, not padded.

## Files
- committed: bin/sidecoach.js, src/flow-handler-multi-lens-audit.ts, claude/skills/sidecoach/SKILL.md, dist, beats.
