---
name: Stage-0 lead independent verification - eval-integrity linchpin behaviorally confirmed + shared-worktree topology
description: Mid-Stage-0 lead pass over the architect's Sidecoach Phase 2 work. Independently RAN the corpus-integrity suite (ALL PASS) and confirmed the freeze gate behaviorally REJECTS author==labeler circularity + unregistered-author classes - the guard that makes the head-to-head eval non-circular. Also discovered the lead + architect share ONE worktree, currently on the architect's branch.
type: project
relates_to: [feedback_sidecoach_mission_beat_oracle.md, session_2026-06-23_sidecoach-plan-independent-gate.md]
---

Collaborator: Jonah Cohen

## Shared-worktree topology (important for coordination)
`git worktree list` shows ONE worktree (`/Users/spare3/Documents/Github/improv`) currently checked out to `sidecoach-phase2-reimplement @ 1c269efe` - NOT `main` (the session-start git snapshot was stale). The lead and the `sidecoach-architect` teammate share the SAME working tree + branch checkout: when the architect commits, HEAD moves for the lead too. Implication: the lead reads the architect's Stage-0 files directly (no branch checkout needed); be careful not to disrupt the architect's branch state. My uncommitted hook/beat edits ride along as working-tree changes regardless of branch.

## Independent verification of the eval-integrity linchpin (behavioral, not just file-level)
The labeler division of labor (Jonah's ruling: independent Codex labels subjective, architect never sets them) is the linchpin that makes "beats oracle" non-circular. Verified at two levels myself:
- FILE: `sidecoach/eval/corpus/rule-authors.json` registers all 15 subjective classes to `sidecoach-architect`, so `corpus-tool.verify` rejects any subjective label with `labeledBy=architect`. Confirmed by eye.
- BEHAVIORAL: ran `node sidecoach/eval/corpus-tool.test.mjs` myself -> `ALL PASS` (exit 0). The deliberate "VERIFY FAIL" cases confirm the gate actually fires: `author==labeler rejected` (alice/gradient-text), `unregistered-author class rejected` (icon-tile-stack), locked-case-removed detected, split-move heldout->dev detected, post-freeze file-content tamper detected (hash mismatch), canonical hash collision-safe + order-independent. So the freeze gate genuinely blocks circularity + tampering - not just claimed.
- The secondary `propose-label --kind subjective` refusal bailed on a missing candidate ("no candidate testcase") so I did NOT behaviorally confirm it; re-check at the Stage-0-completion gate with a real candidate. The freeze-gate guard (above) is the un-bypassable one and is confirmed.

## The 15 subjective classes (now concretely named in rule-authors.json)
cream-palette, ai-color-palette, hero-eyebrow-chip, repeated-section-kickers, numbered-section-markers, icon-tile-stack, italic-serif-display, nested-cards, side-stripe-borders, glassmorphism-default, hero-metric-template, gradient-text, marketing-buzzword, aphoristic-cadence, dark-glow. These are the taste/idiom classes (Codex-labeled, held); the 15 OBJECTIVE classes are WCAG/CSS spec-math (architect-computed).

## Lead sourcing guardrails relayed + folded (commit df73929b, architect)
The 5 guardrails I sent were folded + verified by the architect. Guardrail #1 (oracle-exclusion - the independence-critical one) is now DOUBLE-LOCKED: (a) capture TOOL-REFUSES oracle-host URLs (architect-verified oracle.style + github.com/oracle refused, HN/Stripe allowed) AND (b) the joint manifest review is the human backstop for rule-designed pages the tool can't detect from a URL. Diversity recorded as diversity.{quality,era} per case (audited at review, not a defect label). Brief N corrected to power-locked (>=20 floor, target 20-30) across registers + 3 calibration types. To independently confirm the tool-refusal at the gate; the human-review backstop means the oracle can't enter the test set even if the tool-refusal had a hole.

## Deferred to the Stage-0-completion gate (my named checklist items)
1. Re-confirm the `propose-label --kind subjective` refusal with a real candidate (exit 1 + refusal message).
2. Independently confirm the oracle-exclusion capture-refusal (oracle-host URL -> refused) with a real attempt.
3. Codex item-8 on the candidate-sourcing tooling + the full migration harness (architect folds it into Stage-0-completion review; linchpin already had a mid-stage Codex pass).
4. The joint candidate-manifest independence review (lead + Jonah) before freeze.

## MIGRATION HARNESS - independent lead gate PASSED (criterion-2 surface, commit ~11 Stage-0 commits)
The architect completed all 5 subsystem compatibility-contract goldens (scanner=Stage2, routing=Stage4, buildreport=Stage3, convergence=Stage3, reference=Stage5), Codex item-8-clean over 2 rounds (round 1: 2 BLOCKER + 4 MAJOR incl convergence using an INVALID status 'fail' -> degenerate goldens; round 2: 2 NEW producer-seam parity bugs from the rework; all 8 folded). This is the deletion-safety backbone for every Stage 2-5 destructive deletion, so I ran a real independent gate (not a trust-the-report ack), since Codex item-8 already covered the cross-model level:
1. ALL 5 verify IDEMPOTENT (current == golden), exit 0, ran myself. PASS.
2. PRODUCER SEAM IS LIVE (the property that makes it a contract, not theater): each snapshot exposes `verify(producer)` defaulting to the current module but injectable with the new one at deletion time. I injected a BOGUS producer into scanner + routing and verify FAILED (ok=false) while default passed (ok=true) -> the seam genuinely exercises the INJECTED module, not secretly the old one. Proven behaviorally on scanner (Stage 2, the first/most-important deletion) + routing; the verify(producer) pattern is identical across all 5. PASS.
3. CONVERGENCE golden NON-DEGENERATE: top-level = 4 distinct named scenarios (stall / converge / progress_then_converge / error_running), each a real boundary-eval step sequence with valid validator statuses; the round-1 invalid-'fail' degeneracy is fixed and the error path is present. PASS.
VERDICT: migration harness BLESSED as a genuine compatibility contract. De-risks Stages 2-5; the new owned module must reproduce these goldens (via the live seam) before each old module is deleted. (Buildreport/convergence/reference seam-injection to re-confirm at the Stage-0-completion gate for completeness; pattern-identical so structurally covered.)

## CORPUS SELECTION-BIAS judgment call + lead resolution (the "0 captures" was this, not a capability block)
Captures sat at 0 not because the architect can't web-source (it validated capturing gov.uk@2019 via web.archive.org `id_` -> 31KB clean, oracle+labeler run) but because it was circling a genuine INTEGRITY call it hadn't named: the no-pre-filter rule + joint review guard the LABELS, but the SET OF URLS the rule-author picks is itself a bias variable (unilaterally choosing ~90 sites risks home-field tilt). It surfaced this when I sent the stall-check + worktree-grounded direct question. Good catch by the architect; the lesson (relayed) is to surface such calls proactively, not circle them (a bare idle reads as a silent block from the lead side).
ARCHITECT'S RESOLUTION (validated): source by a RULE-AGNOSTIC method - web.archive.org `id_` snapshots (original HTML, no wayback chrome), fixed date-slices + breadth across registers/quality/era, selection-method recorded in provenance for the joint review to audit; mix in live current pages for currency.
LEAD DECISION: APPROVED the archive-method (consistent with Jonah's "architect web-sources + joint review" ruling, and strictly better than freehand selection) + 3 mandatory reinforcements:
1. PRE-REGISTER the selection method BEFORE bulk capture - freeze the domain universe + date-slices + register/quality/era quotas in a committed record, so it can't be steered post-hoc; joint review audits result-vs-method. "Rule-agnostic" only holds if the method is fixed before captures are seen.
2. CURRENCY FLOOR (mandatory, not "mix in some") - archive skews OLD, but taste-currency is a key differentiator we must be able to test; set a first-class quota of current (2025-2026) live pages per register. Don't let archive-convenience bias the corpus old.
3. ARCHIVE-VALIDITY CHECK - verify each archived capture renders as the ORIGINAL intended (CSS actually inlined/present, not a broken-archive artifact) before it's a case; archival breakage mislabeled as a design defect would poison labels. Reject incomplete-archive captures.
Plus: harden the script-strip before bulk (architect's flag 1, 4 `<script>` survived - harmless for static scanning but do it); flag 2 (bulk ~90 sites is multi-turn + network-flaky, criterion 1 several batches out) is fine - keep grinding, surface at target. Seed-list option (Jonah/lead supplies URLs for maximal independence) left OPEN for Jonah but NOT required - the auditable method satisfies his ruling.

## SELECTION-METHOD pre-registration reviewed (commits 622bd2bc strip-harden, ad7c08a1 3-reinforcements, 33bf89db first 5-case batch)
The grind started in the exact order directed: selection-method.md frozen-before-capture, 3 reinforcements implemented concretely, first 5 objective-labeled cases captured (off 0). Read selection-method.md as the lead while small (it governs all ~90 captures; reviewing at 5 catches flaws cheaply). STRONG: >=50% current-live per register currency quota (concrete, not "mix in some"), archive-validity in validateCapture (reject broken-archive/missing-CSS), rule-agnostic domain universe by CATEGORY (gov/edu/enterprise/small-biz/personal/news/OSS/product) not detectability, exclusions incl host-refused oracle, per-case provenance + selection-slot, seed-list supersedes.
ONE REQUIRED CLARIFICATION (sent, before bulk - it's a clarification not a re-freeze; does NOT change which URLs are picked): the method sets ~8-defect/~8-good per register but never says HOW the defect/good split is determined. It MUST be LABELER-DETERMINED, never architect-quality-judged: objective case = spec-math labeler finds >=1 objective defect, clean = 0; subjective dimension added by the held Codex pass. Architect sources rule-agnostically + lets labels fall where they fall, topping up quota by sourcing MORE rule-agnostically (never by picking pages that "look defective"). If a register can't yield ~8 defect-bearing rule-agnostically, REPORT it as a finding, don't cherry-pick. diversity.quality stays an audit marker, not a label. (Without this, "source 8 defect per register" implies the rule-author pre-judging quality = the exact bias the method removes.)
MINOR (confirm at joint review, not blocking): architect-authored calibration briefs (oracle-should-win / tie / Sidecoach-should-win) carry a mild conflict (rule-author deciding what each looks like) - keep separate from real-public A5 briefs + extra scrutiny at review; confirm heldout-vs-dev split methodology for known-good + briefs at the review.

## LABELER GROUND-TRUTH VALIDITY - architect paused bulk (PROACTIVE catch, the right call) + lead direction
Applying the labeler-determined-split rule (a59b8c14) exposed the v1 objective labeler is context-BLIND and not trustworthy as ground truth. Evidence (10 real captures): 9 defect / 1 good = 10% known-good yield (40-good quota unreachable) AND the labels are WRONG: gov.uk (GDS gold-standard) flagged tight-leading though every <1.3 line-height is on HEADINGS (.govuk-heading-* @ 1.04-1.25 = correct heading typography, not a body defect); all-caps-body ANDs two unrelated facts (uppercase utility + an unrelated long <p>); layout-transition fires 6/10 (intentional - a taste call, not spec-math). ROOT CAUSE: the labeler conflates "CSS value present" with "defect in context." Grinding 90 captures against it would poison A1-A4 ground truth for BOTH our scanner and the oracle. The architect paused PROACTIVELY (the coaching landed) - correct.
LEAD DIRECTION (sent): (1) APPROVED the context-aware rewrite (parse selector{decls}, scope each rule to element context - body-leading not heading). (2) Objective/subjective BOUNDARY re-scope: architect proposes a split, lead signs off + Jonah spot-checks (per the labeler ruling). TEST for objective: computable by spec-math with proper element context such that two independent spec-faithful impls agree WITHOUT taste; if "is it defective HERE" needs aesthetic judgment -> SUBJECTIVE (held Codex pass). By that test: layout-transition + bounce-easing -> subjective; keep contrast/broken-image/justified-body/extreme-tracking-on-body/tight-leading-on-BODY objective; tiny-text + skipped-heading borderline (propose with reasoning). (3) INTEGRITY: any class moved objective->subjective MUST be added to rule-authors.json (architect registered) so the freeze gate rejects architect-labeling it - more classes independently labeled = more conservative. (4) VALIDITY BAR before resuming bulk: calibrate the rewritten labeler BIDIRECTIONALLY - 0 objective defects on a known-CLEAN gold-standard set (gov.uk/GDS + others) AND catches planted defects on a known-DEFECTIVE fixture set; ground each rule in the public SPEC (WCAG SC / CSS spec) so it's a neutral referee independent of both detectors-under-test, not a re-impl of either. (5) Codex item-8 on the rewritten labeler (it IS the ground-truth oracle). Sequence: rewrite -> send proposed boundary split + bidirectional calibration -> lead sign-off + Jonah spot-check -> THEN resume bulk.
Also landed: SPA empty-shell guard in validateCapture (grafana 16KB "starting up" shell passed the CSS check - good catch, another validity guard); method amended+committed.
REFINEMENT (architect proposed, lead approved): DECOUPLE capture from labeling. Capture is rule-agnostic + label-independent, so the capture grind runs IN PARALLEL with the labeler bug-fix - loses no wall-clock time. Sharpens the earlier "pause bulk": the bidirectional-calibration bar gates LABELING/FREEZING, not CAPTURING. During the parallel grind enforce only the capture-time quotas (currency floor, register breadth, era, archive-validity); the defect/good split + final objective labels apply AFTER the labeler is fixed+calibrated, with rule-agnostic top-up sourcing if a register's yield is short. No labels frozen until calibration passes.
BOUNDARY RULING (lead, the 2 clear classes; Jonah spot-checks the full split): layout-transition + bounce-easing -> SUBJECTIVE (held Codex pass) - both need taste judgment ("is animating layout/bounce a defect HERE" - usually intentional), so not objective spec-math. Both added to rule-authors.json (architect registered) so the freeze gate rejects architect-labeling them. Borderline classes (tiny-text, skipped-heading) still come in the architect's full proposed split for lead sign-off + Jonah spot-check.

## SPEC-CRISPNESS BOUNDARY adopted (architect's refinement, lead-adopted; reshapes Contract-6; Jonah informed for spot-check)
After the context-aware bug-fix (ed4999cd, 27 tests) gov.uk STILL flagged tight-leading - because its compact components legitimately run 1.15-1.25 leading. Root insight: typographic thresholds (tight-leading<1.3, tiny-text<12px, wide-tracking>0.05em) are TASTE-calibrated, NOT spec-defined, so they can't separate defect from fine -> you can't measure A2 precision/false-positives without genuinely-clean pages, and almost no real page is "clean" under fuzzy thresholds. So draw the objective/subjective boundary by SPEC-CRISPNESS, not class type (sharper than my class-type test - it corrected my earlier "extreme-negative-tracking objective" lean; "extreme" is a fuzzy threshold).
- OBJECTIVE (architect spec-math, graded A1-A4 parity = the FLOOR): crisp-spec accessibility set - low-contrast (WCAG 1.4.3), gray-on-color, broken-image, target-size (2.5.8), skipped-heading (1.3.1), justified-text (1.4.8 AAA).
- SUBJECTIVE (held Codex pass, graded A5 strictly-better = the DIFFERENTIATOR): the taste set - tight-leading, wide-tracking, tiny-text, extreme-negative-tracking, all-caps-body, layout-transition, bounce-easing (PLUS the original 15 idiom classes).
WHY ADOPTED: faithfully implements Jonah's success bar (parity on a11y-crisp floors / strictly-better on taste differentiators), makes the objective labeler trustworthy (gov.uk stops being "defective"), and is the honest operationalization of "you can't measure precision on fuzzy labels." Test for objective = a spec CONSTANT both faithful impls must agree on (4.5:1), not a taste-calibrated threshold (1.3 leading).
OPEN THREAD (asked architect to specify in the Contract-6 update): the subjective classes still get Codex labels, so SPECIFY how they're graded COMPARATIVELY vs oracle (detection-agreement against Codex ground truth? A5 generative? both?) so we don't lose the taste head-to-head. skipped-heading = softest objective member (1.3.1 real spec but legit component exceptions) - keep objective, revisit if it FP-noises. CONSEQUENCE: register ALL newly-subjective classes in rule-authors.json (freeze-gate coverage); my Codex labeling pass now covers the larger subjective set. VALIDATION: post-boundary, re-run objective labeler on gov.uk + gold-standard set -> must yield 0 OBJECTIVE defects (proof the boundary fixed ground truth) + planted-defective caught.

## RENDERED-PASS OBJECTIVE LABELER ruling (Codex item-8 caught regex referee unreliable; lead APPROVED rebuild)
My prior flags all RESOLVED (commit aa5d259e): spec-crispness boundary applied (tight-leading + extreme-negative-tracking correctly -> subjective), all 7 newly-subjective registered in rule-authors.json (22 total, freeze gate covers all), grading spec written (A5a taste-DETECTION head-to-head vs SAME Codex labels [deterministic diffs strict-better by pass/fail; graded taste strict-better by significance; per-commit CI] + A5b generative head-to-head [blind briefs, judges, CI lower-bound>0]; both must clear - strong answer to "don't drop the taste comparison"), bidirectional calibration passes on the current narrow set.
THE FINDING (architect's own Codex item-8, verified): the STATIC REGEX labeler is not a trustworthy referee - scans HTML comments (`<!-- <img> -->` FP), `\bsrc` matches data-src (FN on lazy img), `\bstyle` matches data-style (FP), `--text-align:justify` custom-prop (FP), unquoted attrs missed, and the decisive one: CASCADE/SPECIFICITY/INHERITANCE can't be regex-resolved so justified-text isn't tied to the APPLIED style. Passes narrow calibration, fails adversarial cases. The referee must be MORE reliable than the detectors it grades; a regex referee isn't.
LEAD RULING: APPROVED building the OBJECTIVE labeler on the RENDERED pass (Playwright real DOM + getComputedStyle) - the browser resolves comments/quoting/data-*/cascade/specificity/inheritance/hidden/ARIA authoritatively; broken-image=naturalWidth===0/no resolvable src; justified=computed text-align; skipped-heading=DOM h1-h6+[role=heading] hidden-aware; +contrast/gray-on-color via computed colors (WCAG 1.4.3). Correct + not scope creep: it's the SAME Playwright pass contrast already requires, and the static captures (script-stripped, self-contained) render deterministically. This is the CORRECT TERMINAL design for the referee (browser = ground truth; can't do better than computed styles). REINFORCEMENTS I added: (a) PIN DETERMINISM (fixed viewport + Chromium version, animations/transitions disabled at render, fonts pinned/fallback) - a non-deterministic referee = non-reproducible ground truth; (b) LOCK Codex's adversarial cases as PERMANENT calibration+regression fixtures (comment-img, data-src, --text-align, .prose{justify}{left}) so no future labeler change regresses them; (c) DEFINE the contrast methodology for NON-SOLID backgrounds (gradient/image/alpha behind text) - a defensible defined rule or exclude-with-flag, never silent guessing; (d) Codex item-8 AGAIN on the rebuilt referee; (e) full bidirectional calibration (gold-standard 0 + planted caught + adversarial fixtures pass) before ANY labels freeze. The static labeler stays as the prototype that nailed the CLASS SET; the rendered pass is the referee.
META: this is the referee being hardened through successive adversarial passes (v1 context-blind -> v2 context-aware static -> v3 rendered), each driven by a VERIFIED defect - convergent hardening, not thrashing. The rendered pass is the terminal design; once it clears Codex item-8 + full calibration, the referee is SETTLED and bulk labeling proceeds (no further redesign absent a NEW verified defect). The labeler is the A1-A4 validity lynchpin, so this is the highest-value Stage-0 investment. Capture grinds in parallel; still 10 captures, NO labels frozen.

## RENDERED LABELER - independent lead verification PASSED (the A1-A4 referee is trustworthy)
The architect built the rendered objective labeler (a4503a8a, commit "terminal referee"); captures grew to 17 (batch-3, domain-diverse). I ran an independent lead gate on it (it's the A1-A4 validity lynchpin), not trust-the-commit:
- RAN labeler-calibration-rendered.mjs MYSELF -> green on all 3 axes: (a) CLEAN gold-standard (gov.uk/w3.org/MDN/NNgroup) -> 0 objective defects (the gov.uk FP that started this is FIXED); (b) PLANTED broken-image/justified/skipped-heading/low-contrast -> caught, clean controls silent; (c) ADVERSARIAL (Codex item-8 cases as permanent regression fixtures) -> ALL pass, including the decisive one: `.prose{justify}` then `.prose{left}` correctly resolves to NOT-justified because the real browser resolved the cascade regex fundamentally couldn't. data-src lazy caught, comment-img/custom-prop FPs gone, sr-only not contrast-checked.
- REINFORCEMENTS independently confirmed: #1 determinism (referee header: pinned viewport 1280x800 + reduced-motion + animations zeroed + external aborted + srgb; "font-metric-independent classes"); #2 adversarial cases locked as permanent regression fixtures (in the calibration); #3 non-solid-bg contrast (lines 26-29: alpha-composite bg up the ancestor chain to first opaque layer; background-image/gradient before opaque -> CONTRAST-INDETERMINATE, EXCLUDED-WITH-FLAG, never silent-guessed); #5 full bidirectional calibration passes. All exactly as specified.
- OPEN (the only one): #4 Codex item-8 on the REBUILT rendered referee - not yet confirmed run (commit doesn't mention it). Asked the architect to run it; once clean, the referee is SETTLED and bulk labeling proceeds.
VERDICT: the rendered referee is independently trustworthy. The v1->v2->v3 hardening converged exactly where it should (browser = ground truth). Highest-value Stage-0 piece, now verified. Still NO labels frozen.

## REFEREE SETTLED - Codex item-8 (13 findings) folded + lead re-verified post-fold (ab14db6c)
Reinforcement #4 done: the architect ran Codex item-8 on the rebuilt rendered referee -> 13 findings, ALL folded (ab14db6c, "calibration green"). 13 findings into the lynchpin is a big change, so per "fold every finding + re-verify the WHOLE unit" I RE-RAN the calibration myself -> still GREEN on all 3 axes, no regression. The adversarial fixture set GREW with the 13 as permanent regression fixtures - sophisticated cases now handled: ARIA heading levels (aria-level), role=presentation stripping heading semantics, inert subtrees excluded from outline, multi-layer translucent bg compositing (red/blue/white stack), partial-ancestor-opacity -> indeterminate (not wrong-flag), inline-span vs block justify, JS-disabled determinism, contrast on <code> (broadened text detection). gold-standard still 0, planted still caught.
VERDICT: all 5 reinforcements satisfied + independently verified. The A1-A4 ground-truth referee is SETTLED + locked (no further redesign absent a new verified defect). Released bulk labeling: run the full corpus through the settled referee + resume capture to the ~90 target. This was the hardest + highest-stakes Stage-0 piece; it's done. Next milestone = the full candidate manifest for the joint independence review (Jonah + lead).

## KNOWN-GOOD YIELD watch-item (raised to architect; not yet a blocker)
Bulk grind producing (referee settled, corpus 22/90 all labeled, split 15 defect / 7 good = 32% known-good - up from the buggy-labeler's 10%). But: under crisp a11y objective rules, genuinely-clean pages (0 objective defects) are RARE - most real pages have some contrast/heading issue. So hitting 40 known-good rule-agnostically may need FAR more than 90 captures (40/0.32 ~ 125), OR tempt cherry-picking pristine gold-standard pages to hit quota = a known-good REPRESENTATIVENESS bias (known-good set becomes only-gold-standards, not real-clean-across-registers). Flagged: track the known-good yield; do NOT cherry-pick clean/gold-standard pages to fill the quota; if rule-agnostic sourcing can't reach 40 good without skewing the set, SURFACE it as a finding (a11y-clean pages are rare) - we'd adjust the target / known-good definition (e.g. smaller good-N with rationale, or "0 defects in primary content") rather than bias the set. The labeler-determined-split + rule-agnostic top-up still governs; this is its binding constraint.

## KNOWN-GOOD DEFINITION RULING - Jonah chose B (primary-content-clean), 2026-06-23
The finding triggered (architect surfaced at 29 captures, exactly as flagged): yield 34% (10/29 -> ~116 captures for 40 good); the 10 clean skew PRISTINE (gov.uk/w3c/MDN/rust/nngroup/a11yproject/github-login/stackoverflow/bootstrap/basecamp); dominant defect low-contrast (15/19) is mostly PERIPHERAL chrome (footers/disabled/brand-buttons), not primary content. I took the 3-option adjustment to Jonah via AskUserQuestion (I'd committed to looping him on a target change). JONAH CHOSE B: refine "known-good" = 0 objective defects in PRIMARY content (ARIA-landmark-scoped); peripheral-chrome defects don't disqualify. (A=smaller-N and C=keep-40-source-120 rejected.)
LEAD SPEC for B (the coherence the architect must nail):
- LANDMARK TAGGING (additive to the rendered referee): tag each defect's region - PRIMARY = main/article/role=main; PERIPHERAL = nav/footer/aside/header/role=contentinfo|banner|navigation|complementary. Mechanical via ARIA + implicit landmark roles, stays objective.
- NO-LANDMARK EDGE CASE (never silent-guess, per the referee discipline): if primary content can't be deterministically identified (no main/article/role=main), the page is NOT eligible as known-good -> excluded-with-flag (doesn't count toward the good quota), rather than guessing a main region or laxly passing it. Architect may propose an alternative DETERMINISTIC rule.
- KNOWN-GOOD INCLUSION = 0 PRIMARY-content objective defects (peripheral defects allowed).
- GROUND TRUTH STAYS FULL-PAGE for BOTH A1 recall AND A2 precision: a detector flagging a REAL peripheral defect on a known-good page is a TRUE positive (it's in full-page ground truth), NEVER an FP. FP = flags minus full-page ground truth, across the whole corpus. B refines the known-good INCLUSION/sourcing criterion, NOT the FP ground truth. (This is the subtlety that keeps the precision measurement honest.)
- A1 recall unchanged: detector must still catch defects ANYWHERE (full-page).
- PROCESS: additive referee change -> re-run full bidirectional calibration (stay green) -> Codex item-8 on the landmark-tagging addition (it touches the lynchpin) -> register the refined definition + rationale ("a11y-clean pages rare; primary-content scoping de-skews + matches design-relevant clean") in selection-method.md/Contract-6 (pre-freeze, recorded, joint-review-audited). Then resume known-good grind under the new definition; defect-bearing + briefs continue.

## RULING B IMPLEMENTED + lead-verified (801023f7); Codex item-8 on the addition pending
The architect implemented ruling B (landmark-scoped primary-content known-good) additively in the rendered referee (match count 1->15). I re-ran the calibration myself (it touches the lynchpin) -> GREEN, no regression, and it now has a new section (d) LANDMARK CLASSIFICATION testing exactly my spec: peripheral-only defect in <footer> -> known-good; primary defect in <main> -> defect-bearing; clean primary+chrome -> known-good; NO primary landmark -> excluded (no guess); and the LOAD-BEARING case - "peripheral defect must remain in FULL-PAGE labels (true positive, not FP) -> full-page has [low-contrast]". So the full-page-ground-truth subtlety is correctly implemented with an explicit assertion. Clean, exactly to spec. Captures held at 36 (focused on the referee change this turn). ONE step left before the referee is re-settled: Codex item-8 on the landmark addition (asked). Then resume the known-good grind under the new definition + register the refined def in selection-method.md.

## RULING B COMPLETE + Codex-verified (ac567fec); modern-skew flagged with a fallback trigger
B fully landed: additive landmarkRegion (PRIMARY=main/article/role=main; PERIPHERAL=nav/footer/aside/header/role=contentinfo|banner|navigation|complementary; nearest landmark wins; explicit ARIA role overrides implicit). classifyKnownGood (primary-clean=good / primary-defect=defect / no-primary=EXCLUDE-WITH-FLAG). Load-bearing #3 locked by a fixture (peripheral defect = TP in full-page labels, never FP; FP=flags minus full-page GT corpus-wide). Calibration GREEN + 7 landmark fixtures. Codex item-8 confirmed the no-mutation invariant + folded 2 findings (shared effectiveRoleRegion; ARIA-role-over-tag precedence: <main role=navigation>=peripheral, <nav role=main>=primary; 2 fixtures). Registered in selection-method.md + Contract-6. Corpus under B (n=36): 16 good / 11 defect / 9 excluded-no-primary; ~59% eligible yield -> 40 good reachable at ~90 captures, back on plan. (I independently re-ran calibration last turn -> green incl the FP-invariant case.)
MODERN-SKEW (architect proactively flagged, not blocking): ~25% of captures lack main/article (older/archive) -> excluded -> known-good skews modern/semantic. TWO things directed: (1) CLARIFY whether excluded-no-primary pages are dropped ENTIRELY or only out of the OBJECTIVE good/defect split but RETAINED for subjective/A5 (era-diversity matters for taste-currency; dropping them entirely would waste the era-diverse sourcing - that severity determines urgency). (2) FALLBACK TRIGGER, conditionally pre-approved under my B-spec "propose a better deterministic rule" door: if at the manifest the exclusion rate stays ~25%+ AND is era-correlated (OR excluded pages are dropped entirely), adopt the architect's fallback (primary = content NOT inside a peripheral landmark; explicit primary landmark still wins when present) - deterministic + spec-defensible - subject to the standard referee gate (re-calibrate green + Codex item-8 + lead independent re-run). Decide at/before the manifest on actual numbers; don't carry an unexamined skew. Since objective a11y classes are largely era-insensitive (contrast is contrast), the skew is likely genuinely mild IF excluded pages stay in the corpus for A5.
DISPOSITION ANSWER = RETAINED (the mild case): excluded-no-primary pages stay in candidates.json (full-page labels, diversity, provenance, subjectiveStatus=pending) - out of the objective good/defect quota only, fully available for the A5/subjective Codex pass. Era-diversity preserved for taste-currency; skew touches the objective known-good set only. TRIGGER DATA (n=53): 30% excluded; era-correlation STRONG + monotonic (2012=86%, 2016=50%, 2020=33%, 2026=18% - semantic <main> common ~2015+); dashboard register 75% excluded (admin templates use divs). Both trigger conditions (>=25% + era-correlated) currently MET. Architect correctly NOT adopting preemptively - deciding at the manifest on final numbers (per my instruction); fallback pre-approved + gated. LEAD AFFIRM + sharpening: the era-correlation is STRUCTURAL (older pages predate semantic HTML - won't wash out; the currency floor's deliberate archive mix guarantees the trigger near-certainly fires at the manifest), so plan to adopt the fallback, not treat it as a coin-flip. AND the stronger reason for the fallback is the dashboard 75%-exclusion = a REGISTER-representativeness gap (we want admin/dashboard UIs in the objective eval; they have real a11y issues) - weigh register-representativeness, not just era. Deferring is still right: re-classification at the manifest is a cheap automated referee re-run, no need to gate a mid-grind referee change. Progress n=53: good 21/40, defect-bearing 16/40 (now lagging), briefs 0/20-30 (starting).

## FALLBACK ADOPTED (7a4855b2) - lead calibration re-run GREEN; Codex item-8 on it = remaining gate item
The architect adopted the pre-approved fallback mid-grind (trigger met + structural, so within pre-approval; applying it now rescues dashboard/excluded yield for the rest of the grind - reasonable). The fallback = primary is content NOT inside a peripheral landmark when no explicit primary landmark exists; explicit primary landmark still wins. I re-ran the calibration myself (referee change to the lynchpin) -> GREEN, and it now has the right fallback fixtures: "no-landmark div content is PRIMARY -> defect-bearing", "fully-chrome page (all content in footer) -> EXCLUDED" (degenerate edge case correctly still excluded, not force-classified), "no-landmark clean div -> known-good"; the role-override + FP-invariant cases still pass. So the lead-independent-re-run half of my gate is met. REMAINING GATE ITEM: Codex item-8 on the fallback change (commit doesn't mention it ran - asked). Also landed: A5 briefs started (54744525 - pre-registered spec + 3 calibration briefs). Captures held at 62 this period (pivoted to fallback + briefs). Once Codex item-8 on the fallback is clean, the referee is re-settled under the fallback; finish captures + briefs -> manifest.

## FALLBACK-v2 + REFEREE LOCKED (1ff1fea5); good-N is a power-governed MANIFEST decision (not pre-sanctioned)
Codex item-8 on the fallback (1ff1fea5): full-page labels confirmed unchanged + 2 real consistency bugs folded: (1) primaryContentIdentified used a hand-maintained tag allowlist that could DISAGREE with the defect pass (a primary-defect page wrongly excluded) -> fixed by tracking eligibility in the same passes/visibility as the defect detector + invariant `eligible = hasPrimaryContent OR primaryDefects>0` (a primary defect always implies eligible, so eligibility can't contradict the labels); (2) isHidden missed off-screen-RIGHT (r.left>=innerWidth) -> all-chrome+off-right page wrongly eligible. I re-ran calibration MYSELF -> GREEN, 3 new fixtures pass (broken-image-only->defect-bearing; form-control-only->eligible; all-chrome+off-right->excluded). REFEREE NOW FULLY LOCKED under fallback-v2 (calibration v1+v2 lead-verified + Codex item-8 done). Re-classified n=62 under fallback: good 24 / defect 38 / excluded 0.
RESIDUAL YIELD GAP (the new situation): even under B+fallback, clean-PRIMARY pages are ~39% (most real pages have a primary low-contrast defect - a REAL finding about web a11y, not a referee bug; referee is gold-standard-clean). So 40 known-good still needs ~100 captures. defect-bearing 38/40 ~done; known-good 24/40 is now binding. CLARIFIED to architect: I did NOT pre-sanction smaller-good-N - Jonah chose B OVER A(smaller-N); B improved yield (31%->39%) but didn't fully close it, so the good-N is a NEW MANIFEST-TIME decision for the joint review (Jonah+lead). And it's POWER-GOVERNED: set known-good N to the pre-registered POWER ANALYSIS requirement for A2 precision (not arbitrary) - smaller-N acceptable only down to the power floor + the "clean-primary pages rare" rationale; if power needs 40, source more. Bring the power number + yield reality + options + recommendation to the manifest. Briefs: 3 calibration done, real briefs sourcing.

## BRIEF DIVERSITY flag (A5 validity) + power-analysis reminder
Briefs at 15 files (commit ee5c8257: 10 real briefs "extracted from GOV.UK Design System patterns" + 3 calibration). FLAGGED: all 10 real briefs from ONE source (GOV.UK Design System) = single-source + government/plain-style skew. A5 is the TASTE differentiator eval; plain accessible government briefs don't exercise the aesthetic range where Sidecoach must be strictly-better, and could bias toward a11y-style answers. Briefs must span REGISTERS (marketing/landing/product/editorial/app-ui/forms) + aesthetic styles from VARIED sources, matching the calibration spread - not all GOV.UK. Also: confirm the briefs are genuine PROBLEMS/specs, not reverse-engineered from a specific solution (which biases toward that solution's style). POWER ANALYSIS still not run (0 artifact) - it's the input to the good-N decision; reminded. Captures held at 62 (briefs focus this period); known-good still binding.

## POWER ANALYSIS done (e95fc260) + BRIEF SOURCING ruling (authored-neutral, but CODEX-authored)
POWER ANALYSIS (e95fc260): floors = known-good 35 / defect-bearing 35 (MET, 38) / briefs 20. So GOOD-N IS POWER-SETTLED AT 35 (not arbitrary smaller-N; reachable at ~90 captures from 24 now at ~39% yield) - the earlier smaller-good-N concern is RESOLVED by the power floor, no separate Jonah decision needed unless 35 proves unreachable. Architect acted on the brief-diversity flag: capped GOV.UK to 3 (flagged plain/government + solution-implied), removed the other 7, spec now requires register+style diversity from varied sources + solution-agnostic + single-source cap. briefs.json = 6 (3 real + 3 calibration); need ~14-17 more to floor 20.
BRIEF SOURCING FORK -> LEAD RULING: real-scrape can't guarantee neutrality+diversity+solution-agnosticism (good challenge sources JS/login-gated; extractable design-system patterns are solution-implied = the bias I flagged). AUTHORED-NEUTRAL approved (architect's reasoning correct: both tools get the SAME brief, so authored-vs-real doesn't bias WHICH wins; real is realism-nice-to-have not validity; authored gives stronger neutrality+diversity control). BUT independence strengthening (consistent with author!=labeler): the ~17 COVERAGE briefs must be CODEX-AUTHORED (independent model, no Sidecoach stake), NOT architect-authored - to a fixed COMMITTED NEUTRAL prompt + 5-register x style-range coverage matrix + solution-agnostic requirement. Architect commits the neutral generation prompt (auditable, no Sidecoach steering); LEAD reviews the prompt + spot-checks output for neutrality+coverage. Why: briefs are the A5b INPUT; architect-framed briefs are the #1 "you rigged the inputs" attack surface - Codex-authoring removes it, matching the freeze-gate author!=labeler rigor. The 3 TOOL-AWARE calibration briefs (oracle-should-win/tie/Sidecoach-should-win) stay architect-authored + independent-reviewed (explicit judge-validation purpose). Keep 3 capped GOV.UK + clean real ones as the realism minority. Continue known-good captures 24->35 meanwhile.

## BRIEF-GEN PROMPT REVIEWED + APPROVED (f60be108); PAGE CORPUS COMPLETE (n=77)
Read _coverage-gen-prompt.md in full (the checkpoint before Codex authors). APPROVED - genuinely neutral: rule 1 solution-agnostic (problem-only template, success=achieves-not-how); rule 2 bars naming any tool/library AND favoring any idiom (explicitly: gradients/glass/brutalism/minimalism/a11y-maximalism) - balanced against BOTH a taste-tool's flashy idioms and an a11y-tool's strengths; framed as "two design assistants" (no names, Codex can't favor); coverage matrix (17, 5 registers + flex x balanced style range) deliberately includes RESTRAINT-favoring registers (minimal/corporate-trust/restrained/data-dense) where Sidecoach's idioms would NOT win - so it's balanced against us, not for us. Subtlety noted: each brief has an aestheticStyle field (fair brand-tone constraint given to both tools, balanced across matrix) - OUTPUT spot-check must confirm it's expressed as brand-TONE-in-words not technique, and successCriteria are outcome-based. Released: run Codex to author the 17, then I spot-check for neutrality+coverage+the aestheticStyle-as-tone check.
PAGE CORPUS COMPLETE: known-good 37/35 (MET), defect-bearing 40/35 (MET), excluded 0, n=77, all under the locked referee + fallback. So only the briefs remain before the FULL MANIFEST is ready for the JOINT INDEPENDENCE REVIEW (Jonah + lead) - that checkpoint is now imminent.

## MANIFEST-REPORT draft (6c7e061b) + REGISTER-IMBALANCE finding - lead direction before joint review
The architect built manifest-report.mjs (the joint-review deliverable) + flagged register-imbalance. Ran it myself. Findings:
- PAGES: 37 good / 40 defect / 0 excluded, floors met. BUT register-imbalance concentrated in KNOWN-GOOD: editorial 19/37 (51%), marketing 8, product 6, forms 2, dashboard 1, app-ui 1. So A2 precision would be measured almost entirely on EDITORIAL (clean-primary skews to text-heavy editorial; forms/dashboards have more defects). Defect-bearing is more balanced (ed 10/mk 10/pr 9/db 5/au 3/fm 3).
- THIN SPOTS: broken-image defect class only 5 (low-power for A1 recall on that class); era 71% 2026 (currency floor met, but archive buckets thin: 2012=7/2016=4/2018=4/2020=6/2022=1).
- BRIEFS NOT DONE: 6/20 (3 calibration + 3 GOV.UK), coverageCodexAuthored=0 - the architect built the report BEFORE running the Codex briefs, so the manifest is INCOMPLETE.
LEAD DIRECTION (before the manifest is review-ready): (1) RUN the Codex coverage briefs (as approved - it pivoted to the report instead). (2) BOUNDED re-balance of the thin known-good registers: the fallback now rescues div-based dashboards, so source more clean forms/dashboard/app-ui to a per-register MINIMUM (target >=3-4 known-good each) so A2 isn't editorial-only; if a register genuinely can't reach it (clean dashboards/forms rare), DOCUMENT the residual + report A2 precision PER-REGISTER (editorial well-powered, thin registers diagnostic-only/wide-CI) - don't force fake balance, don't leave A2 editorial-only. (3) broken-image thin (5) -> plant a few more OR mark diagnostic-only; note the era archive-bucket thinness for A5a. (4) Regenerate the manifest-report, then surface the COMPLETE manifest (pages+briefs+the documented residuals) for the joint review. The imbalance + residuals are themselves joint-review items (Jonah+lead) - tee them up explicitly in the report.

## FULL MANIFEST AT TARGET (ae3951ca) - brief spot-check PASS + integrity audit PASS -> joint review
BRIEF SPOT-CHECK (my gate, PASSED): Codex authored 17 coverage briefs to the approved prompt (genPromptSha=f60be108, codexAuthored:true, reproducible). I deep-read 5 spanning the riskiest styles (expressive-branded, premium-luxury, minimal-form, data-dense, playful) + mechanically grepped ALL 17 for technique/tool/idiom/hex leakage -> ZERO leaks. All: solution-agnostic (problem+content, no visual prescription), aestheticStyle as brand-TONE-in-words (not technique - luxury stayed "assured/refined" not "gold serifs"; data-dense stayed "organized/scannable" not "chart grid"), successCriteria outcome-based, neutral a11y baseline. The briefs are NOT the weak link; independence holds.
INTEGRITY AUDIT (my gate, PASSED, n=85): all subjectiveStatus=pending-independent (ALL HELD for my Codex pass); provenance complete on every record (source+captureUtc+license+selection-slot "why"); single locked labeler version (rendered-referee-fallback-v2-2026-06-23); diversity{quality,era} recorded on all; NO freeze artifact (nothing frozen). (Note: 85 records vs 77 pages in the report - minor count reconcile for the architect, not a blocker.)
MANIFEST: 77 pages (37 good/40 defect/0 excluded, floors 35/35 MET, locked referee) + 23 briefs (17 codex-coverage + 3 architect-calibration + 3 GOV.UK = 20 non-calibration, floor 20 MET). JOINT INDEPENDENCE REVIEW (Jonah+lead) now starting - my audit done, bringing the open items to Jonah. KEY JOINT DECISION: the register imbalance (known-good 51% editorial; forms/dashboard/app-ui thin; hard registers SPA/auth-gated) - accept+per-register-report vs source-more vs hybrid. Other open items teed up: fallback rationale, good-N=35 (power), ~52% primary-defect rate (web-a11y finding), known-good editorial skew.

## JOINT REVIEW - register-imbalance ruling: HYBRID (Jonah, 2026-06-23)
Joint independence review held: my audit (brief spot-check + integrity + result-vs-method) found independence HOLDS (deviations are sourcing-difficulty, NOT favoritism), the issue is REPRESENTATIVENESS (editorial skew). Brought the register-imbalance decision to Jonah via AskUserQuestion. JONAH CHOSE HYBRID (top-up then document) over accept-as-is and source-more-for-full-balance. Spec relayed: bounded top-up of clean forms/dashboard/app-ui toward a per-register KNOWN-GOOD floor of ~4 each (currently forms 2 / dashboard 1 / app-ui 1; defect side already OK at 3/5/3) using the fallback (div-based dashboards) + public templates/demos + real contact/signup forms, rule-agnostic + referee-labeled (only clean-primary count); BOUNDED (cap the extra effort - if clean-yield genuinely can't reach 4, STOP + document the residual); then report A2 precision PER-REGISTER (editorial well-powered, thin registers diagnostic-only/wide-CI) + document the residual in the manifest-report. After top-up -> regenerate report = freeze-ready -> my Codex SUBJECTIVE-labeling pass -> lead+Jonah spot-check -> FREEZE -> baseline scorecard (the first real where-we-stand-vs-oracle number). Everything HELD/unfrozen until then.

## JOINT REVIEW COMPLETE + manifest FREEZE-READY (b41b3cad); next = lead Codex subjective-labeling
The architect's top-up (already executed, crossed with my hybrid ruling) is independently verified BETTER than reported: known-good 47 (floor 35 MET), EVERY register >=4 (editorial 19/product 6/marketing 8/forms 5/dashboard 5/app-ui 4), editorial share 40% (down from 51%), a2NotEditorialOnly=TRUE. And the non-editorial clean pages come from DIVERSE sources (tabler/bootstrap/stripe-docs/vercel/digitalocean/basecamp/python/rust/OSS - NOT just gov/accessible), so the source-skew residual is mild + representative. Reconcile done (85 consistent: 77 was a stale pre-re-balance snapshot, +8 = the re-balance batch; 0 dupes/strays/orphans). Thin-spot diagnostics teed up in the report (a2 per-register, broken-image n=5 diagnostic-only, era 74%-2026 diagnostic). JOINT REVIEW verdict: brief spot-check PASS + integrity PASS + result-vs-method PASS (no favoritism) + Jonah HYBRID ruling executed = manifest FREEZE-READY.
NEXT PIPELINE STEP (mine, the lead-orchestrated independent labeling): the Codex SUBJECTIVE-labeling pass over the 22 held classes for the 85 pages. Division: architect builds the labeling HARNESS (infra: feed page HTML to Codex, record labeledBy=codex into candidates.json) + drafts a NEUTRAL descriptive labeling RUBRIC (committed) - but the RUBRIC is LEAD-OWNED (I finalize it; it must describe each idiom NEUTRALLY per common design understanding, NOT encode the architect's detection logic - it shapes the ground truth its rules are later measured against). Then I review/finalize the rubric + run the Codex labeling through the harness. Same committed-neutral-artifact + lead-review pattern as the brief-gen prompt. After labeling: lead+Jonah spot-check -> FREEZE -> baseline scorecard.
FINAL PRE-FREEZE AUDIT (n=90, d2be4d7b - after the dashboard 3->5 bounded top-up: capped 6, 5 captured, 2 clean + 3 correctly-defect, no cherry-pick): PASSED - 47 known-good / 43 defect (floors met), 90/90 subjective HELD, 90/90 provenance complete, single labeler version, every register >=4 known-good (editorial 40%), no residual. Corpus lead-verified FREEZE-READY. Awaiting architect's labeling harness + draft rubric for my review, then I run the Codex subjective-labeling pass.

## SUBJECTIVE-LABELING PIPE reviewed (34bc33c3): rubric FINALIZED + SCREENSHOT required (the key call)
The architect built the labeling PIPE (harness + draft rubric), labeled nothing (--dry-run only). My review:
- RUBRIC (subjective-rubric.md): genuinely descriptive - no px thresholds, no CSS property names, no selectors, no detection logic; each class = "what the idiom looks like" per common design understanding (cream-palette = off-white cream/sand bg; gradient-text = headline filled with a gradient; tight-leading = lines feel crowded). APPROVED as-is + FINALIZED (lead sign-off). The labels are the ground truth A5a measures the architect's rules against, so the rubric must stay independent of those rules - it is.
- HARNESS (subjective-label-harness.mjs): clean - records labeledBy=codex ONLY (guard physically blocks architect labels), stamps rubricSha+model+utc (reproducible), forbids "other detection logic", --page/--all/--dry-run. BUT it feeds Codex the HTML FILE to READ -> visual idioms judged from CSS text = the mentally-render unreliability we rebuilt the objective labeler to avoid.
- THE KEY CALL (lead): the labeling MUST use the RENDERED SCREENSHOT (the referee's existing Playwright capture) as the PRIMARY signal for the ~18 VISUAL classes (cream/gradient/glass/dark-glow/eyebrow/numbered/icon-tile/nested-cards/side-stripe/hero-metric/italic-serif + the typographic tiny/wide/all-caps/tight/extreme). TEXTUAL classes (marketing-buzzword, aphoristic-cadence) from the copy. MOTION classes (layout-transition, bounce-easing) from the markup motion character (a static image can't show motion; not detection logic). Added this as a LABELING SIGNAL section in the finalized rubric. The labeling model MUST be vision-capable + actually SEE the screenshot - VERIFY on known visual cases before the full run (known cream/gradient -> present from screenshot; plain -> absent). If codex exec can't ingest images, use a vision-capable independent path (still satisfies Jonah's independent-model ruling).
DIRECTED the architect: add the screenshot step to the pipe + vision-verify; then I run the labeling. Sequence after: lead+Jonah label spot-check -> FREEZE -> baseline scorecard.

## LABELING PIPE VERIFIED end-to-end (748e9098) - bidirectional, lead-run; robustness before the 90-run
The architect added the screenshot/vision step + verified (codex exec -i = vision-capable, same independent GPT family; satisfies Jonah's independent-model ruling + vision). Harness: 18 VISUAL->screenshot, 2 TEXT->copy, 2 MOTION->declarations; deterministic Playwright PNG via -i; guard records labeledBy=codex only; rubricSha (41d3a966) + per-class signal + method. I independently verified END-TO-END (real codex calls, not trust):
- Dry-run rendered gov.uk PNG; I READ it -> faithful full-page render (correct visual input).
- gov.uk (plain, I eyeballed the shot: white bg/solid text/sans-serif) -> labeled 0/22 present (ALL ABSENT). Correct.
- python.org -> tiny-text(0.82) present, rest absent; I READ the shot -> matches (dense small text; no gradient/cream/glass); AND it correctly called cream-palette ABSENT despite a yellow accent box (judged DOMINANT palette not an accent - the rubric nuance). Accurate + discriminating + nuanced.
- Bidirectional: plain->absent (gov.uk), idiom->present (python tiny-text + architect's synthetic cream/gradient). Vision genuinely judges from the screenshot.
Pipe is trustworthy. Before the full --all (90 codex vision calls, ~1.3M tokens), requested harness ROBUSTNESS: continue-on-error (one page's failure must not abort the whole run), --resume (skip pages already subjectiveStatus=labeled-codex so a re-run doesn't re-pay), end-of-run summary (labeled/failed/which). Then I run --all --resume. (2 pages already labeled: gov.uk, python.org.) After labeling: lead+Jonah spot-check -> FREEZE -> baseline scorecard.

## SUBJECTIVE LABELING PASS RUNNING (background, bmraa88hg)
Harness robustness added + verified (8ed5a858: continue-on-error, --resume skips labeled-codex, end-of-run summary). I kicked off `node eval/subjective-label-harness.mjs --all --resume` in the BACKGROUND (~88 remaining pages; gov.uk + python.org already done from my verification). Each page = a codex vision call (screenshot + copy + motion-decls), records labeledBy=codex + rubricSha(41d3a966) + signal. Continue-on-error means failures collect + the run finishes; re-run --all --resume mops up failures. Log: scratchpad/subjective-labeling.log + tasks/bmraa88hg.output. ON COMPLETION: (1) check the summary (labeled/failed); re-run --resume for any failures; (2) LEAD spot-check a sample of labels against their screenshots (esp present-calls); (3) bring Jonah his label spot-check (per his "you+I spot-check" ruling); (4) then FREEZE -> baseline scorecard. Everything still HELD/unfrozen until the freeze step.

## LEAD LABEL SPOT-CHECK PASSED (subjective ground truth is trustworthy)
Labeling run progressed to 79/90 (then completing). Aggregate present-call distribution (77 pages, 163 present calls, avg 2.1/page) is SANE: tiny-text 59 + layout-transition 34 common (most pages have small text / size-shifting motion); the distinctive trendy idioms appropriately RARE (gradient-text 1, dark-glow 1, hero-eyebrow-chip 2). LEAD spot-check (read screenshots, compared to labels) across the difficulty range:
- gov.uk (plain): 0/22 present (all absent) - correct.
- python.org (simple): tiny-text only; correctly called cream-palette ABSENT despite a yellow accent box (judged dominant palette). Correct + nuanced.
- mk_astro_live (richest, 9 idioms): read the shot - genuinely dark w/ purple/magenta GLOWS (dark-glow ✓), violet AI-ish palette (ai-color-palette ✓), feature icon-tile grid (icon-tile-stack ✓), nested cards ✓, eyebrow/kickers ✓, tiny-text ✓. All visual calls match.
- au_hn_live (cream, conf 0.97 "ivory cream"): read the shot - dominant bg IS pale ivory/cream. Correct. Confidence well-calibrated (0.97 HN clear cream vs 0.64 Basecamp subtler off-white).
VERDICT: the Codex vision labeler is accurate + discriminating + confidence-calibrated across plain/simple/rich/distinctive cases. Subjective ground truth TRUSTWORTHY.
RUN COMPLETE: SUMMARY labeled 88 | skipped(resume) 2 | failed 0; 90/90 labeled-codex, every page 22 classes, all labeledBy=codex (0 bad). + 5th spot-check: mk_vuejs_live gradient-text (conf 0.98 "hero word green-to-blue gradient") - read the shot, Vue's "Progressive" hero word IS a green-blue gradient. Correct. NEXT: presenting JONAH his spot-check sample (astro/HN/vuejs screenshots + labels, in-conversation) for his confirm/flag (his "you+I spot-check" ruling) -> on confirm, FREEZE -> baseline scorecard.

## JONAH APPROVED THE SPOT-CHECK -> FREEZE + BASELINE (the payoff)
Jonah confirmed the label spot-check ("Looks right, freeze it and run the baseline"). Both lead + Jonah spot-checks PASSED. Directed the architect (it owns the tooling; I verify):
1. FREEZE the REAL 90-page corpus - corpus-tool.freeze's comment says "tooling only, does NOT freeze a real corpus", so confirm/adapt it to lock candidates.json (canonical records + content-sha256/page + referee objective labels + codex subjective labels + provenance + bucket). author!=labeler gate must PASS (objective=referee, subjective=labeledBy=codex, architect=registered rule-author). Commit the fully-labeled candidates.json first, then freeze; `verify` must report OK; rollback git tag (corpus-frozen-v1). [Non-destructive - the HARD CHECKPOINT is before Stage 2 deletion, not this.]
2. BUILD + RUN the BASELINE SCORECARD (no scorecard harness exists yet): run BOTH detectors on the frozen corpus - current Sidecoach scanner (validateTaste+objective from migration-harness/dist) + oracle oracle (oracle-comparator) - scored vs ground truth: A1-A4 objective floors (recall/precision/FP vs the rendered referee, PER-REGISTER per the joint-review decision, paired bootstrap CIs per Contract-6) + A5a taste-detection (each detector vs the CODEX subjective labels). Output = where Sidecoach stands vs oracle NOW (pre-reimplementation baseline). DEFER A5b generative head-to-head (heavier: both tools generate from briefs + judges) as a separate follow-on; flag not-yet-run.
3. Codex item-8 on the scorecard harness; then I independently verify the freeze (locked + verify OK) + the scorecard numbers.

## FREEZE INDEPENDENTLY VERIFIED (a033a862, corpus-frozen-v1) + scorecard mapping = next bias vector
FALSE ALARM resolved: I first ran `corpus-tool.mjs verify` (the SYNTHETIC-fixture path, 0 cases) - wrong command. The REAL corpus uses `freeze-candidates`/`verify-candidates` (candidates-aware, lines 173-293). lock-candidates.json = {frozenAt, pageCount:90, briefCount:23, records, briefRecords} - my "5 cases" was miscounting the 5 top-level keys. Ran the CORRECT `verify-candidates` -> OK: 90 pages (47 good/43 defect) + 23 briefs; provenance complete; SUBJECTIVE author!=labeler (codex != architect rule-author = circularity gate holds); OBJECTIVE spec-math (no architect-label); bijection + content-sha256 canonical-record freeze intact (tamper-detection, same mechanism corpus-tool.test already proved). FREEZE is solid + tamper-proof + independently confirmed. Foundation LOCKED.
SCORECARD IN PROGRESS (scorecard-collect.mjs): it's a COLLECTION step (caches each tool's findings/page + builds rule vocabularies); 25/90 cached; NO metrics computed yet. NEXT-STEP BIAS FLAG: the RULE->GROUND-TRUTH-CLASS MAPPING (which Sidecoach/oracle rule-name counts as catching which referee/codex class) is a bias vector exactly like the rubric/briefs - if the architect (Sidecoach author) maps Sidecoach's rules generously + oracle's strictly, the head-to-head tilts. So the mapping must be SYMMETRIC + NEUTRAL: a committed mapping, lead-reviewed, same criteria both tools (or independently built). DIRECTED: finish collection (90/90) + surface the rule vocabularies + the proposed mapping for my review BEFORE computing A1-A4/A5a metrics. Then metrics + per-register + bootstrap CIs -> baseline scorecard. Codex item-8 on it; I verify.

## SCORECARD COLLECTION BLOCKER - oracle subprocess hang (handed to Codex per operating model)
The 25/90 stall: oracle detect.mjs spawns a Chrome grandchild that inherits stdout; collect's runOracle used execFile (resolves only on stdout EOF); the orphaned Chrome holds the pipe open so the call NEVER settles (even past the timeout, which killed the node child but not Chrome). Subprocess-lifecycle bug in OUR oracle-comparator, NOT oracle breaking on HTML. Architect (good, honest surfacing after my stall-check) tried: detached spawn (own process group) + group-kill `process.kill(-pid,SIGKILL)` on 45s timeout -> works STANDALONE (stall page returns 61ms) but the in-context collect STILL stalls at 25/90 (smells like a per-page Chrome LEAK accumulating over 25 pages -> fd/proc exhaustion). Continue-on-error + per-page oracleAvailable (excluded-with-flag from oracle's metrics, not false-clean) + --resume already in - good integrity handling. HANDED to codex:rescue (right call, multiple failure points). LEAD STEER fed to Codex: root = Chrome inheriting the parent stdout pipe (EOF never fires); robust fixes = (a) read detect.mjs output from a FILE + resolve on process 'exit'/'close' not stdout-EOF, and/or (b) spawn detached with stdio Chrome can't hold + resolve on exit + group-kill, and (c) actively reap orphaned Chrome between pages (the accumulation = a per-page leak). Per operating model: Codex fixes -> architect reviews -> I verify the 90/90 collection completes reliably (each page returns or bounded-killed+flagged, no Chrome leak). Then vocabularies + mapping for my review. NOTHING computed yet.

## CORRECTED ROOT CAUSE - it's SIDECOACH's own ReDoS, not the oracle (the eval found a real bug)
Architect re-investigated + corrected its oracle mis-diagnosis (owned the wrong call - good): the collect runs SIDECOACH's current scanner before the oracle; on mk_kubernetes_live (540KB HTML) validateTaste + the side-stripe/gradient/glass scanners return ~140ms, then `scanIdenticalCardGrids` (dist/absolute-ban-detector) HANGS >30s = CATASTROPHIC-REGEX-BACKTRACKING (ReDoS) in SIDECOACH'S OWN scanner on large pages. The oracle group-kill was a real robustness gain but a red herring for THIS stall (oracle returns ~55ms). THE EVAL IS DOING ITS JOB - it surfaced a genuine Sidecoach bug (ReDoS = A-axis robustness hazard, Stage-2 fix target).
FIX (architect, per continue-on-error sanction): subprocess-isolate BOTH detectors per-page with a hard timeout (reuse the group-kill runner) - a sync regex hang CAN'T be caught in-process, so subprocess isolation is the correct robust fix; a hang in EITHER tool fails-closed + page flagged detector-unavailable + excluded-with-flag from that tool's metrics. Then Codex item-8 + re-run to 90/90.
LEAD INTEGRITY POINT (load-bearing): exclude-with-flag must NOT silently advantage Sidecoach. A page Sidecoach can't process (ReDoS hang) being excluded from its PRECISION/RECALL is fair for those metrics - BUT the inability itself is a Sidecoach DEFICIT. The scorecard MUST separately report per-tool "detector-unavailable" page COUNT as a capability/robustness metric. If oracle handles mk_kubernetes_live and Sidecoach hangs, that is a Sidecoach robustness LOSS that must SHOW, not hide behind exclusion. Symmetric both tools. Otherwise the baseline hides Sidecoach's ReDoS weakness = dishonest. Directed this to the architect.

## BASELINE COLLECTION COMPLETE (90/90) - data revealing; oracle-unavailable=23 needs FAIRNESS scrutiny
Collection done (90/90, I verified the cache). Aggregate (pre-mapping, pre-scoring):
- SIDECOACH: 7 unavailable; 6270 findings but 95% from TWO rules (taste/hex-in-interactive-state 4419, taste/fabricated-svg 1527) = likely massive PRECISION NOISE; vocab 11, ALL taste rules - ZERO objective a11y rules (no low-contrast/skipped-heading/broken-image) -> ~0 objective-floor recall = MAJOR baseline gap (current Sidecoach scanner does no a11y; the reimplementation's owned WCAG floor addresses this).
- ORACLE: 23 unavailable (26%!); 378 findings; vocab 15 (side-tab, border-accent-on-rounded, layout-transition, overused-font, gray-on-color, numbered-section-markers, dark-glow, bounce-easing, marketing-buzzword, ai-color-palette, gradient-text, broken-image...). Some a11y (gray-on-color, broken-image).
- VOCAB OVERLAP is small (only gradient-text shared by name); the two tools detect largely DIFFERENT things -> the rule->ground-truth-class MAPPING is what makes them comparable (still the next gate, lead-reviewed).
LEAD FAIRNESS FLAG (symmetric to the Sidecoach-deficit point): oracle unavailable on 23/90 is HIGH - scrutinize whether it's GENUINE oracle failure/hang or an ARTIFACT of OUR oracle wrapper killing it prematurely (45s group-kill too aggressive? oracle slow on big pages?). If our wrapper unfairly drops 23 of oracle's pages, the comparison is biased AGAINST oracle - just as bad as biasing for Sidecoach. Fix: generous + SYMMETRIC timeout (same for both tools), distinguish genuine-error (deficit) from over-aggressive-kill (artifact -> raise timeout + re-run those pages). Directed the architect to investigate + give oracle a fair run before scoring. Then vocab + mapping for my review -> scoring.

## FAIRNESS RESOLVED - both tools 0 unavailable (fair run); ReDoS = TIMING deficit not availability
The generous-symmetric-timeout re-run: oracle unavailable 23->0 (ENTIRELY our wrapper artifact - my fairness diagnosis confirmed, it was our aggressive 45s kill on large pages, not oracle failing). Sidecoach unavailable 7->0 too - the scanIdenticalCardGrids catastrophic backtracking COMPLETES within the generous timeout (slow = minutes, but not infinite). So both tools now fairly process all 90 pages = the fairest availability picture, neither artificially advantaged.
BUT the ReDoS is real and must NOT vanish: with 0 unavailable it's now a PERFORMANCE/TIMING deficit (Sidecoach takes ~minutes on large pages vs oracle's seconds), not an availability one. DIRECTED: the scorecard must capture PER-PAGE TIMING per tool so the ReDoS shows as a Sidecoach speed deficit + stays a Stage-2 fix target - don't let it disappear just because it didn't hit the timeout. Then: surface the rule vocabularies + the proposed neutral mapping for my review (the last integrity gate). The eval-integrity arc held BOTH directions: Sidecoach's noise/no-a11y/ReDoS surface as real deficits, AND oracle got a fair run (no artifact penalty).

## MAPPING REVIEW - algorithm neutral, BUT subjective-class-name lineage favors Sidecoach -> Codex semantic pass
scorecard-mapping.json: algorithm = exact normalized match (lowercase, strip prefix-before-/, strip non-[a-z0-9-]); maps iff normalized==class name; SAME for both tools; no fuzzy/synonyms; unmapped reported; borderline surfaced for lead (not auto-mapped). APPROVED as a neutral foundation. Results: Sidecoach maps 4 (glassmorphism-default/gradient-text/hero-metric-template/side-stripe-borders), oracle maps 9 (ai-color-palette/bounce-easing/broken-image/dark-glow/gradient-text/gray-on-color/layout-transition/marketing-buzzword/numbered-section-markers) - so oracle maps MORE, the algorithm doesn't favor Sidecoach in count.
STRUCTURAL BIAS I caught: the SUBJECTIVE ground-truth class NAMES are Sidecoach-taste-vocabulary-derived (shared lineage), so exact-NAME-match gives Sidecoach free credit when oracle's SAME-idiom rule has a different name. Live case: side-stripe-borders (Sidecoach's rule = the class name -> maps; oracle's border-accent-on-rounded -> unmapped -> oracle under-credited for arguably the same idiom). Objective classes are spec-named (broken-image/gray-on-color/low-contrast) = neutral, and Sidecoach has ZERO objective rules anyway, so this bias is A5a-only + ~1 class.
FIX directed: CODEX (independent) does a SYMMETRIC semantic pass over BOTH tools' UNMAPPED rules - "does this rule's purpose detect any ground-truth class idiom?" - same prompt both tools; confirmed matches get mapped, keeps me+architect out of the bias-prone judgment. Borderline side-tab->side-stripe-borders: I lean NO (side nav tab != accent stripe; different idioms) - Codex confirms. (Pre-checked: border-accent-on-rounded is the only plausible oracle near-match; Sidecoach's unmapped are genuine non-matches incl the noise rules + translatey-in-hover which is transform-in-place NOT layout-transition.) Document the name-lineage + the Codex mitigation in the scorecard. Then score.

## SEMANTIC PASS RUNNING (symmetric, neutral) + collector double-Codex-hardened
Codex semantic pass running (task bktikjsvk): 13 sequential queries (7 sidecoach + 6 oracle unmapped), SAME tool-agnostic prompt each, independent. Proof of neutrality: the FIRST verdict (identical-card-grids -> icon-tile-stack, high) gives SIDECOACH more credit - the pass corrects in BOTH directions, not just oracle's. (LEAD REVIEW TODO when it lands: scrutinize the Sidecoach-FAVORABLE verdicts as hard as the oracle ones - e.g. is identical-card-grids really icon-tile-stack, or is that a Sidecoach over-credit? icon-tile-stack specifically needs ICON tiles in rounded squares; identical-card-grids is broader. Don't let the pass over-map our rules.)
Collector hardened FIRST: architect ran TWO Codex item-8 passes on the collection runner before trusting numbers - pass1 found 7 issues (incl 3 fairness/fail-closed bugs), pass2 confirmed + found 4 more (config-error-as-data, stderr asymmetry); all folded; authoritative --force re-collect running in parallel for clean timing/unavailable numbers. The semantic pass doesn't depend on it (unmapped lists identical). Excellent produce-and-verify rigor. Holding on the 13 calls (~8-10min); then merge verdicts into mapping + resolve side-tab + commit + surface for my review. No scoring until I approve the merged mapping.

## SEMANTIC-PASS VERDICTS REVIEWED + APPROVED (mapping neutral; my own misread caught by Codex)
3 matches + 10 nulls. I verified each against the ACTUAL rule definitions (rule-descriptions.json + source), not just Codex's "high/medium":
- side-tab -> side-stripe-borders (HIGH): ACCEPT. I was WRONG - I'd leaned NO guessing "side-tab"=nav-tab, but its real def is "thick colored border on ONE SIDE of a card" = exactly the side-stripe idiom. Codex (with the description) was right; my lean was a name-misread. This VALIDATES using Codex for the judgment (kept my misread from biasing the result).
- border-accent-on-rounded -> side-stripe-borders (MEDIUM): ACCEPT. The bias case I predicted; defs overlap ("thick accent border on a rounded card" ~ side accent); corrects oracle's under-credit. (Redundant with side-tab for class-credit but matters per-page.)
- identical-card-grids -> icon-tile-stack (HIGH): ACCEPT. Verified Sidecoach's rule IS icon-led ("grid of >=3 cards with icon/svg + heading + paragraph triplet") = matches icon-tile-stack's icon-led grid. Broader-vs-specific concern is handled by precision (over-fire where icon-tile-stack absent = precision penalty, not free credit).
- 10 NULLs (sidecoach noise rules + oracle em-dash/flat-hierarchy/monotonous-spacing/overused-font): correct, no ground-truth equivalent.
NEUTRALITY CONFIRMED: the independent pass net-corrected TOWARD oracle (2 matches vs Sidecoach's 1), and my one disagreement (side-tab) resolved in oracle's favor - so zero Sidecoach-favorable tilt; if anything it removed the name-lineage advantage I flagged. NET effective mapping: Sidecoach 5 classes (4 exact + icon-tile-stack), oracle 10 (9 exact + side-stripe-borders). MAPPING APPROVED. Architect folds verdicts in (side-tab=MATCH), commits, finishes the clean re-collect + 3rd Codex item-8 on the collector, THEN computes metrics. I do final verification of the scorecard.

## BASELINE SCORECARD (preliminary, pre-final-verification) - Sidecoach FAR behind; + the static/browser-mode call
First real number (90 pages, 47 good/43 defect; pending the final scoring Codex item-8 + the mode ruling + my verification):
- RECALL: sidecoach overall R=0.025 (objective 0.000 = zero a11y, subjective 0.037) vs oracle overall R=0.208 (objective 0.063, subjective 0.282). oracle far ahead.
- A5a TASTE: oracle R=0.282/P=0.312 vs sidecoach R=0.037/P=0.200.
- NOISE: sidecoach 6757 raw findings, 99% UNMAPPED (the hex-in-interactive + fabricated-svg noise); oracle 1131 raw, 24% unmapped.
- TIMING: sidecoach median 1157ms / p90 41.8s / max 120s / 30 pages >5s; oracle median 58ms / p90 79ms. Plus sidecoach 4 unavailable (ReDoS >120s), oracle 0.
- PAIRED BOOTSTRAP (sc-imp 95% CI): subjective recall -0.245 [-0.306,-0.184] SIG; overall recall -0.183 [-0.22,-0.148] SIG; precision NS. So Sidecoach is SIGNIFICANTLY behind on recall (+ timing + objective coverage). The honest pre-reimplementation gap - exactly what the eval exists to quantify. The climb is real.
THE MODE JUDGMENT CALL (taken to Jonah): the oracle runs oracle via detect.mjs --json = STATIC engines only -> oracle emits 0 low-contrast + 0 skipped-heading (those need its BROWSER/visual-contrast engine). The rendered referee labels them from real DOM (41 low-contrast, 32 skipped-heading present). So oracle's objective recall 0.063 is a STATIC-MODE artifact, NOT oracle being weak; broken-image (static-detectable) scores R=1.0/P=1.0 (scoring mechanics correct). Both run static = fair static-vs-static, but it UNDERSTATES oracle's true a11y ceiling - and the reimplemented Sidecoach WILL be browser-based, so the REAL floor bar is oracle's BROWSER ceiling, not its static subset. Options: A=static-vs-static+caveat (lean, defers browser number); B=browser-oracle-vs-static-sidecoach (asymmetric, reject); C=static head-to-head + oracle browser-mode objective ceiling as a separate reference bar (honest real bar now, some rework). Lead rec = C (get the true bar on record; consistent with Jonah's harder/more-honest pattern). Asked Jonah. STATUS: scoring code in Codex item-8 now; nothing committed/final until that + the mode ruling + my verification.
JONAH RULED C (2026-06-24): keep the fair static-vs-static head-to-head as the headline AND record oracle's full BROWSER-mode objective ceiling as a separate reference bar = the real objective floor the reimplemented (browser-based) Sidecoach must clear. Directed the architect: run oracle in full browser/live mode over the corpus for the OBJECTIVE classes only (low-contrast/skipped-heading/etc. that static --json misses), recall vs the rendered referee's objective labels, generous timeout (browser mode is slower), reported SEPARATELY + clearly labeled (NOT in the paired static comparison). Then finish scoring Codex item-8 + commit the whole unit + surface for my final independent verification before it goes to Jonah as the committed baseline.

## BASELINE SCORECARD VERIFIED (bcf601d) - lead independent recompute MATCHES; C ceiling to add, then final
Architect committed the final scorecard (bcf601d, Codex item-8 SHIP after 6 passes: collector x4 + scoring x1 + consolidated). My INDEPENDENT verification: recomputed the headline SUBJECTIVE RECALL from scratch (frozen candidates.json labels + raw .scorecard-cache findings + the effectiveMapping incl my 3 approved semantic verdicts) -> sidecoach tp=7/fn=181 = 0.037, oracle tp=53/fn=135 = 0.282 - EXACT match to scorecard.json. So the scoring math (TP/FN/recall), the mapping application, AND my approved verdicts (sidecoach icon-tile-stack, oracle side-stripe-borders) are all correct. broken-image R=1.0/P=1.0 anchor + 6 Codex passes corroborate. SCORECARD VERIFIED + TRUSTWORTHY.
FINAL NUMBERS: overall recall sc 0.025 vs imp 0.208; subjective recall sc 0.037 vs imp 0.282 (paired bootstrap diff -0.245 [-0.306,-0.184] SIG); per-register oracle leads recall in ALL 6 registers; timing sc median 1157ms/p90 41.8s/4 ReDoS-unavailable vs imp 58ms/0; noise sc 6757 raw 99%-unmapped vs imp 1131 24%. Honest verdict: Sidecoach significantly behind on recall + timing + objective coverage. The pre-reimplementation gap, quantified + verified.
LAST GATE: Jonah ruled C (add oracle browser-mode objective ceiling as a separate reference bar) - architect committed under A w/ caveat, will add the one-sided C ceiling (no head-to-head re-commit). Once C is in, the scorecard is FINAL -> goes to Jonah as the committed, lead-verified, honest baseline. STAGE 0 essentially COMPLETE.

## C CEILING COMPUTED - oracle browser-mode objective recall 0.621 (the REAL floor bar; Jonah's C pays off)
scorecard-browser-ceiling.json (90/90, 0 unavailable, uncommitted pending its Codex review): microObjectiveRecall 0.621 (59 TP / 95 present) - ~10x the static-mode 0.063. So oracle's STATIC --json mode understated its a11y capability by an order of magnitude (browser/Puppeteer engine detects contrast + headings; static doesn't). The REAL objective floor bar the reimplemented browser-based Sidecoach must clear is ~0.62 recall, NOT the handicapped 0.063. Jonah's C ruling pays off - the honest hard bar is on record. My spot-check: 0.621 >> 0.063 as expected; confirms the static-mode-artifact diagnosis.
ONE per-class quirk flagged: broken-image recall=0 in browser-mode vs 1.0 static. Cause = serving artifact (the captured pages' images resolve when served over localhost, so no broken image in the rendered DOM) - NOT an oracle capability gap (static proves it detects broken-image). So the 0.621 ceiling slightly UNDERSTATES oracle = a conservative lower-bound bar (fine for a floor target; real bar >=0.621). Directed the architect to explain this in the ceiling comparisonNote.
The static head-to-head (scorecard.json @ bcf601d3) is UNCHANGED = my lead-verified version. Last step: architect finishes the ceiling Codex review + commits scorecard-browser-ceiling.json + surfaces -> then the committed, lead-verified baseline (static head-to-head + the 0.621 true-ceiling reference) goes to Jonah and STAGE 0 CLOSES.

## SELF-ANALYSIS - over-checking false-alarms in the final phase (ease off)
In the scoring/ceiling endgame I fired several premature stall-checks/alarms from inferring state via git/file/proc inspection: (1) "freeze only 5 cases" (ran the SYNTHETIC verify, not verify-candidates), (2) "55 Chrome = leak" (was the user's browser), (3) "no progress 30min on C" (run writes to a gitignored cache my git-check couldn't see), (4) "architect stuck, not committing" (it was correctly waiting on a genuinely-running ceiling Codex review). WHY: I treated absence-of-git-evidence as absence-of-progress, without accounting for the architect's tooling (gitignored caches, /tmp logs, its stated "commit-after-review" gate). HOW IT WENT WRONG: in earlier phases worktree-checks caught REAL issues (0-captures, 2-missing-classes, ReDoS, oracle-23-unavailable) so I leaned on them - but in the mechanical endgame, with the architect executing correctly on stated gates, the same checks produce false alarms + unwarranted pressure. LESSON: when a teammate has named its process + gate, trust the stated process; verify via the teammate's own surfaced artifacts/logs, not git-state inference; reserve hard stall-checks for genuine silence WITHOUT a stated in-flight gate. Ceiling review confirmed running (codex exec -> /tmp/codex-ceiling-confirm.out, verdict finishing); architect correctly waiting, NOT stuck. Backing off; holding for its committed surface.

## STAGE 0 COMPLETE - baseline committed + FULLY lead-verified (handed to Jonah)
CEILING COMMITTED (84b7829c, Codex item-8: fix-first 6 -> 2nd pass SHIP "no overstatement, 0.833 verified"). Refinements since my 0.621 spot-check (both MORE correct): matched the referee's exact page transform (scripts+external stripped = apples-to-apples) + stderr fail-closed (caught a silently-clean mailchimp nav-timeout). I independently RE-VERIFIED the ceiling construction:
- STRICT 0.632 = per-class sum 60/95 (skipped-heading 32/32, low-contrast 28/41, gray-on-color 0 [browser folds into contrast], broken-image 0 [browser engine doesn't run it]). ✓
- HONEST 0.833 = 65/78: present 78 = 95 - 17 gray-on-color (folded into the contrast family, not a separate miss), tp 65 = 60 + 5 broken-image (oracle's STATIC engine detects it, R=1.0). 65/78=0.833. ✓ Principled + conservative; = oracle's true full-objective capability.
FULL STAGE 0 BASELINE (committed, lead-verified, Codex-verified end-to-end):
- STATIC HEAD-TO-HEAD (bcf601d): Sidecoach FAR behind - overall recall 0.025 vs 0.208; subjective recall paired-bootstrap diff -0.245 [-0.306,-0.184] SIG; oracle leads recall in ALL 6 registers; timing sc median 1157ms/p90 41.8s/4 ReDoS-fails vs imp 58ms/0; noise 6757 raw 99%-unmapped vs 1131 24%; Sidecoach 0 objective a11y rules. (I recomputed the headline subjective recall from scratch = EXACT match.)
- TRUE OBJECTIVE CEILING (84b7829c): oracle browser-mode 0.632 strict / 0.833 honest = the REAL floor bar (~13x the static 0.063 that Jonah's C ruling revealed). Sidecoach currently scores 0 on it.
INTEGRITY ARC (held throughout, both directions): externally-sourced frozen 90-page corpus; ground truth by independent referees (spec-math objective, Codex-vision subjective, never the architect); neutral rule-mapping where the independent Codex pass corrected the name-lineage tilt AND overruled the LEAD's own misread (side-tab); fairness guards that removed oracle's 23-unavailable artifact + counted Sidecoach's ReDoS as a real deficit; 8 total Codex passes + my independent recompute. STAGE 0 (the ungameable measurement foundation) is DONE. Next: the reimplementation climb (Stage 1+) against proven targets. Handed to Jonah.

## STAGE 1 KICKED OFF (Jonah's call, 2026-06-24) - reimplement-and-own climb begins
Jonah: "Kick off Stage 1 - start the reimplement-and-own climb." Stage 1 = first BUILD stage (NON-destructive - build the owned new alongside the old; HARD CHECKPOINT still stands before Stage 2's first deletion).
HIGHEST-VALUE FIRST TARGET (grounded in the verified baseline gaps): the OWNED OBJECTIVE FLOOR - Sidecoach currently scores 0.000 objective recall vs the ~0.83 true ceiling; this is the biggest measured gap, has the clearest spec-grounding, and a rendered/owned scanner fixes the ReDoS (no catastrophic regex) + the 99%-noise at the same time. Built from PUBLIC specs (W3C WCAG / CSS Color 4), copy NOTHING from oracle.
KEY INTEGRITY FLAG for the Stage 1 plan: the PRODUCT scanner (what ships, what gets measured) must stay INDEPENDENT of the eval RENDERED REFEREE (the ground truth) - if the product == the referee code, the eval is circular (trivial 1.0). Both can draw on the same public specs, but they must be distinct artifacts so the scorecard stays honest. Architect must address this in the plan.
DISCIPLINE per stage: verifiable-plan-first (protocol item 7: <step> -> verify lines); PROOF = re-run the FROZEN scorecard (new scanner vs oracle on the frozen corpus) showing the objective-recall gain - prove each gain on the same bar; Codex item-8 + lead independent gate per stage; net-SIMPLER not just more-capable (the mission's spine). Directed the architect (re-engaged from standby) to surface the Stage 1 verifiable plan (target + first move + the product-vs-referee independence + the verify/proof) for my + Jonah's alignment BEFORE building.

## STAGE 1 PLAN APPROVED (with strengthenings) - owned rendered objective scanner
Architect's Stage 1 verifiable plan: build Sidecoach's OWNED rendered objective scanner for 5 spec classes (broken-image, skipped-heading WCAG 1.3.1, low-contrast + gray-on-color WCAG 1.4.3 + CSS Color 4, justified-text 1.4.8); recall 0 -> toward 0.833; from public specs, copy nothing from oracle. ENGINE = reuse existing src/validators/browser-evidence-collector.ts (shipped Playwright, getComputedStyle, contrast already computed) - spec-rule logic on top, no new dep = net-simpler-ish. PRODUCT-vs-REFEREE INDEPENDENCE answered strongly: distinct artifacts (product=new src/ module, referee=eval/objective-label-rendered.mjs), both cite the public spec, neither imports the other, + a COMMITTED referee-independence.test.ts asserting the product import graph has ZERO eval/ paths (mechanical enforcement). Steps S0-S7 each with verify clauses; S6 proof = re-run frozen scorecard showing objective-recall gain (static head-to-head/corpus/labels/mapping UNCHANGED); S7 Codex item-8 + lead gate; NON-destructive (the noise/ReDoS deletion is the Stage-2 HARD CHECKPOINT, not Stage 1).
LEAD APPROVED + strengthenings: (1) product CALIBRATION must include the REFEREE'S ADVERSARIAL CASES (cascade/ARIA/inert/multi-layer-alpha - the spec-grounded hard INPUTS, not the referee code) so "agreement=correctness" is robust against shared-author blind spots - the product must independently nail the hard cases, not just easy ones; Codex item-8 on spec-correctness is a second guard. (2) OBJECTIVE FLOOR = PARITY not beat (clear ~0.83 = match oracle's spec-correct ceiling, per Jonah's success bar; differentiation is on taste/A5a + simplicity, not the floor). (3) Stage-1-adds + Stage-2-deletes is a COMMITTED PAIR for the net-simpler spine - hold to the Stage-2 deletion (Stage 1 alone adds, isn't yet simpler). (4) S5 must wire the eval's sidecoach-scan.mjs to the SAME shipping product scanner (not an eval-only copy) so the scorecard measures what ships. GREEN to start S0 (independence scaffold + calibration harness + the guard test). Jonah FYI'd (build plan, no fork needing his ruling).

## STAGE 1 S0 VERIFIED (52a0e471) - integrity boundary established before rule logic
S0 (scaffold + guards, no rule logic yet): owned scanner src/validators/objective-rendered-scanner.ts (Playwright hermetic render, no new dep, public specs, returns [] until S1-S4). REFEREE-INDEPENDENCE GUARD: walks the scanner's transitive import graph, fails on any eval/ reach; architect PROVED-IT-BITES via negative control (added an eval/ import -> guard exit 1 naming the violation -> reverted). Adversarial CALIBRATION fixtures (my strengthening #1) authored from spec: cascade/specificity, ARIA aria-level, inert/hidden, multi-layer alpha, sr-only-counts, presentation-role-excluded (7 green / 10 pending as classes land). MY INDEPENDENT CHECK: ran `tsc --noEmit` clean + the referee-independence test myself -> "OK, zero eval/ imports". Boundary verified + mechanically enforced before any rule code. GREEN to S1-S4 (the per-class spec rules, each turning its calibration fixtures incl adversarial green), then S5 wiring (eval sidecoach-scan -> the SAME shipping scanner) + S6 the frozen-scorecard proof (objective recall off 0) + S7 Codex/lead gate.

## STAGE 1 S6 PROOF (0->0.242) + CRITICAL HELD-OUT STEER (don't overfit the frozen corpus)
S6: sidecoach objective recall 0.000 -> 0.242 (precision 0.561); overall 0.025 -> 0.106. oracle + corpus + labels + mapping UNCHANGED. Per-class: broken-image 1/5 R0.2 but FP=17, skipped-heading 6/32, low-contrast 13/41, gray-on-color 3/17. Calibration 17/17 green (incl adversarial) YET real recall 0.242 = the architect's profound honest finding: hand-authored fixtures aren't hard enough; the REAL corpus is the true test (validates the frozen-corpus approach). Diagnosed: (1) broken-image BUG - flags naturalWidth==0, but hermetic render aborts external imgs so ALL external read broken (17 FP) + real src-cases missed; fix = src-attribute basis (spec-correct for hermetic render). (2) contrast/heading under-detect (likely bg-image-indeterminate skip too aggressive + heading scoping). (3) 4 ReDoS pages still FN (Stage-2 recovers).
NOTE on framing: "beats static-oracle 0.063" is NOT the win - 0.063 is the handicapped static artifact; the REAL bar is the 0.833 browser-mode ceiling, and we're at 0.242 = still far below parity. Architect correctly says "not parity yet."
CRITICAL LEAD STEER (the held-out trap): architect proposed "diagnose page-by-page against the referee labels on the frozen corpus -> tighten -> re-prove -> repeat until 0.83." That is TRAINING ON THE TEST SET - overfitting the product to the held-out bar = corrupts the ungameable eval (Contract-6: tuning-after-heldout disqualifies). RULING: ITERATE to parity YES (don't bank 0.242 - it's a floor, not a landing), but with HELD-OUT DISCIPLINE: (a) fixes must be GENERAL spec-correctness (justifiable as "correctly applies WCAG/CSS-spec X" WITHOUT reference to specific eval pages) - the broken-image src-basis + bg-image-skip qualify; (b) develop against a dev signal DISJOINT from the frozen 90 - harden the calibration with NEW spec-case fixtures + ideally a small SEPARATE dev corpus (rule-agnostic, referee-labeled, disjoint from the eval); (c) the frozen eval is a LIMITED, pre-registered-cadence MEASUREMENT, not a repeated tuning loop - the eval result may PROMPT investigation but must not be the per-tweak signal; (d) Codex item-8 + lead gate verify each fix is spec-grounded not page-fit. This keeps the eventual ~0.83 HONEST (spec-correct + measured on held-out, not overfit). Directed to the architect.

## HELD-OUT DISCIPLINE re-confirmed + a DISCLOSED PEEK ruled on (exclude the page)
Architect re-confirmed the 4-point held-out discipline AND proactively disclosed a slip: it peeked at ed_csstricks (a held-out page) to diagnose the bg-image contrast bug. The disclosure is exactly right - honesty about a slip is what makes it manageable; a hidden peek would be the corruption. The 2 fixes are GENERAL spec-correctness (approved, they proceed): broken-image = src-attribute absent/empty (correct basis under hermetic render where external aborts; the 17-FP bug); background-resolution = an opaque bg-color occludes everything behind it (CSS Color 4/paint order), so contrast is indeterminate ONLY when the nearest backdrop is non-solid with no opaque base - the old code wrongly returned indeterminate on ANY ancestor bg-image. Both justifiable with zero eval reference.
LEAD RULING on the peek: ed_csstricks is now CONTAMINATED (developed-against) -> EXCLUDE it from the held-out OBJECTIVE measurement (report objective recall on the 89 un-peeked pages; recompute the baseline objective on 89 too for comparability - headline UNCHANGED since sidecoach objective was 0.000 regardless of N). Document the exclusion + reason in the scorecard. PRECEDENT set: a peeked held-out page leaves the held-out - peeking has a real, concrete cost. Going forward: ZERO peeks; the dev corpus is the development signal. (Not punitive - principled; the honest disclosure is commended.)
DEV-SET APPROACH approved (the architect's): Signal A = hardened calibration with NEW spec-case fixtures (opaque-container-in-bg-image-body -> low-contrast; text-directly-on-bg-image-no-base -> indeterminate; broken-image src cases) authored from spec; Signal B = a SMALL SEPARATE DEV CORPUS ~15-20 real pages DISJOINT from the eval 90, same-referee-labeled, built BEFORE chasing the remaining classes (so no temptation to peek). Frozen eval = milestone measurement per fix-batch + Codex item-8 + lead gate. Proceeding: broken-image -> bg-resolution -> build dev corpus -> heading/remaining. Surface the milestone measurement + the dev-corpus build for my gate.

## SELF-ANALYSIS (recurring) - premature stall-alarms in Stage 1; check the operation's OWN log FIRST
Fired a stall-check at the architect ("dev-corpus build hung, 5 procs, 0 output") - FALSE. The 5 "eval procs" were mostly unrelated (Adobe Creative Cloud helpers + an 11-day-old Playwright-MCP server, caught by my chromium/playwright grep). The real proc was scorecard-collect --force = the M1 89-page measurement, which was at 89/90 (nearly done), slow ONLY because the OLD scanIdenticalCardGrids ReDoS times out on the large dashboards (db_worldbank/db_datausa marked [SC unavail], correctly - the Stage-2 deletion target). It was progressing fine; I messaged the alarm BEFORE checking the operation's own log (/tmp/m1-collect.out showed 89/90). This is the 3rd-4th premature stall-alarm this stretch (gitignored-cache, "stuck"-while-Codex-reviewing, now this). WHY: I treat rapid bare idles + an incomplete proc/git read as "stuck," during KNOWN long background operations (slow collect, dev-corpus sourcing) where bare idles are just heartbeats. FIX (hard rule for the build phase): when there's a STATED in-flight operation, check THAT operation's own progress artifact (its log/output/cache count) FIRST; conclude "stuck" only if the operation itself shows no advance; never fire an alarming stall-check off a proc-grep alone. Reserve stall-checks for genuine silence with NO stated in-flight op. (The Stage-0 worktree-checks caught real issues; the Stage-1 ones have mostly been noise because the architect is executing long ops correctly.)
M1 STATE (correct): the 89-page measurement is at 89/90 finishing; the old-ReDoS slowness on large dashboards persists until the Stage-2 deletion. Let it complete + surface M1 + the dev-corpus build.

## STAGE 1 M1 RESULT (0->0.894) - HUGE gain BUT exceeds the ceiling = scrutinize HARDER (neutrality both ways)
batch-1 fixes -> sidecoach objective recall 0.000 -> 0.894 (precision 0.977) on the 89 un-peeked pages. broken-image FP CLEARED 17->0 (src-attribute fix; now 5/5 R1.0 P1.0); low-contrast 38/40, gray-on-color 11/17, skipped-heading 30/32; misses are all ReDoS-unavailable pages (Stage-2 recovers). Architect independently recomputed the headline from raw cache. STRONG, fast progress.
RED FLAG the architect HONESTLY self-flagged (good): 0.894 EXCEEDS oracle's browser ceiling 0.833, near-perfect on available pages (heading + low-contrast 1.0, precision 0.977). The objective floor target was PARITY (~0.83), NOT exceed. A scanner matching the referee near-perfectly is the CIRCULARITY RISK I flagged (strengthening #1). Two candidate causes (architect named both): (a) broken-image - oracle's BROWSER ceiling has broken-image=0 (static-engine rule) but sidecoach detects it 5/5, so part of the "exceeds" is a CONSTRUCTION mismatch (the ceiling under-counted broken-image), not real over-perf; (b) RENDER-COUPLING to the referee's transform - if the product renders with the SAME hermetic transform as the referee + applies the same WCAG logic, it computes the same function -> agrees ~perfectly regardless of true spec-correctness = a subtler circularity than code-sharing (the import guard doesn't catch it).
MY SCRUTINY (to do at the full surface, neutrality demands I check our favorable result HARDER than oracle's): (1) independently RECOMPUTE 0.894 from raw cache; (2) DECOMPOSE per-class + report apples-to-apples with the ceiling's construction (is the >0.833 ONLY broken-image, or the shared contrast/heading classes too?); (3) RENDER-COUPLING test - confirm the hermetic transform is the product's GENUINE shipping behavior (not an eval-matching artifact) AND the product's spec-correctness is proven INDEPENDENTLY (adversarial fixtures authored from spec where the answer is known w/o the referee + Codex item-8), so the agreement is legitimate parity not circular convergence; (4) review the Codex item-8 10 findings (may include render-coupling). DO NOT report 0.894 as a "win" until explained - the >ceiling is unverified. Architect finishing the dev corpus (20 pages, labeling) then full surface ~2min.

## M1 GATE - 0.894 VERIFIED but "beats oracle" is an OVERCLAIM (rendered-vs-static = unfair to oracle)
0.894 RECOMPUTE: I independently recomputed from raw cache -> sidecoach objective tp=85/present=95=0.895, matching the architect's 0.894. broken-image FP cleared (17->0, 5/5), low-contrast 38/40, gray-on-color 11/17, skipped-heading 30/32. Legitimate spec-correct detection ON THE FROZEN HERMETIC CORPUS (the product is spec-correct on the adversarial fixtures + Codex). VERIFIED.
CRITICAL NEUTRALITY CATCH: the committed head-to-head shows sidecoach objective 0.894 vs ORACLE objective 0.064 - oracle is in STATIC mode (0.064) while sidecoach's NEW scanner is RENDERED. That's apples-to-oranges FAVORING US = the EXACT INVERSE of the Stage-0 fairness issue (where our timeout handicapped oracle). The architect's "sidecoach SIGNIFICANTLY beats oracle (+0.119 overall recall)" rides on comparing our rendered scanner to oracle's handicapped static mode. NOT honest. The FAIR objective comparison is sidecoach-rendered 0.894 vs oracle-BROWSER ceiling 0.833 = ~PARITY (the floor goal per Jonah's success bar, NOT a beat). The >0.833 residual is mostly the broken-image construction artifact (oracle browser engine emits 0 there). REAL-WORLD caveat: 0.894 measures hermetic-snapshot detection; real-world (scripts-on/external-CSS) is the separate S5b - frame the number as "objective recall on the frozen hermetic corpus," not real-world a11y.
RULINGS: (1) RE-RUN the objective head-to-head with oracle in BROWSER mode (both rendered, apples-to-apples) -> report the objective axis as PARITY (~0.89 vs ~0.83), DROP "beats oracle" on objective (it's against handicapped static-oracle). Jonah's C static-head-to-head is now OBSOLETE for the objective axis (sidecoach is no longer static). (2) FOLD batch-2 (the 10 Codex general-spec findings - ARIA role/aria-level, Color-4 canvas parsing, srcset/picture, opacity-indeterminate, etc.) - yes, they harden independent spec-correctness (strengthens "agreement=correctness not coupling"). (3) DEV CORPUS: agree with architect - it came back all-clean (no defect signal; modern docs + curl-unstyled), so for the small remaining objective gap rely on spec-fixtures+Codex (general fixes, not page-tuned); build a real DEFECT-BEARING dev corpus when starting the SUBJECTIVE frontier (oracle still leads taste 0.277 vs 0.033). (4) S6 heading 6/32->30/32 anomaly unreproducible (--force overwrote cache) - minor, current 30/32 raw-verified. HEADLINE (honest): objective floor CLEARED TO PARITY (verified), not a beat; subjective/taste is the real differentiation frontier next.

## M1 - architect WITHDREW the "beats" claim (exemplary); honest position endorsed + rulings
The architect tore at its own 0.894, decomposed apples-to-apples, withdrew "beats the ceiling." I independently verified the decomposition: oracle-browser ceiling per-class = skipped-heading 32/32=1.0 (we LOSE at 0.938), low-contrast 28/41=0.683 (our edge), broken-image tie (static). So the >0.833 is ENTIRELY contrast-family - and the architect's diagnosis is honest: that edge is RENDER-STATE-CONFOUNDED (sidecoach renders each eval page the EXACT way the referee labels it - setContent+stripScripts+abort-external; oracle-browser uses its own render + diverged on 13 pages). Sidecoach agrees with the referee MORE because it SHARES the referee's render, not because its contrast LOGIC beats oracle's. The import-guard doesn't catch this; the lead-gate did. Genuine shipping render = S5b (file://+same-origin, not built/measured); 0.894 is on the eval-render-basis, NOT real-world.
ENDORSED HONEST POSITION: Sidecoach's objective rule LOGIC is independently SPEC-CORRECT (adversarial fixtures - cascade/multi-layer-alpha/ARIA/presentation-role, answers derived from spec, known WITHOUT the referee - pass + Codex). The real Stage-1 objective achievement = a CAPABILITY GAIN: 0 a11y detection -> a working independently-verified spec-correct a11y scanner. NOT a comparative win (contrast edge render-confounded; we lose heading). Floor "logic-cleared" on the eval basis; NO objective-comparative or real-world claim until S5b (genuine render) + a render-decoupling test.
RULINGS: (1) FOLD BATCH-2 - the 10 Codex general-spec findings; esp #1/#2 heading ARIA role+aria-level (lifts the class we LOSE to oracle toward 1.0) + #9 gray-on-color subtype (our weakest 11/17). (2) DEV CORPUS: agreed - all-clean result -> spec-fixtures+Codex for the small remaining objective gap; build a real DEFECT-BEARING dev corpus for the subjective frontier. (3) SEQUENCING (lead call): fold batch-2 to finish the objective LOGIC + a quick render-decoupling sanity check, then MOVE TO THE SUBJECTIVE/TASTE FRONTIER (where the mission is actually won - we trail 0.033 vs 0.277); full S5b genuine-render is DEFERRED as the objective-finalization to complete BEFORE any final objective claim. No objective-win claim banked until then. HEADLINE to Jonah: spec-correct a11y now exists where it didn't (verified) - a capability gain, NOT a beat; taste is next.

## Branch state at this pass
sidecoach-phase2-reimplement, 7 Stage-0 commits, all non-destructive: 8c77f897 oracle comparator+smoke | 8fb6114f corpus integrity tooling | e378b1d2 Codex hardening (7 holes) | 6930adf1 candidate sourcing | d53c7ef5 migration harness scanner goldens | 1c269efe subjective-labeler tool-enforcement. HARD CHECKPOINT before Stage 2's first destructive deletion stands.

## Files
- .claude/memory/session_2026-06-23_sidecoach-stage0-lead-verification.md (this)
