---
name: P4a-1 v2 Codex review - needs CLEAN REWRITE (changelog contradicted task bodies)
description: P4a-1 v2 still NEEDS-FIXES; root cause = I patched the changelog but not the task bodies, making the plan self-contradictory; this beat is the complete fix-spec for a clean v3 rewrite
type: project
relates_to: [session_2026-06-13_p4a1-plan-v2.md, feedback_autonomous_phases_codex_partner.md]
---

Codex P4a-1 v2 (task-mqcyy723; session 019ec340) NEEDS-FIXES. CLOSED: P1-2
(missing/coverage-gap required rules -> inconclusive over effective set), P1-5
(sourceSeverity guard), P1-6 (exact derivation predicates).

**ROOT CAUSE / lesson:** I folded fixes into the v2 CHANGELOG as "binding
requirements" but left the TASK BODIES showing the old code -> the plan
contradicts itself (changelog says X, Task says not-X). A changelog that
contradicts its task bodies is worse than none. FIX BY REWRITING THE TASK BODIES,
not adding a summary. Self-analysis: at deep session depth I patched-by-summary to
save effort; it produced an inconsistent artifact Codex correctly rejected.

**COMPLETE FIX-SPEC for a clean v3 (edit the TASK BODIES):**
- P1-1: materialize findings BEFORE the early error/inconclusive returns too -
  every returned ProductValidationResult (error/inconclusive/findings/clean)
  carries the findings materialized from effective fails (error may carry none).
- P1-3: the evaluator must actually CONSUME requiredCoverageByScope. Define a
  run-derived coverage input = per-rule {ruleId, inspectedFiles[],
  discoveredApplicableFiles[], evidenceKindsPresent[]} and a SATISFACTION
  function: a required rule is coverage-satisfied iff (requireAllDiscovered ?
  inspected superset of discovered : inspected non-empty) AND at least one
  supportedEvidenceAlternative is present. Not a renamed id list.
- P1-4: ProductValidatorRegistration += optional validateProduct?; capability must
  be GENERATED by Task 4 from an AUTHORED BASE field that distinguishes
  empty-flow advisory vs none (e.g. authored {flowId, productValidatorIds,
  baseCapability:'advisory'|'none'}); generated capability = productValidatorIds
  .length>0 ? 'product_validator' : baseCapability; --check authored-vs-generated.
- P1-7: Task 4 BODY must export pure validateRegistry()/deriveValidator() and the
  test BODY must contain failing-first fixtures for EACH rejection (empty
  requiredRuleIds, missing metadata, two owners, conflicting alias, unsatisfiable
  coverage, undocumented severity divergence).
- P1-8: Task 2 BODY spells out all 6 seed rules WITH sourceVocabulary +
  sourceSeverity (not one sample + placeholder).
- P2-1: Task 2 BODY wording corrected (22 canonical rules, each aliasing the
  cross-registry duplicate ids for the SAME semantic; use the REAL ids - the
  polish-standard numeric id + the extended-domain POLISH_0NN - not invented
  EXT_POLISH_001); test representative + conflicting-alias rejection.
- P2-2: Task 3 capability test asserts exact generated equality + every owner
  registered + every lane-member validator classified.
- P2-3: Task 4 generates EXPLICIT zero tolerance entries per owned blocking
  (severity,findingClass) pair (not {}).
- NEW P1: Task 5 test must compile - pass validatorId + coverageSatisfiedRuleIds
  (or the richer coverage input), NOT inspectedRequiredRuleIds.
- NEW P1: missing-required-rule synthesis must NOT fabricate severity/findingClass
  - look up the ProductRuleDefinition (via getRule/by ruleId) for the real
  canonicalRuleKey/severity/findingClass; if truly unknown, mark inconclusive
  with a normalizedErrorCategory, never invent 'major'/'unknown'.
- NEW P1: ProductValidationResult.coverage must be REPRODUCIBLE - carry the
  run-derived inspectedFiles/skippedFiles/source kinds/measuredScope, not empty.
- NEW P1: evidence-compatibility MODEL - define how evidenceRequirements values
  ('css-rule','computed-style','dom','markup','contrast') map to source kinds
  ('css','scss','tsx',...) so the coverage satisfiability guard is well-defined.
- NEW P1: generated-capability circularity - resolve via the authored baseField
  above.
- NEW P2: duplicate required-rule results (same ruleId twice in input) - reject or
  mark inconclusive, don't silently collapse via Map.
- NEW P2: validatorError.category must be NormalizedErrorCategory (closed vocab),
  not any.

**Decision:** do a CLEAN v3 rewrite of the whole P4a-1 plan with task bodies
correct + the evidence-compat model + reproducible coverage. NOT a 4th patch.
This is P4's foundation; getting the model right is worth the rewrite. Per
autonomous mandate, continue; if v3 still doesn't converge, that pattern is the
signal to reassess scope with Jonah.

Collaborator: Jonah.
