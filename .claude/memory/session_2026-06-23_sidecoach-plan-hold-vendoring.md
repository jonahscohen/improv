---
name: HOLD - Sidecoach plan v13 engineering-converged but Jonah held on the VENDORING bet (foundation under reconsideration)
description: v13 reached true convergence (architect self-loop + lead round-4 independent gate both APPROVE; Jonah's success bar met) - the engineering is sound. But Jonah declined the central strategic bet (vendoring oracle's code/corpus as the base). Plan foundation paused; awaiting Jonah's reservation + new direction (likely reimplement-and-own with oracle as a studied reference, not vendored code). No plan edits until direction relayed.
type: decision
relates_to: [session_2026-06-23_sidecoach-evolution-plan-draft.md, session_2026-06-23_sidecoach-plan-codex-log.md, feedback_sidecoach_mission_beat_oracle.md]
---

Collaborator: Jonah Cohen

## State
v13 reached TRUE CONVERGENCE on engineering: architect's self Codex loop (round 13 APPROVE) AND the lead's round-4 INDEPENDENT gate both APPROVE the same version, and Jonah's parity-on-floors + strictly-better-on-differentiators success bar is met. The engineering is sound and not in question.

Jonah HELD on the central strategic bet: he does NOT want to VENDOR oracle's code/corpus as the base. That removes v13's foundation. Lead instruction: STAND BY, do NOT start Phase 2, do NOT edit the plan yet; a v14 will swap the vendoring foundation for the new one once Jonah relays his reservation + direction.

## What FALLS with vendoring removed (must be reworked in v14)
- CONTRACT 4 "vendor read-only oracle detector tree + sync test" -> becomes REIMPLEMENT-AND-OWN the detector (regex/static-html/visual + the contrast/OKLCH/var-resolution floor), with oracle as a studied reference/quality-bar, not copied code.
- The VENDORING - CENTRAL STRATEGIC BET section + Apache-2.0 LICENSE/NOTICE/sync-from-tag handling -> removed (no vendored code to attribute).
- TASTE CURRENCY "pinned oracle upstream + scan --taste-sync diff" -> taste corpus becomes OWN-authored; "currency" mechanism re-grounded on our own sources, not a pinned oracle tag.
- CONTRACT 6 parts that compare against a VENDORED baseline -> the head-to-head eval still runs Sidecoach vs oracle (oracle installed as an external comparator/quality-bar), but "match the vendored floor" reframes to "match oracle's measured floor with our OWN implementation".

## What STANDS (vendoring-independent, keep verbatim)
- Collapse-the-sprawl thesis: one scanner, one vocabulary, one classifier, bill of materials, net-reduction spine.
- Protected-leads preservation: qa-run convergence (Contract 1), BuildReport contract (Contract 3), references-as-context-bundle, beats memory, the 26-flow migration matrix (Contract 2/2a/2b).
- The UNGAMEABLE head-to-head eval METHODOLOGY (Contract 6): external corpus sourcing + provenance, A5 leakage control, mandatory independent dual judges + human adjudication, power-locked N + paired bootstrap CI, fixed refresh cadence, parity-floor vs strictly-better-differentiator gate types. (Oracle stays as the external comparator; only "vendored baseline" wording changes.)
- DELETION DISCIPLINE (rollback/compat/deletion criteria), distributability/plugin work (plugin.json, pin, update-check, graceful degradation, install-from-copy), MCP shim-until-audit.
- Jonah's success bar (parity-on-floors + strictly-better-on-differentiators).

## Strategic note for v14
Reimplement-and-own is actually MORE mission-aligned on one axis (true ownership, no derivative-code identity question) but RAISES the bar on the detector floor: we must now PROVE our own contrast/OKLCH/var-resolution implementation MATCHES oracle's correctness (Contract 6 A1-A4 become harder - they validate OUR code against oracle-as-oracle, not a copied engine). The "don't reinvent" efficiency argument flips: we DO reinvent, but own it; the eval is what guarantees we didn't regress by reimplementing. This is a real cost trade Jonah is choosing deliberately (ownership > vendoring-efficiency).

## Action
Standing by. No plan edits, no Codex runs, no Phase-2 code until the lead relays Jonah's reservation + new foundation direction. Then: fold v14 (swap foundation, keep all vendoring-independent work), re-run both gates.

## Files
- .claude/memory/session_2026-06-23_sidecoach-plan-hold-vendoring.md (this)
