---
name: tilt-lab - consolidated
description: Canonical merge of 39 tilt-lab project beats (2026-05-29..06-04) - concept, runtime/compositor, 25-effect acquisition + 1:1 fidelity, asset delivery, instrument UI + QA, tilt-verify, portable embed export.
type: project
supersedes: [session_2026-05-29_tilt-lab-acquisition-exec.md, session_2026-05-29_tilt-lab-acquisition-plan.md, session_2026-05-29_tilt-lab-asset-delivery-wired.md, session_2026-05-29_tilt-lab-brainstorm.md, session_2026-05-29_tilt-lab-builderC-scrub-export.md, session_2026-05-29_tilt-lab-canvas-shell-thumbnails.md, session_2026-05-29_tilt-lab-cd-review.md, session_2026-05-29_tilt-lab-compositor-bug.md, session_2026-05-29_tilt-lab-fidelity-sweep.md, session_2026-05-29_tilt-lab-foundation-exec.md, session_2026-05-29_tilt-lab-foundation-plan.md, session_2026-05-29_tilt-lab-FUNCTIONAL-COMPLETE.md, session_2026-05-29_tilt-lab-instrument-foundation.md, session_2026-05-29_tilt-lab-layer-composition.md, session_2026-05-29_tilt-lab-media-and-post-pipeline.md, session_2026-05-29_tilt-lab-qa-gate.md, session_2026-05-29_tilt-lab-restoration-complete.md, session_2026-05-29_tilt-lab-restore-DONE.md, session_2026-05-29_tilt-lab-restore-loop.md, session_2026-05-29_tilt-lab-role-dropdown.md, session_2026-05-29_tilt-lab-scope-reconciliation.md, session_2026-05-29_tilt-lab-taste-DONE.md, session_2026-05-29_tilt-lab-taste-elevation.md, session_2026-05-29_tilt-lab-ui-audit.md, session_2026-05-29_tilt-lab-ui-exec.md, session_2026-05-29_tilt-lab-ui-fixes.md, session_2026-05-29_tilt-lab-ui-plan.md, session_2026-05-29_tilt-verify-tool.md, session_2026-05-30_tilt-lab-header-and-dnd.md, session_2026-05-30_tilt-lab-justify-tooltips.md, session_2026-05-30_tilt-lab-layer-cards.md, session_2026-05-30_tilt-lab-tooltips-justify.md, session_2026-05-31_tilt-lab_motion-core_fidelity_audit.md, session_2026-05-31_tilt-lab-fidelity-audit-libs.md, session_2026-05-31_tilt-lab-fluid-fx-interactions.md, session_2026-05-31_tilt-lab-post-fx-fixes.md, session_2026-05-31_tilt-lab-regent-fidelity-audit.md, session_2026-06-03_tilt-lab-export-and-skill-PLAN.md, session_2026-06-04_tilt-lab-embed-export.md]
relates_to: [feedback_tilt_lab_fidelity_mandate.md, decision_behavioral_verifier_build_own.md]
---

# tilt-lab - consolidated

Consolidation of 39 `type: project` tilt-lab session beats spanning 2026-05-29 to 2026-06-04 (collaborator: Jonah). tilt-lab is a sidecoach-owned, locally-served visual-effects playground in claude-dotfiles: browse a catalog of ~25 generative/shader effects, layer compatible ones, tune params live, and export a paste-anywhere embed that renders the exact previewed stack in any project. This canonical carries forward every durable fact and decision at recall level; the 39 originals remain on disk (superseded, never deleted) and are grep-able for deep detail. The standing beats that govern this work stay individually visible and were NOT consolidated: `feedback_tilt_lab_fidelity_mandate.md` (1:1 fidelity mandate), `session_2026-05-29_tilt-lab-asset-gap-FAILURE.md` + `_recon-team.md` (feedback), and the decision beats `_design-direction.md`, `_pointer-contract-gap.md`, `_recon-synthesis.md`, `_slider-accent.md`.

## Concept, scope, and architecture
- tilt-lab (named by Jonah, was working title "shader-lab"), inspired by basement.studio's shader-lab. Lives in claude-dotfiles, owned by sidecoach, launched on demand into a cmux pane / browser tab, ALWAYS served locally. (brainstorm)
- 4 locked foundational decisions (AskUserQuestion): (1) home = claude-dotfiles, sidecoach-owned, local-only; (2) output format = framework-agnostic Web Components (`<tilt-{id}>`, `<tilt-stack>`); (3) sourcing posture = verbatim everything, personal-use, attribution/license/redistribution stored as per-effect metadata (not hard-enforced in v1); (4) app stack = Vite + React + TS shell, effects stay framework-free web components. Package dir `<project>/tilt-assets/<stack-name>/`. (brainstorm)
- Runtime contract: `Effect { init, frame, resize, setParam, dispose }`. Manifest carries id/name/category/layerRole/params/requiredAssets/origin/license/attribution/redistribution/tags. Generic manifest-driven web-component wrapper owns canvas mount, RAF, ResizeObserver, prefers-reduced-motion, dispose. (brainstorm)
- Layering model: 4 layer roles - background (exclusive max 1), midground (stackable), pointer (stackable), post (exclusive max 1, composited last). Valid order bg -> mid -> pointer -> post. Playground greys out incompatible adds with a reason. The post layer must sample the composited output beneath (the known hard part). (brainstorm)
- Repo dir: `tilt-lab/{app (Vite+React), runtime (effect contract + web-component bundles), runtime/effects (per-effect + manifest.json), runtime/lib, server, dist}`. Top-level `shaders/` is unrelated ghostty terminal shaders. (brainstorm, foundation-plan)

## Plans (sequence, not one monster plan)
- Built as a SEQUENCE of plans. Key resequencing decision: the spec listed Acquisition as Phase 1, but the foundation (runtime contract) MUST precede acquisition, else N parallel agents invent incompatible contracts. So Plan 1 = foundation (critical path), Plans 2-4 = acquisition / UI / server. (foundation-plan)
- Plan docs under `docs/superpowers/plans/`: 2026-05-29-tilt-lab-foundation.md, -acquisition.md, -playground-ui.md. Design spec at docs/superpowers/specs/2026-05-29-tilt-lab-design.md. (foundation-plan, acquisition-plan, ui-plan, brainstorm)

## Foundation (Plan 1) - BUILT
- 9 TDD tasks, built inline while a recon team fetched source concurrently: (1) scaffold package.json/tsconfig(resolveJsonModule)/vitest.config(happy-dom)/.gitignore; (2) types.ts (Effect/Manifest/LayerRole/ParamSpec/LayerConfig/EffectOpts/EffectFactory); (3) manifest.ts validateManifest; (4) stack.ts validateStack + orderLayers (bg max1, post max1, mid/pointer stackable, stable role-order sort); (5) element.ts defineEffectElement generic wrapper (canvas mount, RAF, resize, reduced-motion single static frame, attr->setParam, dispose on disconnect); (6) compositor.ts Compositor + tilt-stack; (7) effects/gradient reference effect (Canvas2D, the TEMPLATE acquisition agents copy); (8) index.ts barrel + registerBuiltins + effectFactories registry; (9) build.js esbuild bundle -> dist/tilt-runtime.js (ESM). FOUNDATION COMPLETE: 24/24 tests, tsc clean, bundle smoke passes. (foundation-exec, foundation-plan)
- Vitest MUST run from inside tilt-lab/ (happy-dom env is lost from repo root). esbuild mirrors justify/build.js pattern. (foundation-plan, acquisition-exec)

## Acquisition (Plan 2) - 22 effects ported + registered
- Contract addendum: Effect gained OPTIONAL `onPointer?(x,y)` + `mount?(host,opts)`; new runtime/pointer.ts PointerTracker (element-relative pointermove); element.ts wires mount() after init, PointerTracker when onPointer present. Deps added: three ^0.170, ogl ^1.0.11, cobe ^0.6.3. Conformance template runtime/effects/_TEMPLATE.test.ts.md (3-case + headless-GL-guard). (acquisition-exec, acquisition-plan)
- Team `tilt-acquire` (5 teammates) ran 9 lane tasks (one agent per lane, write effect dirs only, no runtime/index.ts edits to avoid write conflicts; central registration in Task 6). Hit a batch-spawn double-claim race on lane 1 (see feedback_team_spawn_claim_race.md) - resolved by moving an agent off lane 1. (acquisition-exec)
- `three` ships no type declarations (TS7016 repo-wide); FIX = `npm i -D @types/three@^0.170.0` (real types + skipLibCheck), NOT a `declare module 'three'` shim. ogl + cobe ship their own types. (acquisition-exec)
- ACQUISITION COMPLETE: 22 effects + gradient = 23 registered in runtime/index.ts (manifest+factory pairs, validateManifest at registry build, builtinManifests exported for the UI catalog). runtime/integration.test.ts added. 106 tests / 31 files PASS, tsc 0, build OK. Role corrections (builders followed recon over the brief, correct): swarm=pointer, halftone=post, interactive-grid=midground. Renderers used: Canvas2D, raw WebGL1/2, three(+addons), OGL, cobe, vanilla-DOM. (acquisition-exec)
- Carry-forwards flagged then: image effects shipped PROCEDURAL FALLBACKS (no photo binaries); OGL effects passed dpr:1 (revisit); fluid GPU particle layer compiled but unwired. (acquisition-exec)

## Compositor render bug (systemic, found by visual verification) - FIXED
- Symptom: clicking an effect added the layer + param controls but the CENTER PREVIEW stayed BLACK. (compositor-bug)
- Two-part rework first: the Plan-1 Compositor never sized its canvas or called effect.resize(), and shared ONE canvas across all layers (a canvas holds only one context kind). Reworked to per-layer OWN element stacked in the host (position:absolute; inset:0; 100%; mix-blend-mode per layer; z-index by order), ResizeObserver driving resize() for all layers, PointerTracker on the host root forwarding onPointer. (compositor-bug, ui-exec)
- ROOT CAUSE of the persistent blackness (found via instrument + cmux console, a debugging-protocol win over theorizing): the compositor's own one-liner `if (!this.root.style.position) this.root.style.position='relative'` clobbered PreviewCanvas's `.preview-canvas` (which fills its parent via `position:absolute; inset:0`), collapsing the host to ~0 height -> canvas h=1 (2px) -> invisible. Hit EVERY layer (Canvas2D + WebGL). FIX: only set relative when computed position is `static`/empty (never clobber absolute/relative/fixed). After fix lava-lamp (OGL) + aurora (raw WebGL2) render full-screen, visually confirmed. Earlier hypotheses (effects create own canvas; double-getContext attribute mismatch) were red herrings. (compositor-bug, ui-exec)

## Scope reconciliation + fidelity restoration (the 1:1 mandate)
- After a `/loop` "leave nothing out, do not fail me again" directive, re-anchored to Jonah's ORIGINAL verbatim 25-effect list. The acquisition had wrongly DROPPED 4 requested effects and ADDED an unrequested one. (scope-reconciliation, restore-DONE)
- The 25-effect catalog (ids): fluid, fractal-glass, halftone, mesh-gradient, swarm (regent); grain-gradient, neuro-noise (paper.design); animated-gradient (spell.sh); globe (cobe); particles (casberry); cursor-trail (unlumen); aurora (unlumen); dithered-image, fake-3d-image, glass-slideshow, mc-globe (motion-core globe), halo, infinite-gallery, interactive-grid, lava-lamp, neural-noise, plasma-grid, specular-band, water-ripple (motion-core, all 12); ascii (ascii-magic). NOTE: TWO globes (cobe + motion-core) and TWO noises (paper neuro-noise + motion-core neural-noise) were both intentionally requested. (scope-reconciliation)
- Reconciliation: rebuilt the 4 wrongly-dropped (spell animated-gradient [substituted paper "swirl" was WRONG], glass-slideshow, infinite-gallery, mc-globe [wrongly deduped vs cobe]); REMOVED the unrequested paper "swirl"; demoted "gradient" to a non-catalog test fixture (CATALOG_EXCLUDE / renamed "Reference Gradient" to stop colliding with spell "Animated Gradient"). (scope-reconciliation, restore-loop, restore-DONE, restoration-complete)
- Autonomous `/loop` team `tilt-restore` (7 pre-assigned tasks, no spawn-race): #1 verifier tool, #2 glass-slideshow + infinite-gallery, #3 mc-globe + animated-gradient + DELETE swirl, #4 regent restore + wire fluid GPU particle layer, #5 paper + ascii, #6 motion-core 9, #7 globe/particles/cursor-trail/aurora. (restore-loop)
- Contract additions for fidelity: optional `onPointerLeave?()` on Effect (wired element.ts/compositor.ts) for cursor-trail mouseleave wipe. globe markers drivable via params but need a future `marker-list` ParamType (array of {location,size,color}). (restore-loop, restore-DONE)
- Result: catalog = exactly 25 requested effects; full fidelity restored (full original param sets, real defaults, nothing fabricated). 26 effect dirs = 25 catalog + gradient fixture; swirl orphan `git rm`'d. tsc clean, vitest 133/133 / 38 files, build OK, bundle smoke catalog=25. (restoration-complete, restore-DONE)
- Inert-but-1:1 params kept for fidelity (present because the original exposes them though its render does not read them): fluid.curl, fractal-glass.fluidInfluence/glassAmount/bloomStrength, halftone.fluidInfluence. spell named presets (Prism/Lava) NOT reproduced - recon only had ranges, reproducing would be fabrication; the 16 atomic params are the full surface. (restoration-complete, restore-DONE)

## Fidelity audits (per-effect 1:1 vs originals)
- Audit method per effect: read recon report `docs/superpowers/tilt-lab-recon/lane-*.md` (verbatim original + param table); for regent effects ground truth is the local source `/Users/spare3/Documents/Github/regent/app/(app)/tools/<effect>/` (presets.ts/shaders.ts/types.ts; the `(app)` parens break Read globbing - stage clean copies to /tmp); diff for anything dropped/simplified/fabricated; restore additively; verify tsc+vitest+visual. (fidelity-sweep, regent-fidelity-audit)
- mesh-gradient (worked exemplar): build had FABRICATED colors (#3a1c71/#d76d77/#ffaf7b/#2c3e50, in no original preset) and dropped the `scene` select + 5 scene presets + colorStops. Restored 5 SCENE_PRESETS verbatim (preset0 #8ecae6/#219ebc/#023047/#ffb703/#fb8500...), scene select + color1..5 params. (fidelity-sweep)
- regent lane-1 (fid-regent, 5 effects): mesh-gradient already 1:1 (vertex+fragment verbatim, PlaneGeometry 1.5x1.5x200x200, uFrequency 3,6, 5 presets). fluid FIXED - port had ADDED uniforms the original hardcodes; reverted to verbatim (MouseForce v*=0.999/R=0.015/targetVelocity=mouseVelocity*dx; MouseDye hardcoded 0.9797/0.9494/0.9696; Display abs(); GL filter modes velocity/pressure/divergence/particles=NEAREST, dye=LINEAR; final blit plain copy not additive; splatForce min 0.1->1); dead params kept inert exactly as original ships. swarm FIXED - restored verbatim drawShape geometry (diamond/triangle/sparkle 4-point star/cross 12-point). halftone+fractal-glass+lib/fluid-solver.ts migrated to ONE shared GPU Three.js sim (createFluidSim/stepFluidSim) - verified verbatim (FLUID_SHARED_GLSL, SIMPLEX_NOISE/curlNoise, per-effect FluidConstants). fractal-glass manifest ranges fixed vs FractalGlassControls (highlight/midtone/shadow max 3->2, fluteCount 10-150->2-100, ior max 2->2.5, etc). (regent-fidelity-audit)
- motion-core (6 effects): halo/dithered-image/fake-3d-image already 1:1. glass-slideshow superset (9 uniforms exposed; 5 extra with neutral 1.0 defaults). infinite-gallery verbatim shaders. mc-globe MAJOR fixes vs upstream GlobeScene.svelte/Globe.svelte: restored display-transform layer (uDisplayScale/Offset/Rotation + rotate2/transformUv helpers), restored uTime + full marker pulse loop, REMOVED a fabricated `radius` param (upstream has none; uScale is constant DEFAULT_GLOBE_SCALE=1; real props scale/offsetX/offsetY/rotation), frame() now advances uTime. globe params verified (pointCount 15000, landPointColor #f77114, rimColor #FF6900, atmosphereColor #FF6900, etc). (motion-core_fidelity_audit)
- library/interactive (4 effects, all already 1:1 - audit only): globe -> cobe v0.6.5 (phenomenon v1 API: toggle(false) to kill its RAF, drive render() from frame; FIX = reverted manifest from README demo values to cobe's real library defaults mapSamples 10000/mapBrightness 1/diffuse 1/dark 0/baseColor #ffffff/markerColor #ff8000); particles -> casberry reimpl (13 params, 8 render-style shaders verbatim, fog dropped for transparent midground, OrbitControls autoRotate replaced with worldGroup.rotation.y at exact rate); cursor-trail -> unlumen vanilla-DOM port (duration 400ms cubic-bezier(0.23,1,0.32,1), itemSize 120/trailLength 8/spawnDistance 80/rotationRange 20); aurora -> unlumen bare WebGL2 quad (fragment shader verbatim, 25 params, dpr capped at 2). (fidelity-audit-libs)

## Asset delivery (the real functional gap) - CLOSED
- The gap (self-analysis): 9 effects declare requiredAssets but the runtime hardcoded `assets: {}` in compositor.ts + element.ts, so those effects rendered PROCEDURAL FALLBACKS; tilt-verify SKIPPED their canvas-paint and skips were wrongly counted as passes. "skip != pass." (asset-gap-FAILURE feedback - standing, not consolidated; FUNCTIONAL-COMPLETE)
- Fix (team tilt-assets, asset-core): runtime/effects/<id>/assets.ts for all 9 asset effects; central runtime/effect-assets.ts registry (own module = acyclic; index.ts re-exports); compositor.ts + element.ts pass `assets: effectAssets[id] ?? {}`; build.js esbuild png/jpg dataurl loader; 10 generated sample PNGs (scripts/gen-sample-assets.mjs, 384x384; fake-3d depth.png = grayscale radial). (asset-delivery-wired, FUNCTIONAL-COMPLETE)
- KEY FIX (agent caught a bad brief assumption): `new URL('./x.png', import.meta.url)` is NOT transformed by esbuild (any loader) -> would deliver nothing. Switched to default image imports (`import x from './assets/x.png'`) which esbuild inlines as base64 AND Vite resolves natively. Added runtime/assets.d.ts ambient png/jpg decls. (asset-delivery-wired)
- Validated: tsc 0, vitest 133/133, both bundlers build (17 inlined base64 PNGs, 0 broken refs). tilt-verify canvas-paint went 16pass/9skip -> 25 PASS / 0 skip (every effect paints REAL content). Claude-in-Chrome confirmed real content (dithered-image dithers the warm-sphere sample with a visible Bayer pattern, mc-globe orange atmosphere, cursor-trail trails 10 real images after a real mouse path, etc). The 2 remaining tilt-verify skips = headless-no-GPU perf advisories (fluid, particles), fine on real GPU. (asset-delivery-wired, FUNCTIONAL-COMPLETE)

## Image/video media layer + post-pipeline fix
- Added the user-supplied media background the source tools had and had been dropped: new `file` ParamType (types.ts + manifest.ts PARAM_TYPES) + ParamControls `<input type=file accept=image/*,video/*>` -> object URL via setParam; new `media` effect (id "media", BACKGROUND, params source/fit(cover|contain)/opacity, loads URL as Image, falls back to looping muted video, default bundled assets/sample.png). BUG fixed: init used `params.source ?? assets.source` but file param defaults to "" so `??` returned "" - changed to `||`. (media-and-post-pipeline)
- POST-PIPELINE FIX: for a Canvas2D `post` layer the compositor composites the lower layers' canvases into its OWN canvas before frame() (the earlier per-layer-canvas rework had dropped this, so ascii/halftone posts had no input). Catalog became 26 (25 + media). ASCII over a Media background visibly asciifies the real image - confirmed in Chrome. (media-and-post-pipeline)
- Later WebGL analogue (fx-mc-post + fluid-fx): optional `Effect.onBeneath(source: HTMLCanvasElement)` hook - for a post layer the compositor composites enabled beneath layers into a compositor-owned 2D scratch canvas (composeBeneath/composeBeneath honors per-layer opacity) and hands it over once per frame so WebGL posts (getContext('2d')===null) can upload it as a texture. (post-fx-fixes, fluid-fx-interactions)

## Pointer interactions + post-overlay variants
- Root bug across fluid/fractal-glass/halftone: compositor/PointerTracker deliver canvas-relative PIXELS (top-left origin) but the effects' onPointer treated x/y as normalized [0,1], so injection was effectively dead. FIX: normalize pixels by canvas CSS size. New pure helpers `fluidClipFromPixel(px,py,w,h)` (fluid) and `pointerUVFromPixel(px,py,w,h,pressed)` in lib/fluid-solver.ts (fractal-glass/halftone). (fluid-fx-interactions)
- fluid injection gate matched verbatim to the shader: force+dye splat ONLY while pressed (drag); hover just tracks. Added onPointerDown (seeds last=current, no velocity spike), onPointerUp, onPointerLeave. fractal-glass/halftone use the inverse (hover stirs, press pauses) - verbatim regent inversion. (fluid-fx-interactions)
- New post-overlay variants createFractalGlassPostEffect / createHalftonePostEffect + dirs fractal-glass-post/ + halftone-post/ (layerRole 'post') consuming onBeneath so the glass refracts / dots halftone the LIVE layers beneath; fall back to generated bg standalone. element.ts now dispatches the full pointer surface (pressed + down/up/wheel) so single `<tilt-fluid>` etc. get drag too. 219 tests green. (fluid-fx-interactions)
- post-fx-fixes (3 broken effects): dithered-image added onBeneath (dithers beneath, flipY:true); fake-3d-image was UPSIDE DOWN (root cause flipY:false on both color+depth textures -> changed to flipY:true; labelled colorSrc/depthSrc params); water-ripple added onBeneath + onPointerDown ripple-on-press (shared spawnWave()). ParamSpec gained optional `label`. 190/190 tests. (post-fx-fixes)

## tilt-verify tool (our own verifier)
- Built `tilt-lab/verify/` (tilt-verify), expect-inspired but NO expect branding (grep-clean), leveraging /tmp/expect-src under Jonah's personal-local-use authorization (see decision_behavioral_verifier_build_own.md). Playwright vs localhost:5180, diff-aware. cli.mjs + lib/{catalog,git-diff,checks,png}.mjs. Invoke `npm run verify` (diff-aware) / `-- --all` / `-- <ids>` / `-- --json` / `-- --headed`; non-zero exit on fail (CI-ready); typecheck stays green (verify/ outside tsconfig include). (tilt-verify-tool)
- 5 functional checks/effect (functional truth only, visual stays with screenshots): add-layer, canvas-paint (real PNG pixel decode; post effects seeded over a base; skipped for asset effects pre-asset-wiring), param-interaction (real input), perf-frames (~1s rAF), console-clean. NON_CATALOG skip set (gradient) mirrors the live catalog. Full-catalog after asset wiring: 25 effects, 0 failures. fluid perf ~12fps in headless = headless-no-GPU artifact (no GPU accel), confirmed fine on real GPU - calibrated to advisory-in-headless rather than lowering fluid quality (which would break fidelity). (tilt-verify-tool)

## UI build (Plan 3) + instrument redesign
- Plan 3 Vite+React playground (catalog-driven, exercisable with just the gradient fixture): state modules framework-free + unit-tested - catalog.ts (loadCatalog returns runtime builtinManifests, pre-validated; filterCatalog), stackStore.ts (add/remove/reorder/setParam, add gated by validateStack, subscribe for useSyncExternalStore); components ParamControls / BrowseGrid / PreviewCanvas (React<->Compositor bridge, RAF, setLayers on change) / LayerStack / TopBar / ProjectPicker / AddShaderModal / App shell (3-col grid browse|preview|layers). app on port 5180 -> dist/app; vitest environmentMatchGlobs app/**->jsdom, runtime stays happy-dom. 122 tests/35 files, build 948KB (three+ogl heavy). (ui-plan, ui-exec)
- Instrument-not-dashboard redesign (design-team): self-hosted JustifySans/JustifyMono (Anthropic faces via the justify rename) into app/public/fonts; styles.css FULL REWRITE - warm near-black surface ladder (--bg #0c0b0a), Yes& red #dc2618 accent (hover #b01f15), shared primitives (.module/.module__label/.meta/.value/.btn/.btn--accent/.icon-btn 40x40/.field), canvas-dominant grid with [data-rail-left/right=collapsed] + .app[data-fullscreen], tight radii 3/5/6px. Builders write co-located component CSS, NEVER edit styles.css (CD-owned). (instrument-foundation, design-direction decision - standing)
- Builder A (shell+browse): App.tsx rail-collapse + fullscreen state (Esc exits), BrowseGrid live-thumbnail grid, NEW ThumbnailPreview. Builder B (layers+composition): LayerConfig gained optional `enabled?`/`opacity?`; stackStore setEnabled/setOpacity; compositor honors enable (display:none + skip frame) + opacity (style.opacity / globalAlpha); LayerStack channel strips (drag handle, enable, opacity fader, % readout). Builder C (controls+export): Slider scrub-to-edit numerics (PX_PER_STEP=4, double-click inline input, Shift=10x coarser, one quantize() clamp+snap), server-free export.ts (buildStackConfig/serializeStackConfig `tilt-lab/stack` v1 JSON, downloadStackConfig/copyStackConfig), AddShaderModal re-homed. (canvas-shell-thumbnails, layer-composition, builderC-scrub-export, instrument-foundation)
- Thumbnail re-architecture (critical perf fix): 26 simultaneous live WebGL contexts exceeded the browser ~16-context cap -> "THREE.WebGLRenderer: Context Lost", letter-fallback on most cards, main preview starvation. Off-screen pausing does NOT help (a created context is held regardless). NEW design: a module-level QUEUE + single async pump() worker (concurrency 1) renders each effect ONCE through a SHORT-LIVED context - factory() -> init(canvas,{params,assets}) at 160px -> step 24 frames @ 33ms simulated to settle -> canvas.toDataURL('image/png') -> dispose effect -> free GL via getContext('webgl2'||'webgl').getExtension('WEBGL_lose_context').loseContext(). Synchronous per effect (drawing buffer intact at toDataURL, no preserveDrawingBuffer). posterCache: Map<id, dataURL|'failed'>; cards render `<img src=poster>` / shimmer skeleton / dark letter tile. At most one GL context ever exists. Posters are stills = reduced-motion friendly. (canvas-shell-thumbnails, cd-review)
- CD review: LOOK transformed (mono wordmark, warm dark, dominant preview, composition panel). Found 3 dead features (enable/opacity/export) caused by Builder A's App.tsx not passing the new props - fixed directly. Added &dev logo (verbatim brand SVG marketing-site/assets/and-dev-white.svg). cobe globe poster gen throws one async exception (getUniformLocation: image onload races shared-context dispose) -> graceful dark "G" fallback. (cd-review)
- Role filter pills -> Select dropdown (5 options > 4, styled native dropdown) per Jonah's screenshot. (role-dropdown)

## UI audit + fixes (sidecoach QA)
- /sidecoach audit (flowK 5-dimension) on tilt-lab/app/src: a11y 2, perf 2, theming 2, responsive 1 (worst), anti-patterns 3. Verdict: clean (not AI-slop) but generic + desktop-only. (ui-audit)
- tilt-ui team fixes (strict per-file ownership): P0 responsive @media (<=900 narrows rails, <=640 single-column stack, preview order:-1 on top); P1 :focus-visible accent ring; P1 param-label wrap (minmax + min-width:0); P2 40x40 hit targets (--hit-min); P2 prefers-reduced-motion guard; P3 21 hardcoded hex -> :root vars; modal a11y (aria-modal/autofocus/focus-trap/Escape/backdrop-close); PreviewCanvas role=img + aria-label; vite manualChunks vendor split (app 1005KB -> 244KB + vendor-three 492/react 194/ogl 60/cobe 12); NEW PRODUCT.md + DESIGN.md (Google spec, lint 0/0). QA gate (critique+polish) added a tabular-nums `<output>` per slider + scale-on-press. 137/137 tests. (ui-audit, ui-fixes)
- KNOWN TOOLING BUG (backlog): @google/design.md v0.2.0 linter crashes on NESTED color/spacing groups (colorStr.trim / raw.match) - worked around with FLAT tokens; marketing-site/ + reference/ DESIGN.md fail this (queued T-0043). (ui-fixes, qa-gate)

## Taste elevation + QA gate
- tilt-taste team (4 agents, co-located CSS): replaced raw HTML controls with designed primitives controls/ (Slider/Switch/Select[segmented when <=4 opts]/ColorField/FileDrop), Lucide IconButton (verbatim paths), COLLAPSIBLE param groups via <details> accordion when >8 params grouped by camelCase prefix (aurora 25 -> General+Layer1-4+Movement+Sky; fluid 15 -> General/Splat/Dye/Color) - kills the slider wall. Declined per-role color dots (single-accent / no-color-only-signaling brand law). 151/151 tests, build app 248KB. (taste-elevation, taste-DONE)
- QA triad PASS (qa-gate): audit 5-dimension PASS; KEY finding was a stale DESIGN.md (old #5b8cff/system-font dashboard) fully resynced to instrument tokens, `npx @google/design.md lint DESIGN.md` 0/0. Slider fills toned to muted, red on :active/:focus-visible (per the slider-accent decision beat - standing). tactical-polish largely satisfied (tabular-nums, font-smoothing, scale-on-press, no transition:all, 40x40, focus rings, flat-by-design hairlines). (qa-gate)

## Punch-list polish (2026-05-30)
- Header refresh + DnD: header 44->60px (3-zone grid 1fr auto 1fr); ProjectPicker native select -> centered contextual popover menu (role=menu, outside-click/Escape close); rightmost actions icon-only (Lucide Plus/Download/Copy/Check); removed paint-order caption; drag-and-drop reorder replaced native HTML5 DnD (not reliably automatable) with POINTER-based reorder (grip handle + setPointerCapture; onPointerMove measures each .channel mid-line via getBoundingClientRect to pick the drop slot; slot->index conversion to = from < slot ? slot-1 : slot; keyboard up/down retained for a11y). (header-and-dnd)
- Layer cards rework: enable Switch -> eye/eye-off IconButton in the actions row [up][down][eye][trash]; cards collapse to a single row (grip + stacked title/role + chevron disclosure, aria-expanded); drag handle stripped to a bare grip span (no button wrapper, no index number); EyeIcon/EyeOffIcon verbatim Lucide. (layer-cards)
- Tooltips: on-brand Tooltip component (portal to body, getBoundingClientRect viewport-clamp, mouse-hover + keyboard-focus triggers gated by a document-level input-modality tracker, Escape/scroll/resize hide). Then restyled to match Justify's toolbar tooltips (ref justify/core/toolbar.ts ~400/545-599): pill (var(--r-full)), drop shadow 0 2px 8px rgba(0,0,0,0.3) (the one intentional shadow Jonah wanted), padding 5px 14px, 11px JustifySans-500, INSTANT show (delay default 0) with a 120ms opacity+translateY fade via rAF (mount at opacity0+offset, rAF flips .tooltip--visible so the CSS transition runs). 167/167 tests. (NOTE: the original `_tilt-lab-tooltips.md` beat is already superseded by `_tilt-lab-tooltips-justify.md` and was left out of this consolidation.) (tooltips, tooltips-justify, justify-tooltips)
- HARNESS NOTES (recurring, worth keeping): chrome MCP `hover` updates CSS :hover but does NOT dispatch JS mouse/pointer events - verify hover-triggered UI via the focus path (focus a sibling, Shift+Tab onto target; a keypress arms keyboard mode). chrome MCP coordinate offset makes small-target clicks miss - use ref-based clicks (find -> ref). Run `npm test` not bare `npx vitest run` (the latter misses the jsdom env from vite.config). (tooltips, layer-cards, tooltips-justify)

## Export + embed (deliverable, 2026-06-04) and the "open tiltlab" skill
- 2026-06-03 PLAN beat: Jonah approved two equal-value builds (portable embed export; a fuzzy "open tiltlab" skill) but the build was BLOCKED mid-start by macOS Full Disk Access / TCC loss (harness Read/Edit/Write of pre-existing files failed EPERM; new-file writes still worked - same failure mode as handoff_2026-05-24). Full design captured for mechanical resume. Resume needs the user to re-enable Full Disk Access. (export-and-skill-PLAN)
- Built after TCC restored (embed-export, deliverable 1 of 2): the OLD export emitted only an inert `tilt-lab/stack` v1 config JSON, and `<tilt-stack config-src>` needed that JSON hosted at a fetchable URL - so "paste your export into Claude and retrofit it into a hero" did not hold across repos. (embed-export)
- runtime/index.ts new `mountStack(host, config, opts?)` + MountStackOptions: registers builtins (idempotent), news a Compositor over effectFactories, setLayers, runs a RAF loop UNLESS `prefers-reduced-motion: reduce` (then paints one static frame via renderFrame(0)); returns a disposer (cancels rAF + compositor.dispose()); `respectReducedMotion:false` forces animation. Closes the reduced-motion gap that `<tilt-stack>`'s own loop has (element.ts guards, the stack element does not). (embed-export, export-and-skill-PLAN)
- app/src/lib/export.ts `buildEmbedSnippet(layers, {runtimeUrl?, className?})` returns a self-contained HTML block: an absolutely-positioned background div (inset:0; z-index:0; pointer-events:none) + a CLASSIC `<script>` (NOT module - `document.currentScript` is null in ES modules) that captures the host via `document.currentScript.previousElementSibling`, inlines the config as `var config = {...}`, then `import(runtimeUrl).then(m => m.mountStack(host, config))`. Plus copyEmbedSnippet (clipboard) + downloadEmbedSnippet (.html). Constants EMBED_SNIPPET_FILENAME, DEFAULT_RUNTIME_URL='./tilt-runtime.js', DEFAULT_EMBED_CLASS='tilt-bg'. New CodeXmlIcon (lucide code-xml, verbatim) + a "Copy embed snippet" icon button in TopBar (disabled until layers exist). (embed-export)
- Verified: npm run typecheck clean; npm test 254 passed (46 files, 8 new); npm run build -> dist/tilt-runtime.js exports mountStack (`aR as mountStack`). Browser-verified in Chrome: a throwaway dist/embed-verify.html with the exact buildEmbedSnippet shape rendered the Aurora WebGL stack full-bleed as the hero background with content layered above (z-index:1 over z-index:0); the Copy-embed button swapped to the check on click (handleCopyEmbed -> copyEmbedSnippet). (embed-export)
- Deliverable 2 (open-tiltlab skill): claude/skills/tilt-lab/SKILL.md created (canonical) + mirrored to ~/.claude/skills/tilt-lab/SKILL.md. Body = launch (npm run dev :5180 -> open browser -> explain loop) + the retrofit contract (vendor dist/tilt-runtime.js, NEVER reimplement the shader, mount via mountStack, background positioning, reduced-motion, match tokens). dist/tilt-runtime.js is GITIGNORED so consumers run `npm run build`. (embed-export, export-and-skill-PLAN)

## Key environment / infra notes (carry forward)
- Dev server: `cd tilt-lab && npm run dev` -> Vite on port 5180 (watch the NODE_OPTIONS shim, see session_2026-05-31_node-options-shim-fix.md). (export-and-skill-PLAN)
- runtime/index.ts exports: effectFactories, builtinManifests, registerBuiltins(), defineEffectElement, defineStackElement, setStackFactory, Compositor, PointerTracker, validateStack, orderLayers, effectAssets, mountStack, types. (export-and-skill-PLAN, embed-export)
- Inside cmux with CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1 the silent `Agent` tool is blocked (must use TeamCreate + named teammates, or do the work directly). Concurrent multi-writer collisions: re-read on "File has been modified since read" and check git diff. (regent-fidelity-audit)

## Superseded originals (39, on disk + grep-able for deep detail)
session_2026-05-29: tilt-lab-acquisition-exec, -acquisition-plan, -asset-delivery-wired, -brainstorm, -builderC-scrub-export, -canvas-shell-thumbnails, -cd-review, -compositor-bug, -fidelity-sweep, -foundation-exec, -foundation-plan, -FUNCTIONAL-COMPLETE, -instrument-foundation, -layer-composition, -media-and-post-pipeline, -qa-gate, -restoration-complete, -restore-DONE, -restore-loop, -role-dropdown, -scope-reconciliation, -taste-DONE, -taste-elevation, -ui-audit, -ui-exec, -ui-fixes, -ui-plan, tilt-verify-tool. session_2026-05-30: tilt-lab-header-and-dnd, -justify-tooltips, -layer-cards, -tooltips-justify. session_2026-05-31: tilt-lab_motion-core_fidelity_audit, -fidelity-audit-libs, -fluid-fx-interactions, -post-fx-fixes, -regent-fidelity-audit. session_2026-06-03: tilt-lab-export-and-skill-PLAN. session_2026-06-04: tilt-lab-embed-export.
