- [** ACTIVE ** Stage 4b SHIPPED (not committed) - default-typeface Ground B (brand mismatch) wired LIVE. It shipped in 4a calibrated + tested but INERT because nothing fed the seam a committed family. SOURCE (determined from repo, not invented): DESIGN.md `typography.<role>.family` tokens - the one place a project names a CHOSEN face; PRODUCT.md has none. New `loadCommittedFontFamilies`/`committedFontFamilies` in project-context.ts take the LEAD family of each stack (quote-aware), dedupe, and DROP system/generic leads by reusing the scanner's own SYSTEM_FONT_STACK_FAMILIES (single-sourced). Flows DESIGN.md -> run-validator.ts (from context.projectPath) + audit-rendered.ts (from context.projectPath||cwd) -> the existing `TypefaceFindingOptions.brandFamilies` seam. FAIL-CLOSED by construction and asserted at 3 layers: no DESIGN.md / no typography / system-only -> `[]` -> opts undefined handed to the scanner -> Ground B silent (a page with no committed face is NOT a mismatch - the sacred property). Scanner class touched ZERO lines (data passed IN); rendered-checks.ts already Ground-B-aware, unchanged; BRAND_PRESENCE_MIN=0.25 + Ground A untouched. A5a UNAFFECTED (grades typefaceFindingFromScore(score, {}) empty-opts): re-run OURS R=1.000 P=1.000 FP=0/18 exit 0. Verify: 3 required e2e checks pass (mismatch FIRES brand-mismatch, used does NOT, no-brand does NOT); npm test 76 suites (75 baseline + new wiring suite, 23 assertions); build clean, generate-validators --check no drift. Codex 2 rounds: 1 High folded (orchestrator `/sidecoach audit` passed no projectPath -> blind cwd could grab an unrelated project's family -> fixed with context.projectPath||cwd, the idiom used twice in that method) + confirmed closed. Not committed - PROJECT (2026-07-24)](session_2026-07-24_stage4b-groundb-live-wiring.md)
- [** ACTIVE ** Stage 1a/1b + probes LEAD-VERIFIED - stage1-mine built provider-sample.mjs (1a, key-gated Claude/gpt-5.4/Gemini adapters, fail-closed no-key exit 2) + defect-distribution.mjs (1b, SHIPPING scanner over samples, inconclusive excluded from denominators, exit-3 on all-inconclusive) + the two open probes, all eval/ new files, npm test green (76, +1 from a concurrent teammate's run-tests.ts line, 0 failed). LEAD independently confirmed the load-bearing bits: (a) re-ran Probe 1 myself - REPRODUCES: effects >=10pp feasible within a $200/line budget, <=5pp cost ~$40k and are indistinguishable from generation noise, so **Stage 1d is feasible ONLY as a coarse gross-priming-line finder, NOT for fine prose tuning** (scopes 1d before we build it); (b) grepped the model IDs - ships claude-opus-4-8 + gpt-5.4 ONLY, no hallucinated IDs. The Codex reviewer web-searched and FALSELY flagged those as stale in favor of nonexistent claude-opus-5/gemini-3.6-flash; the teammate correctly PUSHED BACK and kept the environment's authoritative IDs - the right call, agents must not fold to a hallucinating reviewer. Probe 2 (our own concept sameness, decides if 2c earns its effort) HONESTLY PENDING - needs a provider key (none in env); tool proven on mocks, no number fabricated. Codex folded 5+NOTE (BLOCKER: all-inconclusive no longer exits 0 clean; N-sizing bias corrected). HELD uncommitted for a clean wave integration (3 teammates still editing the shared tree) - PROJECT (2026-07-24)](session_2026-07-24_stage1a-1b-provider-defect-mining.md)
- [** ACTIVE ** Sidecoach Stage 3b SHIPPED (not committed) - `claude/hooks/sidecoach-detect.sh` + `test-sidecoach-detect.sh`, the REAL detect hook path replacing the removed fake. Advisory PostToolUse wrapper over the Stage 3a CLI. LOAD-BEARING SPLIT: honest COUNT (the CLI's fail-closed verdict; a scan that did not run is `inconclusive`, NEVER clean) + fail-OPEN DECISION (only CLI exit 0 is silent `{}`; findings/inconclusive/infra all surface as an EXPLICIT not-clean advisory via additionalContext; hook ALWAYS exits 0, never blocks, never auto-fixes). This is the exact bug the fake hook had - conflating "could not scan" with "clean". Scope filter: local UI extensions only (html/css/tsx/jsx/vue/svelte/astro + css/html variants), non-UI skipped silently; http(s):// forwarded to the CLI. CLI path resolved SCRIPT-RELATIVE via realpath (no hardcoded home dir - the SKILL.md bug). Portable timeout via python subprocess (no macOS coreutils `timeout`). test = 34 passed 0 failed: STUB tier (engine-independent 0/1/2/3+malformed translation matrix incl. inconclusive!=clean) + LIVE tier (real CLI over real fixtures + unroutable URL). All 5 verify checks pass. Touched ONLY claude/hooks/. NOT auto-registered - left to Jonah. Codex-reviewed - PROJECT (2026-07-24)](session_2026-07-24_stage3b-detect-hook.md)
- [** ACTIVE FEEDBACK ** Visual-verify gate armed on EVAL-FIXTURE HTML writes; Jonah overrode (no rendered UI deliverable) - the Stop gate keys on visual write EXTENSIONS (.html/.css/.tsx) and cannot tell a product UI file from an eval fixture, so any sidecoach session authoring test HTML arms it. This turn was agent orchestration + a read-only audit; the armed files were detector/labeler fixtures (p04-webfont.../clean-page.html) graded BEHAVIORALLY by tests + the A5a gate, several deliberately painting as plain system fonts so a screenshot proves nothing. RULE: when the gate fires and the only visual writes are eval/test fixtures, that is a legitimate override - state it and take it, don't perform a meaningless screenshot; when a REAL product UI changed, honor it with an actual screenshot Read. Flag cleared - FEEDBACK (2026-07-24)](session_2026-07-24_visual-gate-override-eval-fixtures.md)
- [** ACTIVE ** simplify-audit VERIFIED by lead (spot-check before trust) - the two mission-primary gaps' 06-23 claims were RE-MEASURED and mostly hold, with the ENGINE-REBUILT-SINCE hypothesis DISPROVEN (convergence unified the DETECTOR, not routing/vocabulary). Lead independently confirmed the load-bearing structural claims: processWithEntryPoint = 1 occurrence (its own def at sidecoach-orchestrator.ts:643), ZERO callers = dead router; flow-handlers-new-tiers.ts (416 lines) ZERO importers = dead; run-tests.ts is a 75-entry ALLOWLIST against 157 test files on disk (~half never run). ONE audit number was INFLATED 2.5x: it claimed node_modules = 6370 tracked files; real count is 2473 (of 8584 tracked under sidecoach - that total was right). Trustworthy on structure, loose on one count - exactly why lead verifies. Plan at docs/superpowers/plans/2026-07-24-simplification-plan.md (stamped e378a632): top consolidations = delete 5 dead handler dupes (838 lines) + 5 dead router modules (~775) + triage the 86 orphaned tests; ~22 modules/~3.7k non-test lines removable with the gate proving each. TWO JONAH-DECISIONS surfaced (not agent calls): vocabulary collapse (opt B recommended - keep PHASE_ALIASES, 5 surfaces -> 2 typed, zero breaking) and mcp-server retirement (collapses the triplicated classifier to duplicated). Committed node_modules (2473 files) flagged as its own decision, NOT unilaterally untracked. Execution HELD until wave-1 src/ locks clear - PROJECT (2026-07-24)](session_2026-07-24_simplification-plan.md)
- [** ACTIVE ** Autonomous WAVE 1 dispatched (4 teammates) - Jonah pushed everything (0 ahead, clean @e378a632) and handed over autonomous execution of the backlog + started-not-finished + unstarted plan stages. stage1-mine: the TOP-RANKED untouched Stage 1a/1b (provider sampling + defect distribution on the SHIPPING scanner) PLUS the two open probes the lead flagged and never answered - the POWER question blocking 1d ("infeasible at any affordable N" is an acceptable finding) and OUR OWN concept sameness (only the rival's was ever measured; it decides whether 2c earns its effort). groundb: finish 4a Ground B, inert because nothing supplies a committed family live - load-bearing constraint is it must stay SILENT when none is known, since a false brand-mismatch on every DESIGN.md-less project is worse than inert; frozen 0.25 untouched, certified A5a must reproduce. distrib: manifest + package metadata + the absolute paths left in dogfood/tests, told to OMIT rather than invent and to FLAG not perform any rename. simplify-audit: READ-ONLY on the two mission-primary gaps nobody has touched, instructed to VERIFY the year-old 06-23 claims first (engine was rebuilt registry=engine since) then produce a risk-sequenced CONSOLIDATION plan with runnable verifies and a QUANTIFIED prize, presenting vocabulary collapse as costed OPTIONS because that is Jonah's product call. Coordination: same tree with disjoint ownership (worktrees rejected - node_modules + tracked dist make them expensive), audit forbidden from building so it cannot race, all reviews via the DETERMINISTIC codex-review.py wrapper not the downgrade-prone agent, all beats to the canonical root dir. Wave 2 queued: 3b/3c, 4b/4c/4d, 2a-2d, the audit's plan, 337 unindexed beats, darwin-only containment - PROJECT (2026-07-24)](session_2026-07-24_autonomous-wave1-dispatched.md)
- [** ACTIVE ** Beats corpus CONSOLIDATED + session committed in 11 units - sidecoach/.claude/memory/ was not stray teammate files but 37 beats of real history back to 2026-05-21 running as a SECOND corpus beside the canonical root record. Why it mattered concretely: a teammate reported two of my beats "do not exist" and proposed working around their absence - it was reading the nested dir while the beats sat in the root one; a split record produces confidently WRONG reports about what has been recorded, the opposite of a continuity layer's job. Migrated with git mv (33 renames confirmed, history follows), 4 untracked moved, filenames unique so links survived. Link sweep CAUGHT a pre-existing dangling ref imported from the nested index (phase2_task8 -> phase2_task8_9); re-swept 45 links 0 dangling, 1057 beats now in ONE place. TOMBSTONE not deletion: bash-guard correctly BLOCKED git rm against .claude/memory, so the old path now holds a pointer telling anything landing in sidecoach/ to write to the root - better than deleting, it stops the split recurring. FLAGGED not fixed: 337 root beats are referenced by NEITHER index. Housekeeping: pycache untracked (every hook run dirtied the tree) and .shots ignored (the harness comment claimed gitignored but only corpus had a rule). 11 commits direct-to-main carrying the reasoning + honest limits so the record survives without the beats. Tree CLEAN, post-commit 75 suites green, 98 ahead of origin NOT pushed - PROJECT (2026-07-24)](session_2026-07-24_beats-consolidated-and-session-committed.md)
- [** ACTIVE ** security_reminder_hook SEVEN SIBLING RULES now path-gated - closes the defect class flagged by the exec fix: new_function/eval/dangerouslySetInnerHTML/document.write/innerHTML/pickle/os.system all matched PLAIN SUBSTRINGS with NO path gate, so they fired on markdown, prose and any language and then exit(2). Reproduced all 10 payloads at exit 2 first (incl. `model.eval()`, `redis.eval()`, and the bare word "pickle" in an English sentence); this beat's own first write was BLOCKED by the pickle rule. Fixed with the same shape as the exec fix - a path gate plus a call-shape check: _WEB_EXTS for the browser-DOM rules (deliberately BROADER than upstream 2.0.6, which gates them to JS only and thereby drops inline `<script>` in HTML and server-side templates), NOT _DOC_EXTS for eval plus a dot-excluding lookbehind, _PY_EXTS for pickle/os.system, pickle narrowed to DESERIALIZATION only while keeping cPickle/cloudpickle/pandas/numpy recall. New `regex` key in check_patterns compiled ONCE at import. REVIEW CAUGHT 3 REAL DEFECTS, all folded: (1) str.endswith is CASE-SENSITIVE so README.MD was still BLOCKED for prose - the original bug reintroduced by my own fix - while mod.PY/app.JS silently lost their rules; (2) executable exts omitted by the gate (.pyw/.ipy/.pyx + 8 template languages) - a recall loss the gate introduced; (3) `import pickle as pk` aliased loads slipped through. A 4th surfaced only from driving the REAL subprocess: a perf optimization read on-disk evidence ONLY for JS exts, silently starving any matcher on a non-JS rule - replaced with _needs_file_evidence() derived from the rule table. SECOND PASS after the independent reviewer landed folded 12 MORE findings, one CRITICAL: I reported "backtracking measured clean" and was WRONG - the from-import branch WAS quadratic (86s on a 608KB single line; a 3000-cell .ipynb blocks the editor 5s because json.dumps emits one line). My perf test NAMED that shape in its docstring then measured the one input that cannot exhibit it (one occurrence, long line) - right hypothesis, wrong probe, false green. Fixed by moving the branch out of the regex into a BOUNDED-window scan, and the perf test now varies OCCURRENCE COUNT and asserts a GROWTH RATIO (4x input must cost under 8x) instead of a threshold at one size. Also folded: eval gate INVERTED to an allowlist (the "everything except prose" polarity blocked .patch/.diff/.snap/.toml/.sql/Dockerfile, and with no declaration guard blocked `func eval`/`fn eval`/`def eval` - writing an interpreter was a denial of editing); `$` added to the eval lookbehind (AngularJS `scope.$eval` fired - my own fix script tripped it); whitespace-before-paren dropped as a deliberate test-locked trade; extensionless executables now sniff the SHEBANG (bin/ scripts and container entrypoints slipped all 7 gates); .svg/.xhtml/.aspx/.pug/.vm/.tpl/.heex/.es6 added, .mdx/.astro for the React prop; os alias + `from os import (system)` + the `handler = os.system` REFERENCE form (a regression - the old substring caught it); MultiEdit joined edits with a SPACE and FABRICATED sinks across the seam; regex compile moved inside the fail-open guard; path_check finally lowercased. Classifier gained a `stale` state via an embedded `_SG_PATCH_REV` - marker-presence alone could never upgrade this script's OWN prior output, and markers must now be real CODE not a comment mention. Suite asserts WHICH rule fired and covers Write/MultiEdit (neither tool path was tested at all). 99+37 green. CODEX WAS UNRELIABLE HERE: 6 runs, all exit 0, none produced a report (dead local MCP server aborts it) - gate ran via the sanctioned independent-Claude fallback + a direct probe harness, recorded honestly. VERIFIED: 62/62 new sibling cases + 37/37 exec cases, all 10 repros now exit 0, vendored copy byte-identical, --check clean, durability round-trip patches an exec-only copy while leaving 2.0.6 byte-identical. Classifier hardened to require BOTH markers so an exec-only copy is no longer skipped as done - PROJECT (2026-07-24)](session_2026-07-24_security-hook-sibling-rules-path-gates.md)
- [** ACTIVE ** security_reminder_hook child_process exec detection NARROWED - the plugin's PreToolUse hook matched a bare call token as a PLAIN SUBSTRING with no path gate, so every RegExp `.exec()` call was BLOCKED (exit 2) as shell command injection, with advice to use a `.ts` util that does not exist here. Root cause found by version archaeology: three copies on disk and the LIVE one is the STALE `unknown` cache entry (Apr 8, PreToolUse, blocking), NOT the newer 2.0.6 (PostToolUse, non-blocking) which upstream had ALREADY fixed - so the machine never received the fix. Replaced with a JS-gated three-tier matcher: qualified forms always fire, a namespace alias member call fires, and a bare call fires only with child_process import evidence (read from the ON-DISK file, since an edit's new_string is one line and lacks the import header). The dot-excluding lookbehind is the load-bearing guard, NOT the import gate - proven by the repro file legitimately importing the Sync variant at line 23, so an import-only fix would still have false-fired. VERIFIED: reproduced exit 2 BEFORE touching anything, then 16/16 exec cases (8 TP still block, 8 FP now pass incl. the real repro file) + 10/10 other-rule cases green. Durability: vendored patched copy + idempotent re-apply script that backs up, is round-trip tested, and REFUSES to touch an unrecognized version so it can never downgrade a newer upstream. Honest narrowing recorded: rule no longer fires on Python. SIBLING DEFECT FOUND: 7 of 8 remaining rules still have NO path_filter and fire on prose - one blocked this very beat - PROJECT (2026-07-24)](session_2026-07-24_security-hook-exec-detection-fix.md)
- [** ACTIVE ** security_reminder_hook RegExp `.exec()` FALSE POSITIVE diagnosed - the blocking hook is the STALE `unknown` plugin-cache copy, proven by three independent tells (its hooks.json registers PreToolUse on Edit|Write|MultiEdit with the bare `python3 ${CLAUDE_PLUGIN_ROOT}` invocation from the bug report; it is the only copy naming `src/utils/execFileNoThrow.ts`; it ends in sys.exit(2) which BLOCKS, while 2.0.6 only injects context). Root cause: a bare call token in a plain-substring list, so `/re/i.exec(str)` matched. Reproduced exit 2 against the exact fixture BEFORE any change. Impact is trust, not just noise - a security hook that cries wolf gets ignored when it is right. Self-demonstrating: the hook blocked this beat for quoting the token - PROJECT (2026-07-24)](session_2026-07-24_security-hook-regexp-exec-false-positive.md)
- [** ACTIVE DECISION ** A5a **CERTIFIED** - ground truth hardened, ZERO label flips, ship call RE-ISSUED. Auditing the whole channel (forced by the withdrawal) found something WORSE than the filename: `codex exec --sandbox read-only` restricts WRITES ONLY, so the labeler could READ the fixtures dir AND `typeface-a5a-labels.json` - the answer key itself. Live probe before: "Can you read the labels json? YES"; after a seatbelt deny-read profile: "NO ... sandbox_apply: Operation not permitted", and nested sandbox_apply fails so the labeler has no shell. THREE containment layers now: opaque shot-<sha256>.png attachments + assertNoLeak/assertPromptClean, seatbelt deny-repo-read, and auditTranscript() failing any page whose transcript reaches for the corpus. DECISIVE RESULT: **23/23 labels UNCHANGED** vs both the leaked-filename and cssom-v1 bases, regenerated from a FRESH sink - so the withdrawal was correct on PROCESS while the ground truth it was withdrawn over was in fact SOUND. Numbers held: OURS R=1.000 P=1.000 FP=0/18; ORACLE generous R=0.200 P=0.063 with 15/18 (83.3%) FP (fires overused-font on 11 of 12 REAL pages); strict R=0.000. Regex replaced by a CSSOM walk (nested @media/@supports keep their real inner selector - `body` was being DROPPED; unmatched rules now reported as "puts this font on NOTHING"; author names sanitized while body/h1/code/[role]/:nth-child survive). 65/65 assertions; Codex adversarial 4 Critical + 6 High folded. CORRECTLY REJECTED one Codex finding (bucketing families into chosen/system) - that is the DETECTOR's classification and would make GT a copy of what it grades; precedent worth keeping. LIMITS: recall on 5 CONSTRUCTED positives only (heldout-recall structurally ungradeable), no significance claim at this N, containment is darwin-only, family names still verbatim by design, Inter/Poppins monoculture still honestly undetected. Teammate wrongly claimed 2 beats missing - verified present, it looked in the wrong dir. npm test 75 green, corpus verify OK, detector untouched (mtime-verified). Nothing committed - DECISION (2026-07-24)](session_2026-07-24_a5a-CERTIFIED-hardened-ground-truth.md)
- [** ACTIVE RESULT ** A5a declared-stack relabel WORKED - recall 0.417 -> 1.000, and 3 more ground-truth defects found (ALL in the extractor, NONE in the detector). Numbers: OURS R=1.000 (4/4) P=0.800 FP=1/19; ORACLE generous R=0.250 P=0.063 with 15/19 (78.9%) false fires; ORACLE strict R=0.000 (detects nothing). All SIX branded fixtures + martinfowler flipped to correctly-absent; Codex notes prove declared-stack reasoning ("Inter, SF Pro, Geist declared" / "Only system and generic stacks requested"). ZERO FP across all 12 REAL shipped pages. DEFECT 1: extractor conflated @font-face DEFINITION with APPLICATION - p04 defines two Brackish faces then sets `body{font-family:system-ui}`, so OUR detector was RIGHT and the ground truth was wrong; fixed by reporting APPLIED rules (with selectors) separately from DEFINED faces. DEFECT 2 (most dangerous): the extractor LEAKED p04's own CSS comment stating the answer into the labeling prompt - would have produced a PASSING gate built on contaminated ground truth, destroying author!=labeler independence; fixed by stripping CSS comments, leak-check assertion added. DEFECT 3: dev-subjective-label.mjs keeps a DUPLICATE signal map I missed, so labels recorded signal:'screenshot' while the prompt used TYPEFACE; fixed with a lockstep comment. Also: security_reminder_hook.py FALSE-POSITIVES on RegExp string-matching (thinks it is child_process shell exec) - Jonah ruled rewrite-with-match + durable hook fix task spawned; the same hook then blocked THIS BEAT for quoting the token, worked around per the CLAUDE.md beat mandate. Nothing tuned to force a pass; the DETECTOR was never touched. Re-run in flight - PROJECT (2026-07-24)](session_2026-07-24_a5a-declared-stack-result-and-extractor-fixes.md)
- [** ACTIVE ** A5a construct FIXED to DECLARED-STACK + new TYPEFACE labeling signal (Jonah ruled it). A rubric REWORD ALONE would have been useless - the harness decides what Codex SEES via its VISUAL/TEXTUAL/MOTION sets and default-typeface sat in VISUAL (screenshot), so the identical mismatch would have re-run. FOUR coordinated edits: (1) rubric definition now "what the page REQUESTS", with the disambiguator stated outright - a page naming a chosen family is ABSENT even if that font is unavailable and paints as a plain system face; (2) default-typeface moved OUT of VISUAL into a new TYPEFACE signal category with the webfont-blocking reason recorded inline; (3) harness gained TYPEFACE set + typefaceDeclarations() extracting every declared font-family from style blocks/inline attrs plus an @font-face marker, buildPrompt tags [TYPEFACE] + carries a FONT-FAMILY DECLARATIONS section + explicitly forbids using the screenshot for it, recordLabels writes signal:'typeface'; (4) DELIBERATELY no share threshold/selector logic in the extractor - applying the shipping 0.75 cutoff to ground truth would make the label a COPY of the detector it grades. Verified before spending the pass: rubric SHA 61a6a246 -> bcd9c4bc17f3 (safe - verify does not read rubricSha), and buildPrompt probe shows n01 -> `font-family: "Alluvium Sans"` + @font-face vs p01 -> "no font-family declared anywhere". Old screenshot-basis labels PRESERVED as evidence. Operational fix: re-label launched as a LEAD-session background task (the prior pass was orphaned as a teammate's background child - it had finished 23/23 but nobody graded it). Also confirms the spawned verify-candidates task landed GREEN and is now wired into run-tests. NUMBERS PENDING - nothing tuned to force a pass - PROJECT (2026-07-24)](session_2026-07-24_a5a-rubric-declared-stack-fix.md)
- [** ACTIVE RESULT ** Stage 4a A5a gate NOT CLEARED - independent labeling exposed a CONSTRUCT MISMATCH. Real head-to-head on 23 Codex-labeled pages (12 pos/11 neg): OURS R=0.417 (5/12) P=1.000 FP=0/11; ORACLE generous R=0.500 P=0.375 with 90.9% FP (10/11 real pages!); ORACLE strict R=0.000. Grader exit 4. ROOT CAUSE is NOT a detector bug: 6 of the 7 "misses" are the n01-n06 fixtures the AUTHOR built as NEGATIVES which the independent labeler flipped to POSITIVE - because our detector scores the DECLARED font stack (deliberate 4a choice; hermetic render aborts webfonts so painted scoring would fire on every real page) while the harness hands Codex a SCREENSHOT so it scores the PAINTED face. The fixtures declare FICTIONAL families ("Alluvium Sans", "Tessellate Grotesk") that paint as generic system sans; Codex notes say it outright ("Text reads as generic system sans", "Body resembles default Times serif"). On the 5 fixtures actually designed as positives we are 5/5. WIN FOR INDEPENDENCE: an author-labeled ground truth would have scored a clean pass; author!=labeler caught a class-definition ambiguity on its first real use. Operational notes: labeling was NOT stalled (23/23 done by 02:22, teammate just never re-invoked to grade); first grader run FAILED CLOSED (exit 2) because the comparator resolves from $SIDECOACH_ORACLE_DETECT and the clone lives at /tmp/oracle-v4 - fail-closed worked as designed. TO CLOSE: fix the rubric to state declared-vs-painted, match the labeling signal to it, re-label, re-run. Nothing tuned to force green - DECISION (2026-07-24)](session_2026-07-24_stage4a-a5a-RESULT-construct-mismatch.md)
- [** ACTIVE DECISION ** Stage 4a A5a taste-gate CLOSURE dispatched - Jonah chose "close A5a now" (over commit-first/defer) after 3a+4a passed combined Codex review clean. default-typeface was built+dev-calibrated+reviewed but NOT plan-"shipped" (Stage 4 rule: ships only after A5a recall/precision >= floor; no Codex label existed). MECHANISM grounded: A5a = ours-vs-oracle head-to-head graded against Codex present/absent labels; the LABELER is Codex via `codex exec -i screenshot` (subjective-label-harness.mjs), NOT a Claude agent; rule-authors.json registers font-class so its labels are rejected. TWO load-bearing rulings baked in: (1) HONEST recall/precision split - default-typeface positives are constructed (unstyled/system-stack) pages, heldout is real designs with ~ZERO positives (48 pages max 0.058 share), so recall grades on CONSTRUCTED positives only, precision on real negatives, heldout-recall STRUCTURALLY UNGRADEABLE and stated plainly; constructed positives may NOT enter the locked heldout. (2) CORPUS-INTEGRITY guardrail - extending subjective-rubric.md re-SHAs it; teammate must verify corpus-tool.verify's rubricSha handling FIRST and STOP rather than break the 22 frozen classes' labels. Teammate a5a-label EVAL-ONLY, labeledBy=codex only (stop if Codex down, never fabricate), lead retains the SHIP CALL. Running - PROJECT (2026-07-23)](session_2026-07-23_stage4a-a5a-closure.md)
- [** ACTIVE ** Borrow-list RECONCILIATION - every prior oracle/design gap list bucketed vs the 2026-07-23 upgrade plan so the un-covered backlog is visible; wrote docs/superpowers/plans/2026-07-23-borrow-list-reconciliation.md + appended Section 7 (OPEN+UNSCHEDULED backlog) to the upgrade plan. HEADLINE: the plan is BLIND to the 3 MISSION-PRIMARY gaps - maintainability/complexity (6+ routing impls, triplicated classifier), distributability (no plugin manifest), workflow simplicity (4 vocabularies) - the 2026-06-23 rubric GAP3/4/5 that the /goal names as the biggest objectives; worse, the plan ADDS machinery against the "simpler" half. "Fifth borrow list" is a LOOSE count (07-16 beat says "FOURTH" then "THIRD time" one sentence apart) - verified 6 design-competitive gap lists, 11 across both lineages; "prior four never drained" UNDERCOUNTS the debt. named-vibe-variants.md is an _extracted POSITIVE style-reference file, NOT a gap-list beat (citation drift; real capture = 05-25 capability_gap_analysis + external_taste_absorption). Buckets: SUBSUMED 10, SCHEDULED 4, UNSCHEDULED 19, DEAD 9 (incl 4 honesty defects fixed 07-16, Live Mode cut, OMC MCP+eval closed), UNKNOWN 2. Long UNSCHEDULED tail: visualizer authorship, heading-size-by-role, font-pairing, frequency-first motion, iOS 100vh, saturated-aesthetic lanes, Bencium breakpoints, Forms (ZERO coverage), CSS scroll-timelines. No code changed - PROJECT (2026-07-23)](session_2026-07-23_borrow-list-reconciliation.md)
- [** ACTIVE ** Sidecoach Stage 4a SHIPPED - `default-typeface` rendered SUBJECTIVE class on subjective-rendered-scanner.ts, flagging content text not set in a chosen typeface. Ground A (default/system stack >= 75% of char-weighted content, LIVE + promoted + fail-closed like tiny-text) + Ground B (known committed family < 25%, implemented+calibrated+tested behind a seam but INERT on the live path until a committed-family source is wired). Measures the DECLARED stack not the painted face (hermetic render aborts webfonts, so painted-face scoring would fire on every real page) - which is exactly why it out-detects the static oracle on "webfont declared but never applied". LEADING-FAMILY classification (a calibration-caught bug: all-families conjunction scored the -apple-system/.../Roboto boilerplate as "chosen" because Roboto is deliberately excluded). Single-source split (inPageTypeface score + typefaceFindingFromScore threshold, harness sweeps what ships); vocabulary = SYSTEM_FONT_STACK_FAMILIES exported from reference-data.ts, inlined in-page with a drift test. Thresholds frozen on PRINCIPLE: DEFAULT_STACK_SHARE 0.75 (dev max 0.058, bimodal so sweep confirms a band not a point), BRAND_PRESENCE_MIN 0.25 (ground-B sweep DOES discriminate, first FP at 0.50). Honest exclusion: Inter/Poppins monoculture NOT detected (recommended faces, 13/48 dev pages lead Inter well). Calibration 0 FP on 48 real pages + 5/5 positives + ground-B 0FP/0FN. 59->60 rules; 73 suites; no golden drift (goldens cover legacy modules). Codex 2 rounds, 7 findings folded (fail-closed harness, ground-B calibration path, quote-aware split, body-text scope, ground-tagged verdict, bMeasured guard, infra exit-3), 2 new fixes fault-injection-proven. A5a GATE NOT RUN: no Codex subjective label exists for the class (lead's job; author!=labeler, font-class registered). Known limits reported: ground B not live-wired (needs run-validator/audit-rendered - not owned), shadow DOM unscanned (consistent with all siblings). NOT committed - PROJECT (2026-07-23)](session_2026-07-23_sidecoach-stage4a-default-typeface.md)
- [** ACTIVE ** Sidecoach Stage 3a SHIPPED - `bin/sidecoach-detect.js`, ONE fail-closed detect command replacing the deleted fake one. Four lenses: objective/subjective (rendered Playwright via runRenderedAudit), static-ban (the 5 named absolute-ban scanners over RAW file text), static-check (the product rule registry via run-validator's evaluateCleanPolicy). Dispatch: URL -> rendered; dir/source file -> both static lenses; local .html -> BOTH static AND a rendered pass over its file:// URL. NO new detection logic - pure wiring. FAIL-CLOSED verdict generalizes runRenderedAudit over N lenses: nothing attempted/available -> inconclusive, and a PARTIAL scan with ZERO findings -> inconclusive, never clean; a deliberately skipped lens is reported `attempted:false` with a reason so a two-lens clean cannot read as four-lens. Exit 0 clean / 1 findings / 2 usage-IO / 3 inconclusive. Found and fixed a real bug by probing: normalizeRenderUrl rewrote `file:///x.html` to `http://file:///x.html`, so every local render died ERR_NAME_NOT_RESOLVED. 3 additive source changes (audit-rendered, absolute-ban-detector scanContentForAbsoluteBans, project-collector collectFromSingleFile), all behavior-preserving per the goldens. New demanding known-good fixture (all 6 validators must be CONCLUSIVE, not vacuous). Codex: no P0 false-clean path; 5 findings folded incl. a P1 where the CLI invented severity policy instead of reading the registry override for hero-metric-template. All 5 plan verify clauses pass; build green; 73 suites - PROJECT (2026-07-23)](session_2026-07-23_sidecoach-detect-cli-stage3a.md)
- [** ACTIVE ** Stage 3a + 4a LEAD VERIFICATION - lead re-ran the MERGED detect-CLI + default-typeface tree (not a re-trust of teammate reports): `npm test` = 73 suites green, all 5 migration goldens VERIFY OK, generate-validators --check clean at registry 59->60. Zero golden drift is STRUCTURAL (scanner-snapshot goldens cover legacy taste-validator/absolute-ban, not the rendered subjective scanner). Change surface clean + non-overlapping; only shared file scripts/run-tests.ts has both suite lines on separate lines. Two teammate flags checked: suppress-fix-gate flag already SELF-CLEARED (fix-gate consumes it one-shot, no lingering suppression); Ground B (brand-mismatch) INERT on live path BY DESIGN (needs a committed-family field absent from project-context.ts + run-validator/audit-rendered wiring = follow-up). Frozen thresholds recorded: DEFAULT_STACK_SHARE=0.75 on PRINCIPLE (bimodal dist, sweep does not discriminate - comment says so honestly), BRAND_PRESENCE_MIN=0.25 (sweep DOES discriminate). A5a gate NOT RUN honestly (no Codex label exists for the new class; label authoring is a lead job, author!=labeler). STILL PENDING: combined cross-unit Codex review (the seam - CLI reads the registry font-class grew - is unreviewed), A5a label authoring, Ground B wiring. Neither committed - PROJECT (2026-07-23)](session_2026-07-23_stage3a-4a-lead-verification.md)
- [** ACTIVE DECISION ** Sidecoach upgrade plan - FIRST UNITS chosen: Stage 3a (real `bin/sidecoach-detect.js`) + Stage 4a (rendered font-family taste class), built CONCURRENTLY by named teammates, deliberately NOT opening with the top-ranked Stage 1 defect-mining loop. Reasoning: 3a is wiring over scanners that already work and is the soft prereq that makes 1b + every later Stage 4 class trivial; 4a is a computed font-family read against vocabulary already in the repo; both are days not weeks and provably done or not. Stage 1 stays #1 strategically but is the stage most likely to burn weeks of API budget into an unwired JSON artifact. Rejected: amend-and-re-stamp the plan (that IS the documented failure mode - fifth borrow list, prior four never drained). THREE PUSHBACKS logged for later folding: 1d prose-ablation has an unanswered POWER question (delta may be under run-to-run variance); 2c borrows a conclusion - we never measured OUR OWN concept sameness though 1a's sampler gives it nearly free; sequencing puts the payoff last. TWO PLAN-DRIFT CORRECTIONS: font vocabulary is in `src/reference-data.ts` NOT `fontshare-reference.ts` (that is a service class), and bin/ has 7 files not 6 (the no-detect-CLI claim still holds). Baseline probed before dispatch: build green, 71 suites passed - DECISION (2026-07-23)](decision_sidecoach_upgrade_first_units.md)
- [** ACTIVE ** verify-before-done visual arm narrowed to genuine WRITE TARGETS - a Bash command that merely REFERENCED a .css/.tsx (a codemod arg, a cp read source) armed "visual" and demanded an impossible screenshot; new `_visual_write_target` classifies visual ONLY for a redirect target / sed -i / tee operand / cp-mv destination, wired into BOTH the deploy and write branches. Per-VERB semantics, not a positional heuristic: sed -i and tee change every operand, mv destroys its source, cp alone leaves sources untouched. Codex found 5 recall regressions in the first cut (prettier --write, multi-operand sed/tee, mv, punctuation-captured redirect targets, procsub) - the fold SIMPLIFIED rather than patching each. 102 -> 132 rows, 3-way mutation-tested - PROJECT (2026-07-23)](session_2026-07-23_verify-visual-arm-reference-narrowed.md)
- [** ACTIVE ** Lead verification of the visual-arm work + the project_has_ui FAIL-OPEN it did not reach - re-ran all 5 suites and live-probed both directions, then found `project_has_ui` fell through to a bare `return True` when no package.json existed within 6 levels, so EVERY package.json-less dir read as a UI project and, being OR'd, made the write-target check irrelevant on the deploy branch (a codemod naming only a .js armed visual in an EMPTY dir). Fixed by delegating to a bounded `_has_rendered_html` (depth 3, 400-entry cap, unreadable -> over-fire); the sidecoach fixture trap does not apply because those 209 files sit under sidecoach's own package.json. Static-site recall PRESERVED. Also records the Stop-gate corroboration verification (6 controls) - PROJECT (2026-07-23)](session_2026-07-23_verify-arm-lead-verification-project-has-ui-gap.md)
- [** ACTIVE ** Quoted-MENTION arming NOT fixed, deliberately - a command that only mentions a write verb inside a quoted argument still arms; proven UNDECIDABLE at the command-text level: `bash /tmp/probe.sh 'cp a b'` (inert data) and `bash /tmp/runner.sh 'cp a b'` (evaled) differ only in the script NAME, and `eval "$1"` exists live in this repo at test-component-browser.sh:976. Wrapper-allowlist rule simulated over 15 cases: fixes 3 FPs, introduces 8 FALSE NEGATIVES (watch/parallel/tmux/docker/su/nohup/make/./deploy.sh). Declined rather than trade a visible recoverable FP for silent FNs. Proposes the Stop-gate corroboration instead - DECISION (2026-07-23)](decision_verify_hook_quoted_mention_arming.md)
- [** ACTIVE ** Stop gate now CORROBORATES an armed visual flag against the working tree - the gate demanded a screenshot when zero visual files existed (cost Jonah a manual override); it now withholds the demand ONLY when git proves there is nothing to screenshot. Uses --untracked-files=all so a brand-new never-added component still blocks (plain --porcelain collapses it to `?? newdir/` and would fail OPEN), -z so a filename with a space cannot split into a wrong extension, --ignore-submodules=none, and a dir entry blocks. EVERY uncertainty blocks: no cwd, not a repo, git error, timeout, >20k entries. Arming untouched; stop-side VISUAL_EXTS verified a superset of the arm side. 15 -> 31 rows - PROJECT (2026-07-23)](session_2026-07-23_stop-gate-working-tree-corroboration.md)
- [** ACTIVE ** cmux transient permission-denies fixed by a per-target BREAK-GLASS, not by failing soft - the lead specced fail-soft for an unintrospectable cmux and was WRONG; 9 Codex passes broke 3 drafts of it (a close subcommand keeps working while introspection is down; a bare name resolves differently for the shell than the hook; a missing path can be created earlier on the same line - TOCTOU). Verdict stayed DENY, the REMEDY changed: CMUX_CLOSE_UNVERIFIED=<target>, per-target and deliberately not boolean, replaces "restart cmux and lose your session". Tree outages scope to the asserting close only (a whole-line allow would have re-opened the 2026-07-12 incident). DRIFT deliberately still BLOCKS (cmux answered, so it is provably healthy and can still close; proven with a stub whose `top` drifted while close-surface worked). 88 -> 138 rows, 12 Codex passes (10 bypasses folded, 4 of them self-inflicted), final verdict SHIP + 24-case lead probe across 2 rounds. Late passes closed closes hidden in `$(...)` substitutions (assignment AND argument position) and fixed a self-inflicted FP where escaped `\$(` in prose blocked ordinary grep/echo - DECISION (2026-07-23)](session_2026-07-23_cmux-close-guard-breakglass.md)
- [** ACTIVE ** Three UNMANAGED hooks packaged into install.sh, browser-tree.json and the GUI - task-loop-mandate + justify-queue-mandate -> `grounding` cluster, justify-queue-drain-stop -> `justify` component (it polls the daemon on :9223, so non-Justify installs would get a dead Stop hook). Owners derived from what the repo already DECLARED, not guessed from filenames. Found the tree already LYING in the way the guard predicts (grounding tag said "4 hooks", array listed 2). Codex caught 3 prefix-matching assertions that stayed green regardless; mutation test now turns 6 rows RED where 3 were green before. GUI PROVED to derive from the tree (manifest 64 -> 67). Audit exit 1 -> 0 - PROJECT (2026-07-23)](session_2026-07-23_unmanaged-hooks-packaged.md)
- [** ACTIVE ** WHY those 3 hooks escaped the registry gate - PROJECT SCOPE, proven from transcripts not theorised. All three were written with the Write tool (so the matcher was never the hole) by session 702f03ae whose cwd was ppai; improv's guard is project-scoped, so a ppai session never runs it, nothing armed the flag, and the Stop gate returned at `[ -f "$FLAG" ] || exit 0` for 5 days across 2 commits. FIX: the Stop gate now sweeps the DISK unconditionally instead of trusting a write-time breadcrumb (also catches `cat >`/cp/git-pull creation), symlink-safe REPO_DIR, batched audit 1.82s -> 0.43s, and exit 3 "cannot tell" distinct from clean. Codex: 2 High folded. 29 -> 52 rows, 10 negative-controlled. RULE: an enforcement point must re-ask its own question at enforcement time - PROJECT (2026-07-23)](session_2026-07-23_hook-registry-guard-escape-fixed.md)
- [** ACTIVE PLAN ** Sidecoach staged UPGRADE PLAN authored (oracle v4 regroup) - docs/superpowers/plans/2026-07-23-sidecoach-upgrades.md, stamped @a22d41fc, PLAN not implementation, NOT committed. 4 ranked stages operationalizing the chosen gaps: (1 TOP) provider defect-mining loop + skill-prose ablation (sample models from briefs -> per-provider defect distribution via shipping scanner -> build-time counter-rule compilation -> prose ablation deleting lines that PRIME defects; periodic/release; on our eval+briefs+buzzword-calibrate+Codex rails); (2) staged generative authoring (palette OKLCH+WCAG recipe -> pre-render board+mock audited before build -> outside-ranking roll on OUR OWN curated deck -> exclusion-safe artifact/deck presentation); (3) unified `bin/sidecoach-detect.js` CLI + real hook path + registry consolidation (replaces the removed FAKE detect); (4) taste rule-count delta = ~17 CLASSES not 59 hand rules on the rendered engine (font-family, type extremes, structural, motion) + honest exclusions. Each stage has goal/approach/effort/RUNNABLE verify. LIVE/in-browser-variant HARD-EXCLUDED (browser decision page held out; Live Mode fold = separate Justify track). Edges preserved: rendered engine, fail-closed, independence, beats. Codename only - PROJECT (2026-07-23)](session_2026-07-23_sidecoach-upgrade-plan.md)
- [** ACTIVE ** Sidecoach "live" verb / lane_live / canvas mode COMPLETELY REMOVED - Jonah reversed the Live Mode fold; scrubbed the live VERB (verb-command-registry + sidecoach-verbs.json), lane_live (sidecoach-lanes.json), canvas mode (modes.ts + sidecoach-modes.json), the live.md skillRef, orchestrator preflight, 4 engine tests + python-lane + keyword-hook tests, routing golden re-captured, and all docs (SKILL/CHEATSHEET/README/mcp-server). Precision-preserved every unrelated "live" (scanRenderedLive, leaseIsLive, aria-live, live sources, flowN's Justify "live browser" - flowN STAYS, reachable via /rapid + intent). 71 sidecoach + 297 mcp + 128 keyword + 35 python suites GREEN. NOT committed (lead reviews). FLAGGED: orphan SMOKE_TRANSCRIPT.txt still has stale lane_live. CLAUDE.md needed no edit (all "live" = adjective) - PROJECT (2026-07-23)](session_2026-07-23_sidecoach-live-verb-removal.md)
- [** ACTIVE ** oracle Live Mode DEMO built + verified (Justify fold spike) - self-contained Vite app at ~/Documents/Github/livemode-demo (OUTSIDE improv per Jonah) replicating oracle's Live Mode loop: pick element -> chips/freeform annotate -> 3 genuinely-different variants -> hot-swap preview carousel -> Accept WRITES TO SOURCE (Vite plugin upserts a [data-lm] block into src/style.css, consolidated not inline; HMR reflects). Deterministic variant engine (pluggable LLM seam); deliberately NO per-framework HMR rewriting (framework-agnostic stylesheet consolidation - matches the fold rec to SKIP HMR). Doubles as a working spike of the scoped-variant-preview + accept-to-source folds. Verified end-to-end in cmux browser (7 screenshots: render/toggle/pick/generate/cycle/accept/reload-persists). Codename only. To run: npm run dev -> localhost:5188 - PROJECT (2026-07-23)](session_2026-07-23_oracle-live-mode-demo.md)
- [** ACTIVE ** oracle v4 competitive gap analysis + Justify fold rec (4-agent fan-out) - oracle shipped v4 (Apache-2.0, code borrowable; ~48k stars; native Copilot embed = distribution moat). RANKED new gaps: (1) provider-specific DEFECT-MINING loop + skill-prose ablation = their strongest NEW durable capability, fits our eval+Codex substrate - BORROW THIS ONE; (2) integrated generative pipeline (188 "worlds" dealt as challengers -> direction ROLLED FROM OUTSIDE the model ranking -> browser decision -> render-before-build -> build contract -> finishing-review subagent) ships our generative/visualizer/anti-sameness trio as ONE loop; (3) Live Mode = new Justify-rival axis; (4) unified scanner productized; (5) taste rule delta ~59 vs ~42 (COVERAGE-count gap, not engine-truth - ours is rendered Playwright, theirs static). STILL AHEAD: rendered engine on CI path, fail-closed honesty, INDEPENDENCE (their evals real now but single-vendor), beats memory. Justify fold: propose-then-accept gate + scoped client-side variant preview + sidecoach-verb chips; SKIP framework HMR (keeps our framework-agnostic edge). Meta: 5th borrow list, same 3 gaps re-captured, now compounding. Codename-only, zero real identifiers - PROJECT (2026-07-23)](session_2026-07-23_oracle-v4-gap-analysis.md)
- [** ACTIVE ** memory-nudge.sh unquoted-arrow FALSE POSITIVE fixed (last hook in the class) - memory-nudge false-set .memory-dirty on an UNQUOTED `->` arrow in a for/while compound; it de-quotes (T-0033) so quoted arrows were safe, but its writes list still had a bare `"> "` token. Fixed with the same dash-guarded `(?<!-)>>? ` redirect used in verify-before-done. Audit CONFIRMED bash-guard (real token walker) and the multiple-choice hooks (markdown blockquote strip) are NOT affected, so this was the last hook carrying the class. 6 suites green (test-nudge-debounce 51->58). Codex folded 1 High (no-space >>append recall - regex is (?<!-)(?:> |>>)). Recall intact (sed/cp/rm/spaced+no-space appends dirty; 2>&1 + /dev/null clean) - PROJECT (2026-07-23)](session_2026-07-23_memory-nudge-arrow-fp-fix.md)
- [** ACTIVE ** verify-before-done Bash-arm FALSE POSITIVE fixed - the hook fired "take a screenshot" on read-only/prose commands because a `->` arrow contains `> ` and matched the redirect indicator (a for/while/printf compound is not in the read-only prefix list so it reached the arm branch). MINIMAL fix: dash-guard the redirect indicator - `(?<!-)>>? ` matches a real redirect but not the arrow; write VERBS (cp/mv/tee/sed -i) stay SUBSTRING matches so recall is untouched. A Codex review REJECTED my first over-engineered de-quote + command-position attempt (it under-armed bash -c "cp...", \cp, gsed -i, and added a 2>&1 FP - all recall regressions violating prefer-FP); simplifying to the dash-guard folded all 4 findings. 6 suites green (test-verify-before-done 93->102, +9 rows incl. bash-c/gsed recall guards). Receiving-code-review done right: simplify, do not patch each FN. Broke the live hook TWICE with the literal-apostrophe-in-python3-c class - PROJECT (2026-07-18)](session_2026-07-18_verify-hook-dequoted-triggers-fp-fix.md)
- [** ACTIVE ** verify-before-done flag SESSION-KEYED (the real "all projects" fix) - the verify flag was ONE global ~/.claude/.needs-verification leaking across every concurrent session AND project (a .css edit in repo A armed "visual" globally and blocked front-end commits in repo B); yesterday's 2026-07-17 fix only changed the nudge WORDING (arming unchanged), so it kept hurting. Keyed the flag per-session (.needs-verification.<session_id>) across ALL 6 consumers with byte-identical derivation (verify-before-done/-stop, verify-clear, verify-manual, second-fix-gate, bash-guard) - all-or-nothing since a writer/reader path disagreement FAILS OPEN. Plus a once-per-episode nudge throttle (nudge only when the flag STATE changes) so a run of edits stops re-nagging; set_flag untouched so Stop-hook + commit gate teeth intact. Cleared the stuck flag. 5 suites green (93/51/148/15/11, incl. NEW cross-session isolation test) + live E2E probe + Codex review. Self-analysis: broke the live hook mid-edit with an apostrophe inside a python3 -c single-quoted block - PROJECT (2026-07-18)](session_2026-07-18_verify-flag-session-keyed-reigned-in.md)
- [** ACTIVE ** GUI installer DESIGN authored + approved (browser-based, one-click) - a "visually secure" alternative to the bucket-browser TUI for Jonah's TUI-wary team; localhost python3 server (nonce + allowlist + argv-not-shell) serving the EXISTING bucket-browser prototype HTML wired to `install.sh --manifest` (JSON list source) + the shared apply engine; launch via `install.sh --gui` / `ampersand --gui`; 4 units (manifest, gui launcher, server, adapt prototype); v1 = install+toggle-off, deferred = update banner/--personal/bootstrap/restyle. Spec (2026-07-17-gui-installer-design.md) + 6-task TDD plan (2026-07-17-gui-installer.md) both WRITTEN @84432079. SHIPPED + MERGED TO MAIN (0fc4881d, local only, not pushed): install.sh --manifest/--apply-plan/--gui + claude/installer-gui/{manifest.py,server.py,index.html} + 4 new test-*.sh; --apply-plan reuses apply_pending; e2e install+uninstall+failure-path browser-verified; final Codex holistic review folded (2 fixes) + 2 follow-ups spawned (mock update banner, figma-arm tree registration). --gui = localhost server (nonce+allowlist+argv) serving the bucket-browser prototype - DECISION (2026-07-17)](session_2026-07-17_gui-installer-design.md)
- [** ACTIVE ** FIXED the 4 honesty defects from the four-product gap analysis (Jonah ruled fix-defects over the capability gaps) - the false pass grew into a fail-closed BAN_SCANNERS registry ({name,kind,scan} table; both scan loops + label + count DERIVED from it, so a ban name with no invoked scanner is not expressible; drift guard PROVEN to throw); dead font list deleted (vocabulary safe in fontshare-reference SKILL.md); README's nonexistent `npx sidecoach detect` removed; lotus no longer recommends Inter. Codex found 3 MORE false-certification sites I missed + that tracked dist/ shipped the bug + a latent harness trap; golden delta PROVEN pure-removal before recapture. 66 suites + tsc + 5/5 harnesses + behavioral probe; 5 Codex rounds to GO. Self-analysis: I fixed the instance, not the class - PROJECT (2026-07-16)](session_2026-07-16_honesty-defect-fixes.md)
- [** ACTIVE ** Four-product gap analysis (taste-skill, visual-explainer, slop.md, hallmark) - convergent gap = NO generative aesthetic layer (all our assets are negatives; we can read a color, not choose one); plus visualizer-authorship gap (we own the guard+contract+blocking gate and zero machinery to build the artifact) and anti-sameness rotation (our session memory is write-only); slop.md maps onto our worst lens (subjective R0.188) and we detect ~4 of its ~100 tells. 4 VERIFIED honesty defects found in OUR code: ban-identical-card-grids false-passes forever + summary claims it clean, loadFontReflexReject is dead code (our cheapest detector), README claims a nonexistent `npx sidecoach detect`, lotus anti-slop tells the model to use Inter. Meta: this is the FOURTH borrow list and named-vibe-variants.md already captured ~14 of these gaps on 2026-05-25, unwired - PROJECT (2026-07-16)](session_2026-07-16_four-product-gap-analysis.md)
- [** ACTIVE ** Bucket browser SHIPPED TEAR fixed (80x24) - gum `--height` was derived from the ROW COUNT ALONE (`rows+1`, hardcoded cap 22), ignoring the 5-6 row header render_screen itself prints, gum's own chrome, and the real terminal height; deep screens drew 28 rows into 24, the terminal scrolled, gum's screen model desynced and the orphaned top line was never repaired (duplicate "Up to date"/"Quit", stale cursor). Height now = min(rows, term_rows - BR_HDR_LINES - gum_hdr - 3 - dots), floored at 3; header BUFFERS-AND-COUNTS itself (a predictor would go stale into a torn screen); too-tall screens paginate inside gum. The 110 render checks were structurally blind: a pty BYTE capture cannot see a terminal-level scroll (the bytes are all correct) AND the harness drives at 130x60 where a 28-row frame fits. New frame-fit assertion at 24/20/30 rows, NEGATIVE-CONTROLLED (old math turns 6 checks red). 127 checks - PROJECT (2026-07-16)](session_2026-07-16_browser-gum-viewport-overflow-fix.md)
- [** ACTIVE ** Installer BUCKET-BROWSER redesign - APPROVED design (nested screens, drill-all-the-way-down, unified first-run+manage, staged-then-apply); replaces the flat ~30-item list with siloed buckets (active/partial/not-installed rollup + drill-in + per-item toggle + install-all/uninstall-all). Spec at docs/superpowers/specs/2026-07-16-installer-bucket-browser-design.md. SHIPPED + MERGED to main 2026-07-16 (b25c1f0f, not pushed) - browser IS the installer; run_tui_gum/fresh_flow/returning_flow deleted; --help generated from the tree; all 12 owners honor per-hook off-list - DECISION (2026-07-16)](decision_installer_bucket_browser.md)
- [** ACTIVE ** Stage-3b DONE + committed (1c8f6569, direct-to-main) - packaged the last 7 unmanaged hooks: NEW chrome + figma app components + justify-watch-standing-by -> justify (global, app-wirings pattern); beats-rebuild + beats-staleness-guard KEPT project-scoped (not globalized); fixed a latent Stage-2 set-e abort in the cluster HOOK_OFF reconcile (rm_hook_if_ours now returns 0); both cosmetics done (config summary core-only, returning-flow cluster drill-in); unmanaged-hook audit == 0; independent-reviewer gate (Codex broken on this box). Full record in the execution beat - PROJECT (2026-07-15)](session_2026-07-15_stage3b-execution.md)
- [** ACTIVE ** Bash tool ignored the nvm default (node v12) - FIXED at the root with claude/hooks/node-path-default.sh (SessionStart -> $CLAUDE_ENV_FILE export PATH, makes the nvm default the ONLY nvm dir on PATH). The specced `codex` PATH shim provably CANNOT work (no dotfiles dir precedes real codex at PATH position 8; ~/.claude/cmux is cmux-injected, not on the Bash tool's PATH) - Jonah ruled the hook instead. node v12.22.12 -> v20.19.6, bare `codex --version` -> codex-cli 0.142.5; fixes npm/npx/every global node CLI, not just codex. Never prepends (would bypass the cmux node shim). 3 rounds real Codex (6 findings folded, round 3 CLEAN); parity green. NEEDS A SESSION RESTART - PROJECT (2026-07-16)](session_2026-07-16_node-path-default-hook.md)
- [** ACTIVE ** Beats evolution STAGES 4+5 IMPLEMENTED - incremental vector reuse (full 171.6s -> unchanged/offline 0.56s) + beats-rebuild.sh debounced PostToolUse background compile + beats-staleness-guard.sh SessionStart verify guard + project-scoped settings wiring; both ratified verify clauses proven E2E; suites green (compile 43, search 44, hooks 20); scorer 45/48 exit 0; Codex 4 rounds CLEAN - PROJECT (2026-07-02)](session_2026-07-02_beats-stage4-5-hooks-implemented.md)
- [** ACTIVE ** Beats evolution STAGE 3b LOCAL-EMBEDDINGS HYBRID - qwen3-embedding:0.6b via local ollama + RRF fusion over lexical+vector feeding the existing supersession/index/graph pipeline; recall 79.2% -> 93.75% (45/48, GATE PASSED, scorer exit 0) after Jonah's q21 corpus-truth ruling (commit 71157133); residual misses = documented semantic frontier (q03x2 mode-all 2-hop, q10p paraphrase); FAIL-SOFT lexical-only stage-3a parity when embedder down; suites green; Codex CLEAN - PROJECT (2026-07-02)](session_2026-07-02_beats-stage3b-hybrid-embeddings.md)
- [** ACTIVE MISSION ** Sidecoach Option B CONVERGENCE mandate - Jonah chose full convergence to ONE engine; autonomous cmux-teams loop, Codex adversarial reviewer, 10-version handoff rule, hard bar = beat oracle on every axis - DECISION (2026-06-24)](session_2026-06-24_sidecoach-option-B-convergence-mandate.md)
- [** ACTIVE PLAN ** Sidecoach Option B convergence PLAN - target architecture (registry spine + scanner as rendered core) + 6 staged gates + guardrails; green baseline 60 suites @774ab884 - DECISION (2026-06-24)](session_2026-06-24_sidecoach-option-B-convergence-PLAN.md)
- [** ACTIVE Stage 5/6 ** Sidecoach Stage5/6 KICKOFF grounding - Stages 1-4 DONE (registry=engine, 57+ suites green @44f37d68); honest scorecard baseline; the 2 clean "oracle-beats-us" subjective classes (marketing-buzzword precision-safe PRIMARY, side-stripe conditional); discipline carried forward - PROJECT (2026-06-25)](session_2026-06-25_stage5-6-kickoff-grounding.md)
- [** ACTIVE Stage 5 team ** Sidecoach Stage5 TEAM DISPATCHED - buzzword (marketing-buzzword PRIMARY) + sidestripe (conditional) builders running; lead holds Codex review + frozen-90 milestone + Stage 6; eval pipeline confirmed working - PROJECT (2026-06-25)](session_2026-06-25_stage5-team-dispatched.md)
- [** ACTIVE rebuild ** Sidecoach Stage 5a BUZZWORD v2 REBUILD - v1 collapsed on frozen-90 (r0.125/p0.25, overfit homogeneous SaaS dev corpus); rebuilt with 27-page register-diverse disjoint dev corpus (9 registers) + holistic graded-density redesign (content-text scope, testimonial-excluded, vacuity-tiered taxonomy); steps 1-2 done, distribution generalizes right (docs/editorial low, marketing-ai high), awaiting lead labeling to calibrate - PROJECT (2026-06-25)](session_2026-06-25_sidecoach-buzzword-v2-rebuild.md)
- [** ACTIVE ** Sidecoach BUZZWORD REBUILD mandate - Jonah rejected revert/keep/defer after the held-out collapse (dev r0.44/p1.0 -> frozen r0.125/p0.25); MAKE IT WORK via a richer diverse dev corpus + graded detector, no frozen-90 peeking, beat oracle r0.5/p0.4 - DECISION (2026-06-25)](session_2026-06-25_buzzword-rebuild-mandate.md)
- [** ACTIVE v3 ** Sidecoach BUZZWORD v3 PRECISION plan - v2 won recall (frozen 0.875>imp 0.5, lifted aggregate subjective recall to a SIGNIFICANT win) but lost precision (0.333<0.4); v3 = concreteness counter-signal on DEV + FRESH held-out (frozen-90 spent) - DECISION (2026-06-25)](session_2026-06-25_buzzword-v3-precision-plan.md)
- [** START HERE context ** Sidecoach S5 INTEGRATION GAP + plan - DECISION (2026-06-24)](session_2026-06-24_sidecoach-S5-integration-gap-and-plan.md): oracle-beating scanner proven in eval/…
- [** ACTIVE ** Two Stop gates handled - visual override (standing eval-fixture rule) + Stage 3b hook packaging deferred to integration - PROJECT (2026-07-24)](session_2026-07-24_two-stop-gates-visual-and-registry.md)
- [** ACTIVE ** Distributability manifest+metadata LEAD-VERIFIED - distrib shipped .claude-plugin/plugin.json (repo-root FORCED by the plugin schema - a manifest cannot reference files outside its root, and only the repo root reaches claude/skills/sidecoach without sweeping 18 sibling skills; version/homepage/author.email/agents OMITTED not invented) + a package.json `files` allowlist [dist,bin,data,reference] that cuts npm pack from 196.9MB/6132 files to 5.7MB (eval/mcp-server/node_modules excluded BY CONSTRUCTION - an allowlist ships only what it lists) + absolute paths removed from src (grep exit 1). Codex folded reference/ into the allowlist (runtime read by reference-loader). Left OPEN correctly: the /sidecoach <phrase> path reads claude/hooks/sidecoach-lanes.json OUTSIDE the package - the biggest remaining self-contained-plugin blocker, pre-existing, out of scope. Verified: manifest parses, allowlist present, src clean. Not re-run: npm pack (allowlist guarantees the exclusion). MEMORY.md WRITE-STORM flagged by distrib (its index edits clobbered twice by concurrent read-modify-writes) - no beat lost (files never collide), reconcile-at-integration adopted - PROJECT (2026-07-24)](session_2026-07-24_distributability-plugin-manifest-package-metadata.md)
- [** ACTIVE ** Stage 4b Ground B live-wiring LEAD-VERIFIED - groundb wired default-typeface Ground B (brand-mismatch) from DESIGN.md typography.<role>.family tokens (only plausible source; via parseDesignMd YAML not project-context flat parser) -> loadCommittedFontFamilies (NEW in project-context, drops system/generic leads by reusing the scanner SYSTEM_FONT_STACK_FAMILIES, single-sourced) -> run-validator + audit-rendered -> the existing scanner seam. SCANNER CLASS TOUCHED ZERO LINES; BRAND_PRESENCE_MIN=0.25 + Ground A untouched. LEAD INDEPENDENTLY PROBED the sacred no-false-fire property (not trusting the teammate test): no DESIGN.md -> [] silent PASS; SYSTEM-ONLY DESIGN.md -> [] silent PASS (declaring a system stack is not choosing a brand); real committed family Satoshi -> surfaced PASS (not vacuously silent). Certified A5a STILL R=1.000 P=1.000 FP=0/18 after wiring. Codex folded a real High: audit-rendered blind process.cwd() fallback could leak a committed family from an UNRELATED project (context=A no-DESIGN.md, cwd=B with-typography) and false-fire - fixed by passing projectPath in the orchestrator audit call (idiom already used twice there). NOTE: groundb touched sidecoach-orchestrator.ts (1 line) - the SAME file the simplification wave will edit for the dead-router delete; sequence accordingly. 76 suites green. Not committed - PROJECT (2026-07-24)](session_2026-07-24_stage4b-groundb-live-wiring.md)
- [** ACTIVE Phase 1 ** Simplification dead-code removal - Steps 1-2 + delete-side orphan tests (dead handler dupes, dead router modules, processWithEntryPoint strip); plan drift confirmed (t20-convergence-loop now in gate) - PROJECT (2026-07-24)](session_2026-07-24_simplification-phase1-deadcode.md)
- [** ACTIVE DECISION ** Vocab collapse = ALIAS MAP (option B, zero breaking - verbs+NL+lanes, thin PHASE_ALIASES so old phase words still route, ~300 lines + modes drift gone; rejected hard-cut/breaking + doc-only-relabel) & Plugin coupling = VENDOR WITH BUILD-TIME SYNC (copy claude/hooks/sidecoach-lanes.json into sidecoach/data/ + repoint the orchestrator ~L1014 read, GENERATED from the canonical source at build so no drift; rejected two-hand-maintained-copies + leave-install-coupled). Jonah ruled both at the wave-1 boundary - DECISION (2026-07-24)](decision_2026-07-24_vocab-collapse-and-plugin-coupling.md)
- [** ACTIVE ** Plugin coupling FIXED (decision 2) - vendored claude/hooks/sidecoach-lanes.json into sidecoach/data/ (byte-exact, committed like dist/), repointed the /sidecoach <phrase> runtime read to the package-local copy (path.resolve(__dirname, "..", "data", ...)), new generate-lanes-data.ts generator + fail-fast --check drift gate wired into npm run build (build exits 3 on canonical drift, stronger than a literal generate-validators mirror - Codex Medium folded). No lane resolution changed; 76 suites green; npm pack now ships data/sidecoach-lanes.json. Next target: 3 dogfood dist scripts still escape to ../../marketing-site - PROJECT (2026-07-24)](session_2026-07-24_plugin-coupling-vendor-lanes-data.md)
- [** ACTIVE ** Stage 3b detect hook PACKAGED as OPT-IN - wired sidecoach-detect into browser-tree.json (hooks list + hook_desc + hook_owner=sidecoach) + install.sh (the picked sidecoach install_app_hooks line + deactivate_sidecoach) + app-wirings.json (PostToolUse Write|Edit|MultiEdit, timeout 120); registry audit exit 0, test-component-browser 127/0, test-sidecoach-detect 38/0. OPT-IN via a default-off seed (adds detect to HOOK_OFF only when _AMPERSAND_HOOK_OFF is UNSET) so a plain install leaves the per-edit scan UNWIRED (proven end-to-end against the real install_app_hooks); the browser per-hook toggle overrides it. Codex HIGH ESCALATED to Jonah (not silently patched): the browser MASTER-LEAF install still arms it (apply_plan leaf_install forces every owned hook on) - closing it needs a new default-off-hook concept in browser-lib.sh, a shared-engine design call. Not committed - PROJECT (2026-07-24)](session_2026-07-24_stage3b-detect-packaging.md)
- [** ACTIVE ** Vocab collapse GAP5 option B SHIPPED (not committed) - renamed module-private SLASH_COMMANDS -> PHASE_ALIASES (back-compat alias layer, dropped the dead verb-shadowed `craft` entry) + de-advertised the retired modes (forge/kiln/bloom/trim/ralph) from bin/sidecoach.js list/help while KEEPING getMode resolution so they still resolve (ONE RULE). SKILL.md reframed "parallel command surfaces" -> verbs+NL typed, phases as aliases. BEHAVIOR PRESERVATION PROVEN by a before/after COMPILED-dist capture: every phase word + every retired-mode word + all 21 verbs + 5 phrases resolve to the EXACT same target (diff IDENTICAL); `craft` proven to route through the VERB, so dropping the dead phase entry changed nothing. routing-snapshot verify OK (zero drift - golden tests classifier+verbs, untouched); build clean; npm test 76 suites (== baseline), 0 failed; source diff +59/-47. HONEST PRIZE = a SURFACE collapse, NOT the plan's ~300 lines: modes.ts deletion (-193) BLOCKED (mcp-server/src/registries.ts imports it, Decision B out of scope) and getAvailableCommands phase half (-90) BLOCKED (off-limits orchestrator list handler consumes it) - both left as-is with reason. In-session /sidecoach list still groups phases (needs an orchestrator edit). Codex-reviewed - PROJECT (2026-07-24)](session_2026-07-24_vocab-collapse-phase-aliases.md)
- [** ACTIVE DECISION ** mcp-server RETIRE (Jonah 2026-07-24, REVERSING his own 2026-07-15 wire-up ruling) - after re-reading the origin (goal = a hardened host-callable EXTERNAL MCP surface for sidecoach rule-tools; built T-0018 79 tests, extended, but NEVER wired to a consumer), Jonah confirmed the revisit condition the retire analysis set ("a concrete external MCP client appears") was NOT met, so retire. Eyes-open reversal, not drift. Retiring the 4157-file dir inherently collapses the TRIPLICATED classifier -> DUPLICATED (dead 3rd copy keyword-resolver.ts lives inside). modes.ts NOT deleted here (CLI still resolves retired-mode words via getMode per vocab-collapse; its deletion is a follow-up needing mode->PHASE_ALIASES migration first). The beat decision_sidecoach_mcpserver_fate.md has a 7-step removal sequence but stamped @0b65e983 (HEAD far past) so EVERY current-state claim (tether line in run-tests.ts, docstrings, paths) must be RE-VERIFIED before executing. QUEUED behind stage3c (both edit run-tests.ts - collision) - DECISION (2026-07-24)](decision_sidecoach_mcpserver_fate.md)
- [** ACTIVE ** Stage 3c registry consolidation SHIPPED (not committed) - made product-rule-registry the SINGLE rendered manifest: new RENDERED_RULE_MANIFEST + resolveRenderedRule(scannerRule)->{lens,ruleId,severity,blocking,source} that detect + audit-rendered resolve rendered findings through, closing the two orphans (nested-cards had NO entry; low-contrast scanner-name unaliased to a11y.color-contrast). nested-cards registered AUDIT-ONLY (separate manifest, ruleId:null) NOT a RAW_RULE - a rendered-scan RAW_RULE trips the inverse invariant -> RENDERED_BACKED_RULE_IDS -> run-validator would newly consume it (behaviour change avoided). Added --list-rules CLI flag (60 rules + 9 bindings, exit 0) + a no-orphan test (imports scanner OBJECTIVE/SUBJECTIVE_RULES, asserts no-orphan/no-phantom/behaviour-lock/audit-only/alias-consistency). Behaviour-preserving PROVEN: registry severities exactly match old audit-rendered mapping; defect fixture->blocked, clean->clean; validators.generated.ts byte-identical; 76 suites green; generate-validators --check no drift. Codex real-verdict OK on all 3 questions (no firing change / all registered / verdict-exit intact), 1 LOW folded (--list-rules stdout doc). Did NOT edit run-tests.ts (test went in existing product-rule-registry.test.ts) so the mcp-server-retire run-tests.ts COLLISION does not exist. Next target: eval scan + scanners still resolve by rule-name outside the registry (now provably covered) - PROJECT (2026-07-24)](session_2026-07-24_stage3c-registry-consolidation.md)
- [** ACTIVE - BLOCKED on install.sh ** sidecoach/mcp-server code-surface RETIRED + verified, but Codex caught a live installer surface the beat's premise MISSED - executes Jonah 2026-07-24 RETIRE reversal: severed the run-tests.ts required-suite tether (moved L30->L40) + reworded header THREE->TWO copies, git rm the 4157-file dir + inert .mcp.json, de-misled 4 classifier docstrings, all 8 verify checks GREEN (npm test 76->75, live Python parity 23 cases, build clean). Triplicated classifier -> DUPLICATED. **BLOCKER (Codex HIGH, unfolded by design - out of my 7-file scope + a design call): install.sh:4737-4758 still BUILDS+REGISTERS the server (the reversed 2026-07-15 wire-up landed its install.sh half, contradicting the beat's "install.sh never registers it") and :1720-1729 deregisters it - the deletion breaks `install.sh --only sidecoach` and orphans mcpServers.sidecoach in ~/.claude.json on already-installed machines.** Staged, NOT committed, MUST NOT commit until LEAD resolves install.sh. LOW: bin/sidecoach.js:42 stale comment. modes.ts residue deferred - PROJECT (2026-07-24)](session_2026-07-24_mcpserver-retirement.md)
- [** ACTIVE ** modes-delete (finish vocab collapse) IN PROGRESS - mcp-server retirement (c9985f6f) unblocked modes.ts deletion. Investigation: modes are a CLI-ONLY surface (only bin/sidecoach.js imports getMode; orchestrator never did), current MODES = forge/kiln/bloom/trim/ralph (NOT canvas - cut 2026-07-23, stays unknown), sidecoach-modes.json hook is DEAD (nothing live reads it), getAvailableCommands FUNCTION must stay (live showInteractiveMenu@1560 + slash-command.test consume it) so only the list-handler phase RENDER block is deletable. Plan = fold modes into PHASE_ALIASES (byte-identical chains), drop getMode from CLI, delete modes.ts + sidecoach-modes.json + list-handler phase half. ONE RULE = zero routing change proven via before/after CLI capture. Baseline 75 suites - PROJECT (2026-07-24)](session_2026-07-24_modes-delete-collapse.md)
- [** ACTIVE ** default-off hook ENGINE concept SHIPPED (not committed) - closed the Stage 3b master-leaf gap by teaching browser-lib.sh a `default_off_hooks` tree property PARALLEL to `pinned_hooks`: loader emit BR_DEFAULTOFF_*, reset-loop unset, a `hook_default_off` helper, and ONE guard on the apply_plan leaf-install force-enable (`elif [ "$leaf_install" = "1" ] && ! hook_default_off "$h"`). browser-tree.json gains `default_off_hooks: ["sidecoach-detect"]`. Now a browser MASTER-LEAF Sidecoach install emits `INSTALL sidecoach sidecoach-detect` (detect INSTALL-but-off, other 6 force-enabled unchanged) instead of a bare `INSTALL sidecoach` that armed the per-edit scan. default_off differs from pinned: still owned/toggleable/probe-read, ONLY exempt from the leaf force-enable - so every non-default-off hook is byte-identical under every install path (per-hook toggle both dirs, CLI seed, pinned all untouched). test-component-browser 139/0 (was 127/0, +12). Codex-reviewed (exit 0, no blocking; the bulk "Install all" path deliberately still opts detect in, locked by a test). Files: browser-lib.sh, browser-tree.json, test-component-browser.sh - PROJECT (2026-07-24)](session_2026-07-24_defaultoff-hook-engine.md)
- [verify-before-done visual arm narrowed to skip eval fixtures / test probes / OS-temp scratch. The Stop gate fired 3x in one session on sidecoach/eval/fixtures/**/*.html and *.test/*.spec writes (no rendered surface). Added an anchored (^|/)eval/(fixtures|corpus)/ exemption (catches the cwd-relative case "/eval/" substring missed), a .test./.spec. basename declassify (visual->code), and a DIRECT-temp-scratch declassify (nested repos under /tmp keep arming visual). Fix is arm-side only; stop gate untouched. Codex 2 rounds: 1 recall finding folded (tmp subtree prefix -> direct-child). 6 suites green, test-verify-before-done 145->168 both-ways negative-controlled. PROJECT (2026-07-24)](session_2026-07-24_verify-arm-eval-test-tmp-narrowed.md)
