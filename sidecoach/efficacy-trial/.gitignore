# Installed dependency (pinned in package.json; re-derive with `npm install`).
node_modules/

# Per-brief scratch dirs the arm builder creates to run sidecoach-monitor in. Derived
# from the briefs by lib/product-md.mjs and regenerated by build-arms.mjs; the PRODUCT.md
# content that mattered is already recorded in arms/manifest.json via the payload hashes.
workdirs/

# Full-page screenshots for the EXPLORATORY judging pass. Regenerate with
# `node shoot.mjs --contrast S-vs-P --fullpage`; the verdicts they produced are committed.
judge/shots-fullpage/
