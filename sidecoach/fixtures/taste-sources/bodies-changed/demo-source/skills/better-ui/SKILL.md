# better-ui (fixture body v2 - CHANGED)

This body differs from v1 so the diff-since-last step flags it as CHANGED.

Real principles a taste skill would state:
- Concentric radius: outer radius = inner radius + padding.
- Use shadow, not border, for depth.
- Scale to 0.97 on press (changed value).
- Tabular-nums for changing numbers (new rule).

```css
.button { transition: transform 180ms ease-out; }
```
