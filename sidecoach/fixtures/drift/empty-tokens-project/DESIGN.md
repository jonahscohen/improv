---
typography:
  body:
    family: "system-ui, sans-serif"
    weights: [400, 600]
---

# DESIGN.md - no governed tokens

This DESIGN.md parses (valid frontmatter) but declares NO color / radius / spacing /
motion tokens. There is no readable baseline to compare against, so the tool must fail
closed with "cannot assess", never a vacuous "no drift".
