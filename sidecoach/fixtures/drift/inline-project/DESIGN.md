---
colors:
  brand:
    red: "#DC2618"
    ink: "#1A1F1B"
rounded:
  sm: "4px"
  md: "8px"
spacing:
  sizes:
    "4": "16px"
motion:
  duration:
    fast: "180ms"
---

# DESIGN.md - inline-style fixture

Proves the tool scans custom properties defined in an HTML `<style>` block, not only
in `.css` files. The inline block below carries one sanctioned token and one drift.
