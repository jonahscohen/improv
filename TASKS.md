# Dotfiles tasks

<!-- Managed by /task-list skill. Hand-edits welcome; preserve the structure. -->
<!-- Last ID: T-0018 -->

## dotfiles
### Active
- [ ] T-0001 [P2] 2026-05-25 research QuiverAI implementation methods
- [ ] T-0006 [P3] 2026-05-27 second-fix-gate.sh EXEMPT list looks for `.claude/hooks/` (with leading dot) but the dotfiles source path is `claude/hooks/` (no dot). Edits to hook source files in this repo trip the gate as if they were code fixes. Fix: add `claude/hooks/` and `claude/skills/` substrings to EXEMPT, or use a regex that handles both leading-dot and no-leading-dot variants. Origin: t4-exempt-paths teammate side observation during T-0004 (2026-05-27).

## sidecoach
### Active
- [ ] T-0007 [P2] 2026-05-28 Codex + Gemini CLI orchestration for sidecoach (large, scope-heavy). OMC's distinctive capability surfaced in the 2026-05-28 research is `omc team N:codex/gemini/claude` spawning real worker panes for different CLIs side-by-side via cmux's tmux-shim. Sidecoach currently spawns only Claude teammates. Adding Codex + Gemini means FULLY accommodating them, not a half-measure: (a) detect which CLI is available on PATH, (b) install/auth flow per CLI (codex login, gemini API key), (c) prompt-format adapter per CLI (each one has its own quirks - tool-use formats, system-prompt conventions, context limits), (d) output parser per CLI (the cmux pane integration sees stdout - need to parse each CLI's progress/completion signals), (e) capability map (which sidecoach flows can run on which CLI - design-domain flows likely Claude-only, generic refactor/research flows could fan out), (f) model-tier routing per CLI (each provider has its own Haiku/Sonnet/Opus analog), (g) cost-tracking per CLI, (h) graceful degradation when a CLI is missing. Start with a design memo before any code - the wrong abstraction here would lock sidecoach into a one-provider-only ceiling. Origin: omc-research team findings 2026-05-28.
- [x] T-0008 [P1] 2026-05-28 DONE 2026-05-28 (Jonah) UserPromptSubmit keyword interception hook for sidecoach verbs (port of OMC's keyword-detector architecture, verified 2026-05-28 by arch-detective team). Solves the 2026-05-20 finding that skill auto-triggers don't fire reliably - the fix is to bypass the skill layer entirely and intercept at the shell hook layer. v1: bash + python3 script at `claude/hooks/sidecoach-keyword.sh`. Pipe stdin through sanitizer (strip ``` fenced code blocks, strip inline backtick spans, strip URLs, strip XML tag bodies, strip transcript markers) BEFORE regex matching. Then hyphen-aware word-boundary regex per verb (22 verbs in claude/hooks/sidecoach-verbs.json - the canonical registry T-0010 will consume). On hit, emit `{"hookSpecificOutput":{"hookEventName":"UserPromptSubmit","additionalContext":"User intends to invoke the sidecoach <verb>NAME</verb> flow. Route accordingly."}}`. Context-aware suppression blocks "what is X", "how to use X", "how do I X", "tell me about X", "X is a", "explain X", "define X". Multi-verb tie-breaks to first verb in registry order. Tests at claude/hooks/test-sidecoach-keyword.sh - 74/74 pass covering 22 verb fires + 4 fenced + 4 inline-tick + 4 URL + 3 XML + 3 transcript-marker + 11 informational + 13 word-boundary + 3 tie-break + 3 zero-match + 4 mixed-sanitization. Wired in settings.json UserPromptSubmit after multiple-choice-inject-prompt.sh. Symlinks added to ~/.claude/hooks/.
- [x] T-0009 [P1] 2026-05-28 DONE 2026-05-28 (Jonah) Phase-gated retry loop with hard stop conditions in polish/audit/critique flows. New `sidecoach/src/retry-control.ts` exposes `computeErrorSignature` (sha256 of {validator, sorted failedRules, filePath} sliced to 12 chars), `evaluateHaltConditions(state, config)` (max_cycles first then identical_error_loop), and `recordIteration`/`buildHaltResult`/`attachRetryStateToResult` helpers. Each of the three handlers (flow-handler-tactical-polish, flow-handler-multi-lens-audit, flow-handler-design-critique) reads `retryConfig`/`retryState` from `context.metadata`, checks halt conditions at the top of `execute()`, runs validators, then writes the next state into `result.executionMetadata.enhancedContext.retryState`. Defaults: maxCycles=5, identicalErrorThreshold=3. Halt logs `[flowId] halted: max cycles reached (cycleCount=N maxCycles=M)` and `[flowId] halted after 3 identical attempts at signature=X (validator=Y, attemptCount=3)`. Tests in `sidecoach/src/__tests__/t9-retry-control.test.ts` cover the four mandated scenarios + handler integration: 52/52 pass. tsc clean.
- [x] T-0015 [P2] 2026-05-28 DONE 2026-05-28 (Jonah) Legacy flow cull pass. 38 -> 26 flows. Classification: 12 DUPLICATEs of lettered canonicals (flow1->flowO, flow2->flowJ, flow3->flowK, flow5->flowK, flow6->flowP, flow8->flowR, flow9->flowI, flow10->flowG, flow11->flowF, flow12->flowM, flow13->flowN, flow14->flowQ) removed; 2 RENAMEs (flow4->flowY_explore_discovery, flow7->flowZ_design_component) preserved as genuinely separate flows. Lettered detectors flowJ/K/R/Q/G extended to absorb legacy keyword matchers so existing user phrasings still resolve. Files: sidecoach/src/{flows.ts, types.ts, sidecoach-orchestrator.ts, flow-handlers-core.ts, flow-handlers-extended.ts, intent-detector.ts, intent-detector.test.ts, model-routing.ts, flow-prerequisites.ts, flow-handler.ts, slash-command-router.ts} + 3 test files (t12, sprint5-disambiguation, orchestrator-slash-command) + SKILL.md/CHEATSHEET.md/marketing-site cheatsheet.html (count updated 36 -> 26). Tests: t9-retry-control 52/52, t12-model-routing 54/54, t13-bench-harness 46/46, intent-detector 8/8 (100%), sprint3/sprint5/orchestrator-slash all PASS. tsc clean (only pre-existing T-0016 rootDir warnings remain). T-0013 BENCH_FLOWS unaffected (uses only lettered IDs). Beat: session_2026-05-28_t0015_legacy_flow_cull.md.
- [x] T-0018 [P1] 2026-05-28 DONE 2026-05-28 (Jonah) Sidecoach custom MCP server shipped, hardened end-to-end. Layout: `sidecoach/mcp-server/{DESIGN.md (580+ lines, 10 sections), package.json, tsconfig.json, src/{index.ts, server.ts, errors.ts, logger.ts, schemas.ts, registries.ts, keyword-resolver.ts, tools/(10 tool files + index.ts + types.ts)}, __tests__/{unit, integration, fault-injection, harness.ts, run-tests.ts, smoke.sh, SMOKE_TRANSCRIPT.txt}, README.md}` plus project-level `sidecoach/.mcp.json` for auto-discovery. Stack: `@modelcontextprotocol/sdk` v1.29 + `zod` v3.23, stdio transport. 10 tools shipped 1:1 with DESIGN.md: sidecoach_list_verbs, sidecoach_list_modes, sidecoach_list_flows, sidecoach_resolve_keyword, sidecoach_validate_polish_standard, sidecoach_validate_extended_domain, sidecoach_validate_taste, sidecoach_get_cost_ledger, sidecoach_get_cheatsheet, sidecoach_get_flow_metadata. Quality bar checklist - all met: (a) zero unhandled exceptions reach transport - uniform try/catch guard wraps every handler, converts throws to structured tool errors with requestId; (b) Zod schemas validate every tool input BEFORE handler body runs (length caps: 2MB html/css, 4KB phrases, 128 chars IDs); (c) per-tool timeouts (5s for read tools, 30s for validators) via Promise.race + AbortController, configurable via SIDECOACH_MCP_TIMEOUT_MS env; (d) structured ToolError taxonomy (INVALID_INPUT, DOWNSTREAM_UNAVAILABLE, VALIDATOR_FAILURE, TIMEOUT, INTERNAL_ERROR) returned as JSON in isError:true content blocks; (e) concurrent-request safety - registries immutable post-load, ledger read-only from MCP surface, per-request AbortController + UUID requestId, 10-way parallel test proves no state corruption; (f) graceful degradation - missing registries log warn at startup and return DOWNSTREAM_UNAVAILABLE per-call (server stays alive); validators that throw return VALIDATOR_FAILURE with redacted message; (g) SIGTERM/SIGINT shutdown aborts in-flight + 2s grace + clean exit 0, double-SIGTERM forces exit 1; (h) logger writes structured JSON-per-line to stderr only (stdout reserved for JSON-RPC), redacts Bearer tokens / api_key= / sk-* / password= patterns, never logs tool inputs/outputs (only byte counts). Test results: 79/79 PASS broken down by category - unit 63/63 (errors 7, logger 6, schemas 11, keyword-resolver 13, tools 26), integration 6/6 (in-memory MCP client + real-subprocess stdio with initialize -> tools/list -> 10x tools/call -> invalid call -> SIGTERM exit 0), fault-injection 10/10 (registry-missing 3, validator-throw 4, timeout-and-concurrency 3). Smoke transcript captured at `sidecoach/mcp-server/__tests__/SMOKE_TRANSCRIPT.txt` showing live subprocess initialize handshake + tools/list (10 tools) + 4 successful tool calls + 1 INVALID_INPUT error (structured, not crash). `npx tsc --noEmit` clean. Imports the parent sidecoach via `../../dist/` (decoupling build state); README documents the prerequisite + 2 settings.json snippets (absolute + env-driven). User mandate met.
- [ ] T-0014 [P3] 2026-05-28 Terminal CLI binary `sidecoach` mirroring the slash-command surface. `sidecoach craft <feature>` invokes the same flow as `/sidecoach craft <feature>`. Build at sidecoach/bin/sidecoach.js as commander-based CLI. Verbs: all 22 verb commands + composite presets + teach/document setup commands + help. Per OMC's CLI parity pattern (terminal `omc ...` mirrors `/...` slash). Sidecoach's primary venue is in-session, but CLI enables scripting (CI integration, batch processing, automation). Distribution: install via existing install.sh, symlink to ~/.local/bin/sidecoach or similar. Filed at user request 2026-05-28 alongside T-0008-T-0013 roadmap.

### Done
- [x] T-0016 [P2] 2026-05-28 DONE 2026-05-28 (Jonah) Benchmark runner is now ledger-aware. `runFlowOnFixture` resets the session ledger BEFORE `handler.execute()`, then filters `getSessionLedger()` by flowId after. If matching CostEntry rows exist, the runner sums tokensInput/tokensOutput/estimatedCost from those entries, uses the entry's `model` + `tier` directly (overrides applyModelSelection's prediction if a handler downgraded mid-call), and stamps `tokenSource: 'live'`. If empty, falls back to `estimateSyntheticTokens` AND writes a single synthetic `trackCost` entry so the ledger shape mirrors the live path - then stamps `tokenSource: 'synthetic'`. `FlowRunResult.tokenSource: 'live' | 'synthetic'` added to types.ts with full inline docs. Tests at sidecoach/src/__tests__/t16-bench-ledger.test.ts: 25/25 PASS covering live path (one call), synthetic fallback (no call), mixed run, ledger reset between flows, multi-call live summing, and error-path-still-stamps-tokenSource. Regression: t13-bench-harness 46/46 (synthRun helper updated with `tokenSource: 'synthetic'`), t12-model-routing 54/54. End-to-end `ts-node --transpile-only` bench run produced baseline-latest.json with all 15 flow rows showing `tokenSource: "synthetic"` as expected (no handler emits trackCost yet). benchmarks/README.md got a "Token source" section explaining the detection logic + forward-compat semantics. Note: full-tsc `npm run bench` is currently blocked by T-0015's mid-stream flows.ts edits breaking FlowId in flow-handler.ts + sidecoach-orchestrator.ts + slash-command-router.ts (out-of-scope for T-0016); transpile-only runs cleanly end-to-end and will resolve when T-0015 lands.
- [x] T-0017 [P2] 2026-05-28 DONE 2026-05-28 (Jonah) verify-before-done.sh over-fire fix shipped. New `is_verification_only(cmd)` helper added to the python block of `verify-before-done.sh`, called from the Bash branch after `is_read_only_command` and before the `write_indicators` check. Two-stage logic: (1) deploy-pattern guard returns False if ANY segment matches `npm run build|deploy|dev|serve`, `npm start`, `vite build`, `next build`, `nodemon`, `tsx watch`, `webpack`, `rollup`, `esbuild` (so chained `<verify> && npm run build` still falls through to write_indicators and fires); (2) ~40 token-anchored regex patterns across test runners, type checks, lint runs, benchmark runs, pure introspection. Exits silently when matched - does NOT clear the verify flag (these commands do not visually confirm prior UI work) and does NOT set it (they are not deploys). New tests at `claude/hooks/test-verify-before-done.sh`: 73/73 PASS covering 31 test-runner skips, 6 type-check skips, 8 lint skips, 5 benchmark skips, 10 introspection skips, 5 deploy/redirect fires, 7 chained edge cases, 1 build look-alike. Regression: `test-validation-guards.sh` 51/51 + `test-multiple-choice-enforce.sh` 29/29 still PASS. Side: documented the `python3 -c '...'` apostrophe trap that broke the file mid-edit when a comment used `that is` style contractions - de-apostrophized fixed it. (done 2026-05-28)
- [x] T-0013 [P3] 2026-05-28 DONE 2026-05-28 (Jonah) Benchmark harness skeleton shipped. Layout: `sidecoach/benchmarks/{fixtures, runner, baselines/archive}` + `tsconfig.bench.json` + `README.md`. Runner files: `runner/types.ts` (BenchmarkRun/FlowRunResult/CompareReport schema), `runner/score.ts` (loadFixture + DESIGN.md YAML frontmatter parse + per-validator scoring), `runner/report.ts` (saveBaseline writes baseline-latest.json AND timestamped archive, compareRuns + formatCompareTable), `runner/run-all.ts` (CLI: --save / --compare / --save-only). 5 starter fixtures covering full taxonomy: `brand-studio` (brand/typography), `saas-dashboard` (product/layout-density), `scroll-landing` (motion/scroll-driven), `form-stress` (accessibility/wcag-aa-forms), `mixed-portfolio` (mixed/register-aware). Each fixture: PRODUCT.md + DESIGN.md + fixture.css + fixture.html + meta.json. Runner invokes flowJ/K/L per fixture (15 flow runs per benchmark), calls applyModelSelection (T-0012) to capture tier, runs PolishStandardValidator + ExtendedDomainValidator + validateTaste directly for granular per-rule numbers, captures retryStateCount from T-0009. Synthetic token costs (v1 - handlers don't call LLMs today; README documents the swap to real ledger when they do). Regression triggers: passRate drop >2% (configurable), criticalViolations +N, cost +20% with same tier. npm scripts: `bench`, `bench:compare`, `bench:save-baseline`, `test:bench`. Tests at `sidecoach/src/__tests__/t13-bench-harness.test.ts`: 46/46 PASS covering discovery, fixture loading, validator scoring, runAll schema conformance, all 4 regression detection modes (passRate drop, noise tolerance, criticalViolations, cost-with-tier-stable), tier-upgrade carve-out, saveBaseline archive, and first-run mode. End-to-end manual verification: `npm run bench` produced valid baseline (5 fixtures, 15 flows, $0.578 estimated cost, harnessVersion=b0a66e312a61); injected scale(0.96) + reduced-motion regression in brand-studio CSS, `npm run bench:compare` correctly flagged 3 regressions and exited 1. tsc clean (main + bench tsconfigs). T-0009 (52/52) and T-0012 (54/54) tests still green. CI hook documented in README but not wired. Out-of-scope follow-ups recorded in beat: handler LLM wiring + ledger swap, fixture expansion to 10-20.
- [x] T-0010 [P2] 2026-05-28 DONE 2026-05-28 (Jonah) Single-page verb cheatsheet shipped. Two surfaces created: (a) `claude/skills/sidecoach/CHEATSHEET.md` (in-repo, linked from SKILL.md line 8) with frontmatter `type: reference`, Section 0 Modes (5 modes - forge/kiln/bloom/canvas/trim), Section 1 Verbs grouped by phase per `sidecoach-verbs.json` (Shape, Build, Review, Tone, Docs, Tactical - 22 verbs total), Section 2 Flows grouped by tier from `flows.ts` (Tier 1-6 + Legacy - 38 flow IDs total), Section 3 "How verbs route to flows" paragraph referencing T-0008's keyword hook and the verb-command-registry; (b) `marketing-site/cheatsheet.html` mirroring the same content with the marketing-site's existing Hanken/JetBrains Mono + ink/cream/red token system. Added `cheatsheet` link to the global nav on index.html, sidecoach.html, beats.html, endow.html, reference.html. Added a `.cheatsheet__group` / `.cheatsheet__table` style block to `marketing-site/styles.css` (~120 lines) with a mobile-collapse pattern for sub-880px viewports. Visual verification via cmux surface:41 (http://localhost:8765/cheatsheet.html): nav loaded with cheatsheet active, Section 0 title and ink Section 3 both render correctly with display serif + on-inverse colors. Sources of truth: `claude/hooks/sidecoach-modes.json` (modes), `claude/hooks/sidecoach-verbs.json` (verbs), `sidecoach/src/flows.ts` (flow registry).
- [x] T-0011 [P2] 2026-05-28 DONE 2026-05-28 (Jonah) Modes-as-positioning naming pass. Five modes chosen: `forge` (shape -> craft -> polish, build new), `kiln` (audit -> critique -> harden -> adapt -> polish, ship-ready), `bloom` (colorize -> delight -> animate -> polish, personality), `canvas` (live -> colorize -> polish -> critique, in-browser iteration), `trim` (quieter -> distill -> clarify -> polish, simplify). Registry in `sidecoach/src/modes.ts` (FlowId chains) mirrored by `claude/hooks/sidecoach-modes.json` (verb chains for the bash hook). Extended `sidecoach-keyword.sh` to match both registries with mode-takes-precedence semantics; on mode match emits `<mode>NAME</mode>` + `<chain>v1,v2,...</chain>` so the receiving session knows the verb sequence. Updated SKILL.md (Modes section + table), marketing-site/sidecoach.html (new Modes section). install.sh now symlinks `sidecoach-keyword.sh` + `sidecoach-verbs.json` + `sidecoach-modes.json`. Tests: 87/87 PASS in `test-sidecoach-keyword.sh` (74 existing verb tests + 13 new mode tests covering each mode firing, word-boundary rejection, informational suppression, mode-over-verb precedence, multi-mode registry tie-break). tsc clean.
- [x] T-0012 [P2] 2026-05-28 DONE 2026-05-28 (Jonah) Per-flow model-tier routing shipped. New `sidecoach/src/model-routing.ts` exposes `TIERS` (haiku/sonnet/opus using latest IDs: claude-haiku-4-5-20251001, claude-sonnet-4-6, claude-opus-4-7), `FLOW_MODELS` (38 entries - one per FlowId), `selectModel(flowId, budget?)` (preferred-with-floor downgrade logic), `trackCost`/`getSessionLedger`/`resetLedger`, `summarizeLedger` (per-flow + per-model breakdown), and `applyModelSelection(flowId, context)` (one-line handler helper that stashes selectedModel into context.metadata). Distribution: 6 Haiku, 16 Sonnet, 16 Opus. Modified 22 handler files (canonical + tier3-tier4 + extended + core multi-handler files) to invoke applyModelSelection() at the top of execute(). For polish/audit/critique (T-0009 retry-control), the call sits ABOVE the halt check so even halt-early paths leave a model-selection record - T-0009's 52/52 tests still pass. Tests at `sidecoach/src/__tests__/t12-model-routing.test.ts`: 54/54 PASS, measured 57.3% token savings on a 10-flow synthetic session vs naive-Opus-for-everything (beats OMC's 30-50% claim). tsc clean.
- [x] T-0002 [P2] 2026-05-25 subagent-aware nudge scoping for memory-nudge.sh + verify-before-done.sh. Approach (a): detect via transcript JSONL - `isSidechain: true` (Agent-tool sidechains) or `teamName` field (cmux teammates); both signals already present, no env var or TTL flag needed. Nudge text suppressed, dirty/needs-verification flags still set. (done 2026-05-27)
- [x] T-0003 [P2] 2026-05-25 bash-guard.sh cmux-eval anchoring. Two-stage Python in bash-guard.sh: (1) strip HEREDOC bodies via quoted-delimiter parsing; (2) walk shell command tokens quote-aware (single+double, backslash-escape inside `"`) to extract only the real `cmux ... eval ...` slice; 17 inner pattern checks now grep against the slice, not the full $CMD. Added 5 T-0003 test cases to test-validation-guards.sh; suite now 51/51 PASS (was 46/46). (done 2026-05-27)
- [x] T-0005 [P2] 2026-05-26 multiple-choice-enforce three over-fire patterns. Fix applied to both multiple-choice-detect-stop.sh (production Stop-event fire site) and multiple-choice-enforce.sh (dormant PreResponse hook): precondition `opt_count >= 3 AND (trailing_q OR trailing_deflection)` with bold_label_count >= 2 retained as independent override; is_binary_question() recognizer for yes/no openers and "X or Y?" forms; trailing_q scoped to 250-char window after last list item with newline-preserving Python regex. 29/29 tests green (24 existing + 5 new). (done 2026-05-27)
- [x] T-0004 [P2] 2026-05-25 second-fix-gate.sh EXEMPT list misses global project memory. Added regex `\.claude/projects/[^/]+/memory/` OR-combined with substring list. (done 2026-05-27)
